# Q 0 : # @categories.pluck(:id)
Query(Category)
.select('id')
# Q 1 : # @categories.pluck(:topic_id)
Query(Category)
.select('topic_id')
# Q 2 : # @categories.where("categories.parent_category_id = ?", @options[:parent_category_id].to_i)
Query(Category)

# Q 3 : # @category.subcategories.select
Query(Category)
.where("category_id = ?")
# Q 4 : # @post.post_actions.where(post_action_type_id: PostActionType.flag_types_without_custom.values).each
Query(PostAction)
.where("post_id = ?")
.where("post_action_type_id = ?")
# Q 5 : # @post.post_actions.where(user_id: @user.id).destroy_all
Query(PostAction)
.where("post_id = ?")
.where("user_id = ?")
# Q 6 : # @post.revisions.last
Query(PostRevision)
.where("post_id = ?")
.return_limit('1')
# Q 7 : # @post.topic.first_post
Query(Topic)
.where("id = ?")
# Q 8 : # @post.topic.topic_allowed_users.pluck(:user_id).include?(@user.id)
Query(Topic)
.where("id = ?")
.select('user_id')
# Q 9 : # @post.topic.topic_allowed_users.pluck(:user_id).include?(@user.id)
Query(Topic)
.where("id = ?")
.select('user_id')
# Q 10 : # @posts.includes(:reply_to_user).includes(:topic).joins(:user).limit(@limit)
Query(Post)
.includes('reply_to_user')
.includes('topic')
.joins('user')
.return_limit('')
# Q 11 : # @posts.map(&:topic_id).uniq.length
Query(Post)
.distinct('')
# Q 12 : # @posts.map(&:user_id).uniq.length
Query(Post)
.distinct('')
# Q 13 : # @posts.unscope(:order).order(sort_order: :desc)
Query(Post)
.order('sort_order')
# Q 14 : # @posts.where("COALESCE(users.trust_level,0) >= ? OR posts.score >= ?", @min_trust_level, @bypass_trust_level_score)
Query(Post)

# Q 15 : # @posts.where("COALESCE(users.trust_level,0) >= ?", @min_trust_level)
Query(Post)

# Q 16 : # @posts.where("posts.score >= ?", @min_score)
Query(Post)

# Q 17 : # @theme.remote_theme.update_from_remote
Query(RemoteTheme)
.where("id = ?")
# Q 18 : # @topic.all_allowed_users.where(id: @user.id).blank?
Query(Topic)
.where("id = ?")
# Q 19 : # @topic.allowed_groups.map(&:id)
Query(Topic)

# Q 20 : # @topic.posts.where(deleted_at: nil).each
Query(Topic)
.where("deleted_at = ?")
# Q 21 : # @topic.posts.where(deleted_at: nil).each
Query(Topic)
.where("deleted_at = ?")
# Q 22 : # @topic.posts.where(user_id: @user.id).order("created_at desc").limit(SiteSetting.get_a_room_threshold).pluck(:reply_to_user_id).find_all
Query(Topic)
.where("user_id = ?")
.order('id, created_at')
.return_limit('')
.select('reply_to_user_id')
# Q 23 : # @topic.tags.order("tags.topic_count DESC").first.name
Query(Topic)
.order('id, topic_count')
.return_limit('1')
# Q 24 : # @topic.topic_allowed_groups.where("group_id IN (
#                                               SELECT group_id FROM group_users where user_id = ?
#                                            )", @user.id).exists?
Query(Topic)
.return_limit('1')
# Q 25 : # @topic.topic_allowed_users.where(user_id: @user.id).exists?
Query(Topic)
.where("user_id = ?")
.return_limit('1')
# Q 26 : # @topic.topic_allowed_users.where(user_id: @user.id).exists?
Query(Topic)
.where("user_id = ?")
.return_limit('1')
# Q 27 : # @topic.topic_allowed_users.where(user_id: user.id).exists?
Query(Topic)
.where("user_id = ?")
.return_limit('1')
# Q 28 : # @topic.topic_users.find_by(user_id: @user.id)
Query(Topic)
.where("user_id = ?")
# Q 29 : # @topic.topic_users.find_by(user_id: user_id)
Query(Topic)
.where("user_id = ?")
# Q 30 : # @user.badges.where(id: Badge::BasicUser).count
Query(Badge)
.where("user_id = ?")
.where("id = ?")
# Q 31 : # @user.email_tokens.unconfirmed.active.first
Query(EmailToken)
.where("user_id = ?")
.return_limit('1')
# Q 32 : # @user.email_tokens.unconfirmed.active.first
Query(EmailToken)
.where("user_id = ?")
.return_limit('1')
# Q 33 : # @user.posts.select("distinct topic_id").where("created_at > ? AND post_number > 1", time_period.days.ago).count
Query(Post)
.where("user_id = ?")
.select('distinct topic_id')
# Q 34 : # @user.posts.where(topic_id: @topic.id).count
Query(Post)
.where("user_id = ?")
.where("topic_id = ?")
# Q 35 : # @user.posts.with_deleted.where("created_at > ? AND (spam_count > 0 OR inappropriate_count > 0)", time_period.days.ago).pluck(:id)
Query(Post)
.where("user_id = ?")
.select('id')
# Q 36 : # @user.user_stat.first_post_created_at.nil?
Query(UserStat)
.where("id = ?")
# Q 37 : # @user.user_visits.pluck(:posts_read).sum
Query(UserVisit)
.where("user_id = ?")
.select('posts_read')
# Q 38 : # @user.user_visits.where("visited_at > ? and posts_read > 0", time_period.days.ago).count
Query(UserVisit)
.where("user_id = ?")
# Q 39 : # @user.user_visits.where("visited_at > ?", time_period.days.ago).pluck(:posts_read).sum
Query(UserVisit)
.where("user_id = ?")
.select('posts_read')
# Q 40 : # @web_hook.web_hook_events.where(id: params[:ids])
Query(WebHookEvent)
.where("web_hook_id = ?")
.where("id = ?")
# Q 41 : # ApiKey.find_by(id: params[:id])
Query(ApiKey)
.where("id = ?")
# Q 42 : # ApiKey.find_by(id: params[:id])
Query(ApiKey)
.where("id = ?")
# Q 43 : # ApiKey.find_by(user_id: nil, hidden: false)
Query(ApiKey)
.where("user_id = ?")
.where("hidden = ?")
# Q 44 : # ApiKey.where(key: api_key_value).includes(:user).first
Query(ApiKey)
.where("key = ?")
.includes('user')
.return_limit('1')
# Q 45 : # ApiKey.where(user_id: self.id).delete_all
Query(ApiKey)
.where("user_id = ?")
# Q 46 : # ApplicationRequest.where(req_type: ApplicationRequest.req_types[filter])
Query(ApplicationRequest)
.where("req_type = ?")
# Q 47 : # ApplicationRequest.where(req_type: [ApplicationRequest.req_types.reject { |k, v|
#   
#   k =~ /mobile/
# }.map { |k, v|
#   
#   v if k =~ /page_view/
# }.compact].flatten)
Query(ApplicationRequest)
.where("req_type = ?")
# Q 48 : # Badge.enabled.find(params[:id])
Query(Badge)
.where("id = ?")
# Q 49 : # Badge.find(params[:id])
Query(Badge)
.where("id = ?")
# Q 50 : # Badge.find_by(id: params[:badge_id], enabled: true)
Query(Badge)
.where("id = ?")
.where("enabled = ?")
# Q 51 : # Badge.find_by(name: params[:badge_name], enabled: true)
Query(Badge)
.where("name = ?")
.where("enabled = ?")
# Q 52 : # Badge.includes(:badge_grouping).includes(:badge_type).references(:badge_grouping).order("badge_groupings.position, badge_type_id, badges.name").to_a
Query(Badge)
.includes('badge_grouping')
.includes('badge_type')
.order('id, position, badge_type_id, name')
# Q 53 : # Badge.pluck(:icon).uniq
Query(Badge)
.select('icon')
.distinct('')
# Q 54 : # BadgeGrouping.all.order(:position).to_a
Query(BadgeGrouping)
.order('position')
# Q 55 : # BadgeGrouping.all.order(:position).to_a
Query(BadgeGrouping)
.order('position')
# Q 56 : # BadgeGrouping.all.order(:position).to_a
Query(BadgeGrouping)
.order('position')
# Q 57 : # BadgeType.all.order(:id).to_a
Query(BadgeType)
.order('id')
# Q 58 : # Category.find(SiteSetting.shared_drafts_category)
Query(Category)
.where("id = ?")
# Q 59 : # Category.find(cat.to_i)
Query(Category)
.where("id = ?")
# Q 60 : # Category.find(category || SiteSetting.uncategorized_category_id)
Query(Category)
.where("id = ?")
# Q 61 : # Category.find(manager.args[:category]).require_topic_approval?
Query(Category)
.where("id = ?")
# Q 62 : # Category.find(params["category_id"])
Query(Category)
.where("id = ?")
# Q 63 : # Category.find(params[:category_id].to_i)
Query(Category)
.where("id = ?")
# Q 64 : # Category.find(params[:destination_category_id])
Query(Category)
.where("id = ?")
# Q 65 : # Category.find_by(id: @opts[:category])
Query(Category)
.where("id = ?")
# Q 66 : # Category.find_by(id: @opts[:category_id])
Query(Category)
.where("id = ?")
# Q 67 : # Category.find_by(id: SiteSetting.lounge_category_id)
Query(Category)
.where("id = ?")
# Q 68 : # Category.find_by(id: SiteSetting.staff_category_id)
Query(Category)
.where("id = ?")
# Q 69 : # Category.find_by(id: category.parent_category_id)
Query(Category)
.where("id = ?")
# Q 70 : # Category.find_by(id: category_id)
Query(Category)
.where("id = ?")
# Q 71 : # Category.find_by(id: category_id)
Query(Category)
.where("id = ?")
# Q 72 : # Category.find_by(id: category_id)
Query(Category)
.where("id = ?")
# Q 73 : # Category.find_by(id: category_id).auto_bump_topic!
Query(Category)
.where("id = ?")
# Q 74 : # Category.find_by(id: changes[:category_id])
Query(Category)
.where("id = ?")
# Q 75 : # Category.find_by(id: manager.args[:category])
Query(Category)
.where("id = ?")
# Q 76 : # Category.find_by(id: new_category_id)
Query(Category)
.where("id = ?")
# Q 77 : # Category.find_by(id: opts[:category_id])
Query(Category)
.where("id = ?")
# Q 78 : # Category.find_by(id: params[:category_id])
Query(Category)
.where("id = ?")
# Q 79 : # Category.find_by(id: params[:id].to_i)
Query(Category)
.where("id = ?")
# Q 80 : # Category.find_by(id: params[:parent_category_id].to_i)
Query(Category)
.where("id = ?")
# Q 81 : # Category.find_by(id: search_context[:id].to_i)
Query(Category)
.where("id = ?")
# Q 82 : # Category.find_by(id: topic.category.parent_category_id).name
Query(Category)
.where("id = ?")
# Q 83 : # Category.find_by(slug: params[:id])
Query(Category)
.where("slug = ?")
# Q 84 : # Category.find_by(slug: params[:parent_category_id])
Query(Category)
.where("slug = ?")
# Q 85 : # Category.find_by(topic_id: @topic.id)
Query(Category)
.where("topic_id = ?")
# Q 86 : # Category.find_by_email(address)
Query(Category)
.where("email = ?")
# Q 87 : # Category.find_by_email(email)
Query(Category)
.where("email = ?")
# Q 88 : # Category.find_by_id(id)
Query(Category)
.where("id = ?")
# Q 89 : # Category.find_by_id(params[:categoryId])
Query(Category)
.where("id = ?")
# Q 90 : # Category.find_by_slug(params[:category_slug], params[:parent_category_slug])
Query(Category)
.where("slug = ?")
# Q 91 : # Category.includes(:category_search_data).where("category_search_data.search_data @@ #{
# ts_query}").references(:category_search_data).order("topics_month DESC").secured(@guardian).limit(limit)
Query(Category)
.order('id, topics_month')
.return_limit('')
# Q 92 : # Category.includes(:uploaded_background, :uploaded_logo, :topic_only_relative_url, subcategories: [:topic_only_relative_url]).secured(@guardian)
Query(Category)
.includes('uploaded_background')
.includes('uploaded_logo')
.includes('topic_only_relative_url')
# Q 93 : # Category.includes(:uploaded_logo, :uploaded_background, :tags, :tag_groups).secured(@guardian).joins("LEFT JOIN topics t on t.id = categories.topic_id").select("categories.*, t.slug topic_slug").order(:position)
Query(Category)
.includes('uploaded_logo')
.includes('uploaded_background')
.includes('tags')
.includes('tag_groups')
.select('categories.*, t.slug topic_slug')
.order('position')
# Q 94 : # Category.joins("LEFT JOIN topics ON categories.topic_id = topics.id AND topics.deleted_at IS NULL").where("categories.id <> ?", SiteSetting.uncategorized_category_id).where(topics: { id: nil }).find_each
Query(Category)
.where("id = ?")
# Q 95 : # Category.joins(:topics).find_by(topics: { id: manager.args[:topic_id] })
Query(Category)
.joins('topics')
.where("id = ?")
# Q 96 : # Category.joins(tag_groups: :tags).where(id: guardian.allowed_category_ids, :"tags.id" => tag.id).pluck(:name)
Query(Category)
.joins('tags')
.select('name')
# Q 97 : # Category.pluck(:email_in).select(&:present?).map { |e|
#   
#   e.split("|")
# }.flatten.uniq
Query(Category)
.select('email_in')
.select('present?')
.distinct('')
# Q 98 : # Category.pluck(:topic_id).compact
Query(Category)
.select('topic_id')
.where("id != 0")
# Q 99 : # Category.pluck(:topic_id).compact!
Query(Category)
.select('topic_id')
# Q 100 : # Category.pluck(:topic_id).compact!
Query(Category)
.select('topic_id')
# Q 101 : # Category.post_create_allowed(self).where(id: parent.category.id).count
Query(Category)
.where("id = ?")
# Q 102 : # Category.query_from_hashtag_slug(category_slug).try(:id)
Query(Category)
.select('id')
# Q 103 : # Category.secured(guardian).where(id: ids).map do |category|
#   
#   { slug: category.hashtag_slug, url: category.url_with_id }
#   
# end.compact
Query(Category)
.where("id = ?")
.where("id != 0")
# Q 104 : # Category.select(:id, :topic_id, :num_featured_topics).where("id >= ?", next_category_id).order("id ASC").limit(batch_size).to_a
Query(Category)
.select('id')
.select('topic_id')
.select('num_featured_topics')
.order('id')
.return_limit('')
# Q 105 : # Category.topic_create_allowed(@guardian).pluck(:id)
Query(Category)
.select('id')
# Q 106 : # Category.topic_create_allowed(guardian).where(id: @category.id).exists?
Query(Category)
.where("id = ?")
.return_limit('1')
# Q 107 : # Category.topic_create_allowed(guardian).where(id: @category.id).exists?
Query(Category)
.where("id = ?")
.return_limit('1')
# Q 108 : # Category.topic_create_allowed(self).where(id: category_id).count
Query(Category)
.where("id = ?")
# Q 109 : # Category.topic_create_allowed(self.id).select(:id)
Query(Category)
.select('id')
# Q 110 : # Category.unscoped.where(read_restricted: true)
Query(Category)
.where("read_restricted = ?")
# Q 111 : # Category.where("id > ?", categories.last.id).order("id asc").limit(1).pluck(:id)
Query(Category)
.order('id')
.return_limit('')
.select('id')
# Q 112 : # Category.where("id IN (SELECT category_id FROM category_tags)").where("id IN (?)", guardian.allowed_category_ids).includes(:tags)
Query(Category)
.includes('tags')
# Q 113 : # Category.where("uploaded_background_id IS NOT NULL").each
Query(Category)

# Q 114 : # Category.where("uploaded_background_id IS NOT NULL").pluck(:updated_at).map(&:to_i).sum
Query(Category)
.select('updated_at')
# Q 115 : # Category.where(id: @topic.category_id).update_all(latest_post_id: @post.id)
Query(Category)
.where("id = ?")
# Q 116 : # Category.where(id: @topic.category_id).update_all(latest_topic_id: @topic.id)
Query(Category)
.where("id = ?")
# Q 117 : # Category.where(id: category_id || SiteSetting.uncategorized_category_id, topic_featured_link_allowed: true).exists?
Query(Category)
.where("id = ?")
.where("topic_featured_link_allowed = ?")
.return_limit('1')
# Q 118 : # Category.where(id: category_id).pluck(:sort_order, :sort_ascending).first
Query(Category)
.where("id = ?")
.select('sort_order')
.select('sort_ascending')
.return_limit('1')
# Q 119 : # Category.where(id: category_ids).count
Query(Category)
.where("id = ?")
# Q 120 : # Category.where(id: category_ids).or(Category.where(parent_category_id: category_ids)).to_a
Query(Category)
.where("id = ?")
# Q 121 : # Category.where(id: category_ids).pluck(:id)
Query(Category)
.where("id = ?")
.select('id')
# Q 122 : # Category.where(id: new_category.id).update_all("topic_count = topic_count + 1")
Query(Category)
.where("id = ?")
# Q 123 : # Category.where(id: old_category.id).update_all("topic_count = topic_count - 1")
Query(Category)
.where("id = ?")
# Q 124 : # Category.where(id: params[:category_id].to_i).first
Query(Category)
.where("id = ?")
.return_limit('1')
# Q 125 : # Category.where(id: parent_category_id).pluck(:parent_category_id).first
Query(Category)
.where("id = ?")
.select('parent_category_id')
.return_limit('1')
# Q 126 : # Category.where(id: post_count_query.limit(MAX_SUMMARY_RESULTS).pluck("category_id")).pluck(:id, :name, :color, :text_color, :slug, :read_restricted, :parent_category_id).each
Query(Category)
.where("id = ?")
.select('id')
.select('name')
.select('color')
.select('text_color')
.select('slug')
.select('read_restricted')
.select('parent_category_id')
# Q 127 : # Category.where(id: topic.category_id).pluck(:id)
Query(Category)
.where("id = ?")
.select('id')
# Q 128 : # Category.where(parent_category_id: @search_context.id).pluck(:id).push(@search_context.id)
Query(Category)
.where("parent_category_id = ?")
.select('id')
# Q 129 : # Category.where(parent_category_id: id).exists?
Query(Category)
.where("parent_category_id = ?")
.return_limit('1')
# Q 130 : # Category.where(read_restricted: false).pluck(:id)
Query(Category)
.where("read_restricted = ?")
.select('id')
# Q 131 : # Category.where(read_restricted: false).where.not(id: SiteSetting.uncategorized_category_id).order("id asc").pluck(:id).first
Query(Category)
.where("read_restricted = ?")
.where("id = ?")
.order('id')
.select('id')
.return_limit('1')
# Q 132 : # Category.where(review_group_id: self.id).update_all(review_group_id: nil)
Query(Category)
.where("review_group_id = ?")
# Q 133 : # Category.where(review_group_id: self.id).update_all(review_group_id: nil)
Query(Category)
.where("review_group_id = ?")
# Q 134 : # Category.where(slug: category_id_or_slug).pluck(:id).first
Query(Category)
.where("slug = ?")
.select('id')
.return_limit('1')
# Q 135 : # Category.where(slug: child_slug, parent_category_id: category.pluck(:id).first).first
Query(Category)
.where("slug = ?")
.where("parent_category_id = ?")
.return_limit('1')
# Q 136 : # Category.where(slug: parent_slug, parent_category_id: nil)
Query(Category)
.where("slug = ?")
.where("parent_category_id = ?")
# Q 137 : # Category.where(slug: self.slug, parent_category_id: parent_category_id).where.not(id: id).any?
Query(Category)
.where("slug = ?")
.where("parent_category_id = ?")
.where("id = ?")
# Q 138 : # Category.where(suppress_from_latest: true)
Query(Category)
.where("suppress_from_latest = ?")
# Q 139 : # Category.where(suppress_from_latest: true).pluck(:id)
Query(Category)
.where("suppress_from_latest = ?")
.select('id')
# Q 140 : # Category.where(suppress_from_latest: true).pluck(:id)
Query(Category)
.where("suppress_from_latest = ?")
.select('id')
# Q 141 : # Category.where(suppress_from_latest: true).pluck(:id)
Query(Category)
.where("suppress_from_latest = ?")
.select('id')
# Q 142 : # Category.where(topic_featured_link_allowed: true).pluck(:id)
Query(Category)
.where("topic_featured_link_allowed = ?")
.select('id')
# Q 143 : # Category.where.not(id: self.id).find_by_email(email)
Query(Category)
.where("id = ?")
.where("email = ?")
# Q 144 : # Category.where.not(reviewable_by_group_id: nil).pluck(:id)
Query(Category)
.where("reviewable_by_group_id = ?")
.select('id')
# Q 145 : # CategoryCustomField.where("name = 'import_id' AND value IN (?)", import_ids).select(:category_id, :value).to_a
Query(CategoryCustomField)
.select('category_id')
.select('value')
# Q 146 : # CategoryCustomField.where(name: "import_id", value: "#{
# external_category_id}#{
# import_source}").first.category_id
Query(CategoryCustomField)
.where("name = ?")
.where("value = ?")
.return_limit('1')
# Q 147 : # CategoryCustomField.where(name: Category::NUM_AUTO_BUMP_DAILY).where("NULLIF(value, \'\')::int > 0").pluck(:category_id)
Query(CategoryCustomField)
.where("name = ?")
.select('category_id')
# Q 148 : # CategoryFeaturedTopic.select(:topic_id, :category_id).order(:rank).each
Query(CategoryFeaturedTopic)
.select('topic_id')
.select('category_id')
.order('rank')
# Q 149 : # CategoryFeaturedTopic.select([:category_id, :topic_id]).order(:rank)
Query(CategoryFeaturedTopic)
.select('category_id
topic_id
')
.order('rank')
# Q 150 : # CategoryFeaturedTopic.where(category_id: c.id).delete_all
Query(CategoryFeaturedTopic)
.where("category_id = ?")
# Q 151 : # CategoryGroup.joins(:category).where(["categories.parent_category_id = ?", self.id]).pluck(:group_id, :permission_type).uniq
Query(CategoryGroup)
.joins('category')
.select('group_id')
.select('permission_type')
.distinct('')
# Q 152 : # CategoryTagGroup.pluck(:tag_group_id).uniq
Query(CategoryTagGroup)
.select('tag_group_id')
.distinct('')
# Q 153 : # CategoryUser.where(user: @guardian.user).pluck(:category_id, :notification_level).flatten
Query(CategoryUser)
.where("user = ?")
.select('category_id')
.select('notification_level')
# Q 154 : # CategoryUser.where(user: @guardian.user).pluck(:category_id, :notification_level).flatten
Query(CategoryUser)
.where("user = ?")
.select('category_id')
.select('notification_level')
# Q 155 : # CategoryUser.where(user: user, category_id: category_id).first
Query(CategoryUser)
.where("user = ?")
.where("category_id = ?")
.return_limit('1')
# Q 156 : # CategoryUser.where(user_id: user.id, category_id: category_ids).where.not(notification_level: level_num).update_all(notification_level: level_num)
Query(CategoryUser)
.where("user_id = ?")
.where("category_id = ?")
.where("notification_level = ?")
# Q 157 : # CategoryUser.where(user_id: user.id, category_id: topic.category_id, notification_level: CategoryUser.watching_levels).destroy_all
Query(CategoryUser)
.where("user_id = ?")
.where("category_id = ?")
.where("notification_level = ?")
# Q 158 : # CategoryUser.where(user_id: user.id, notification_level: CategoryUser.notification_levels[:muted]).pluck(:category_id)
Query(CategoryUser)
.where("user_id = ?")
.where("notification_level = ?")
.select('category_id')
# Q 159 : # CategoryUser.where(user_id: user.id, notification_level: level_num).where.not(category_id: category_ids).delete_all
Query(CategoryUser)
.where("user_id = ?")
.where("notification_level = ?")
.where("category_id = ?")
# Q 160 : # CategoryUser.where(user_id: user_id).where("category_id IN (SELECT category_id FROM topics WHERE id = :id)", id: topic_id).where("notification_level IN (:levels)", levels: [CategoryUser.notification_levels[:watching], CategoryUser.notification_levels[:tracking]]).order("notification_level DESC").limit(1).pluck(:notification_level).first
Query(CategoryUser)
.where("user_id = ?")
.order('id, notification_level')
.return_limit('')
.select('notification_level')
.return_limit('1')
# Q 161 : # ChildTheme.where("child_theme_id = ?", id).destroy_all
Query(ChildTheme)

# Q 162 : # ChildTheme.where("parent_theme_id = ?", id).destroy_all
Query(ChildTheme)

# Q 163 : # ChildTheme.where(parent_theme_id: theme_id).distinct.pluck(:child_theme_id)
Query(ChildTheme)
.where("parent_theme_id = ?")
.distinct('')
.select('child_theme_id')
# Q 164 : # ColorScheme.all.includes(:theme, color_scheme_colors: :color_scheme).to_a
Query(ColorScheme)
.includes('theme')
# Q 165 : # ColorScheme.find(params[:id])
Query(ColorScheme)
.where("id = ?")
# Q 166 : # ColorScheme.find_by(base_scheme_id: scheme_name, via_wizard: true)
Query(ColorScheme)
.where("base_scheme_id = ?")
.where("via_wizard = ?")
# Q 167 : # ColorScheme.where(id: missing_scheme_names.values).delete_all
Query(ColorScheme)
.where("id = ?")
# Q 168 : # ColorScheme.where(name: c[:name]).exists?
Query(ColorScheme)
.where("name = ?")
.return_limit('1')
# Q 169 : # CustomEmoji.find_by(name: name).destroy!
Query(CustomEmoji)
.where("name = ?")
# Q 170 : # CustomEmoji.includes(:upload).order(:name).each
Query(CustomEmoji)
.includes('upload')
.order('name')
# Q 171 : # DirectoryItem.where(period_type: period_type).includes(:user)
Query(DirectoryItem)
.where("period_type = ?")
.includes('user')
# Q 172 : # DirectoryItem.where(period_type: period_type, user_id: user.id).first
Query(DirectoryItem)
.where("period_type = ?")
.where("user_id = ?")
.return_limit('1')
# Q 173 : # Draft.where("updated_at < ?", delete_drafts_older_than_n_days).destroy_all
Query(Draft)

# Q 174 : # Draft.where(sequence: sequence, user_id: @user.id, draft_key: draft_key).pluck(:revisions).first
Query(Draft)
.where("sequence = ?")
.where("user_id = ?")
.where("draft_key = ?")
.select('revisions')
.return_limit('1')
# Q 175 : # EmailLog.find_by(bounce_key: bounce_key)
Query(EmailLog)
.where("bounce_key = ?")
# Q 176 : # EmailLog.find_by(id: params[:id].to_i, bounced: true)
Query(EmailLog)
.where("id = ?")
.where("bounced = ?")
# Q 177 : # EmailLog.find_by(message_id: message_id, to_address: to_address)
Query(EmailLog)
.where("message_id = ?")
.where("to_address = ?")
# Q 178 : # EmailLog.joins("LEFT JOIN post_reply_keys
#       ON post_reply_keys.post_id = email_logs.post_id
# AND post_reply_keys.user_id = email_logs.user_id
# ")
Query(EmailLog)

# Q 179 : # EmailLog.where(message_id: message_ids).pluck(:post_id)
Query(EmailLog)
.where("message_id = ?")
.select('post_id')
# Q 180 : # EmailLog.where(post_id: old_post.id).update_all(post_id: new_post.id)
Query(EmailLog)
.where("post_id = ?")
# Q 181 : # EmailToken.where(confirmed: false, id: email_token.id, expired: false)
Query(EmailToken)
.where("confirmed = ?")
.where("id = ?")
.where("expired = ?")
# Q 182 : # EmailToken.where(email: user.email, confirmed: true).exists?
Query(EmailToken)
.where("email = ?")
.where("confirmed = ?")
.return_limit('1')
# Q 183 : # EmailToken.where(token: token).where(expired: false, confirmed: false).where("created_at >= ?", EmailToken.valid_after).includes(:user).first
Query(EmailToken)
.where("token = ?")
.where("expired = ?")
.where("confirmed = ?")
.includes('user')
.return_limit('1')
# Q 184 : # EmbeddableHost.all.order(:host)
Query(EmbeddableHost)
.order('host')
# Q 185 : # EmbeddableHost.where(id: params[:id]).first
Query(EmbeddableHost)
.where("id = ?")
.return_limit('1')
# Q 186 : # EmbeddableHost.where(id: params[:id]).first
Query(EmbeddableHost)
.where("id = ?")
.return_limit('1')
# Q 187 : # Embedding.find
Query(Embedding)
.where("id = ?")
# Q 188 : # GithubUserInfo.find_by(github_user_id: github_user_id)
Query(GithubUserInfo)
.where("github_user_id = ?")
# Q 189 : # GithubUserInfo.find_by(user_id: user.id)
Query(GithubUserInfo)
.where("user_id = ?")
# Q 190 : # GithubUserInfo.find_by(user_id: user.id)
Query(GithubUserInfo)
.where("user_id = ?")
# Q 191 : # GithubUserInfo.where("user_id = ? OR github_user_id = ?", user.id, github_user_id).destroy_all
Query(GithubUserInfo)

# Q 192 : # Group.find(params[:group_id].to_i)
Query(Group)
.where("id = ?")
# Q 193 : # Group.find(params[:group_id].to_i)
Query(Group)
.where("id = ?")
# Q 194 : # Group.find(params[:id])
Query(Group)
.where("id = ?")
# Q 195 : # Group.find(params[:id])
Query(Group)
.where("id = ?")
# Q 196 : # Group.find(primary_group_id)
Query(Group)
.where("id = ?")
# Q 197 : # Group.find_by(id: group_id)
Query(Group)
.where("id = ?")
# Q 198 : # Group.find_by(id: group_id).try(:name)
Query(Group)
.where("id = ?")
.select('name')
# Q 199 : # Group.find_by(id: id)
Query(Group)
.where("id = ?")
# Q 200 : # Group.find_by(id: params.require(:id))
Query(Group)
.where("id = ?")
# Q 201 : # Group.find_by(id: params.require(:id))
Query(Group)
.where("id = ?")
# Q 202 : # Group.find_by(id: params[:group_id].to_i)
Query(Group)
.where("id = ?")
# Q 203 : # Group.find_by(id: params[:id])
Query(Group)
.where("id = ?")
# Q 204 : # Group.find_by(id: params[:id])
Query(Group)
.where("id = ?")
# Q 205 : # Group.find_by(id: params[:id])
Query(Group)
.where("id = ?")
# Q 206 : # Group.find_by(name: name)
Query(Group)
.where("name = ?")
# Q 207 : # Group.find_by(name: name)
Query(Group)
.where("name = ?")
# Q 208 : # Group.find_by(name: params[:group])
Query(Group)
.where("name = ?")
# Q 209 : # Group.find_by(name: params[:group_name])
Query(Group)
.where("name = ?")
# Q 210 : # Group.find_by(name: params[:reviewable_by_group_name]).id
Query(Group)
.where("name = ?")
# Q 211 : # Group.find_by_email(address)
Query(Group)
.where("email = ?")
# Q 212 : # Group.find_by_email(email)
Query(Group)
.where("email = ?")
# Q 213 : # Group.find_by_name(g[:name])
Query(Group)
.where("name = ?")
# Q 214 : # Group.find_by_name(group_name)
Query(Group)
.where("name = ?")
# Q 215 : # Group.joins("LEFT OUTER JOIN group_users gu ON gu.group_id = groups.id AND gu.user_id = #{
# user_id}").joins("LEFT OUTER JOIN topic_allowed_groups tag ON tag.topic_id = #{
# topic_id}").where("gu.id IS NOT NULL AND tag.id IS NOT NULL").pluck(:default_notification_level).first
Query(Group)
.select('default_notification_level')
.return_limit('1')
# Q 216 : # Group.joins(:category_groups).where("category_groups.category_id = ?", self.category_id).where("groups.public_admission OR groups.allow_membership_requests").order(:allow_membership_requests).first
Query(Group)
.joins('category_groups')
.order('allow_membership_requests')
.return_limit('1')
# Q 217 : # Group.mentionable(user).where(id: group.id).present?
Query(Group)
.where("id = ?")
# Q 218 : # Group.mentionable(user).where(name: usernames).pluck(:name, :user_count).map
Query(Group)
.where("name = ?")
.select('name')
.select('user_count')
# Q 219 : # Group.messageable(@user).where(id: target.id).exists?
Query(Group)
.where("id = ?")
.return_limit('1')
# Q 220 : # Group.messageable(user).where("name in (?)", usernames).pluck("name")
Query(Group)
.select('name')
# Q 221 : # Group.messageable(user).where(id: group.id).present?
Query(Group)
.where("id = ?")
# Q 222 : # Group.pluck(:incoming_email).select(&:present?).map { |e|
#   
#   e.split("|")
# }.flatten.uniq
Query(Group)
.select('incoming_email')
.select('present?')
.distinct('')
# Q 223 : # Group.refresh_automatic_groups!(:admins, :moderators, :staff)
Query(Group)

# Q 224 : # Group.refresh_automatic_groups!(:admins, :moderators, :staff)
Query(Group)

# Q 225 : # Group.refresh_automatic_groups!(:admins, :moderators, :staff)
Query(Group)

# Q 226 : # Group.reset_counters(group.id, :group_users)
Query(Group)

# Q 227 : # Group.reset_counters(group.id, :group_users)
Query(Group)

# Q 228 : # Group.visible_groups(user)
Query(Group)

# Q 229 : # Group.visible_groups(user).where("groups.id <> ?", Group::AUTO_GROUPS[:everyone]).order(:name)
Query(Group)
.order('name')
# Q 230 : # Group.visible_groups(user, order ? "#{
# order} #{
# dir}" : nil)
Query(Group)

# Q 231 : # Group.where("LOWER(NAME) in (?) AND NOT automatic", names).pluck(:id)
Query(Group)
.select('id')
# Q 232 : # Group.where("LOWER(name) in (?) AND NOT automatic", split).pluck(:id).each
Query(Group)
.select('id')
# Q 233 : # Group.where("flair_url LIKE '%fa-%'").pluck(:flair_url).uniq
Query(Group)
.select('flair_url')
.distinct('')
# Q 234 : # Group.where("name ilike ?", @options[:group]).first
Query(Group)
.return_limit('1')
# Q 235 : # Group.where("name ilike ?", @options[:group_name]).pluck(:id).first
Query(Group)
.select('id')
.return_limit('1')
# Q 236 : # Group.where("name ilike ?", @options[:group_name]).select(:id, :publish_read_state).first
Query(Group)
.select('id')
.select('publish_read_state')
.return_limit('1')
# Q 237 : # Group.where(automatic: false).where("LENGTH(COALESCE(automatic_membership_email_domains, '')) > 0").each
Query(Group)
.where("automatic = ?")
# Q 238 : # Group.where(id: user.primary_group_id).pluck(:name).first
Query(Group)
.where("id = ?")
.select('name')
.return_limit('1')
# Q 239 : # Group.where(id: group_ids)
Query(Group)
.where("id = ?")
# Q 240 : # Group.where(id: group_ids).select(self.class.lookup_columns).each
Query(Group)
.where("id = ?")
# Q 241 : # Group.where(id: only_subcategory_groups).pluck(:name).join(", ")
Query(Group)
.where("id = ?")
.select('name')
# Q 242 : # Group.where(id: primary_group_id_was).pluck(:title).first
Query(Group)
.where("id = ?")
.select('title')
.return_limit('1')
# Q 243 : # Group.where(id: primary_group_ids.to_a).pluck(:id, :name).each
Query(Group)
.where("id = ?")
.select('id')
.select('name')
# Q 244 : # Group.where(name: group_names)
Query(Group)
.where("name = ?")
# Q 245 : # Group.where(name: group_names).find_each
Query(Group)
.where("name = ?")
# Q 246 : # Group.where(name: names).each
Query(Group)
.where("name = ?")
# Q 247 : # Group.where(name: names.split(",")).pluck(:id)
Query(Group)
.where("name = ?")
.select('id')
# Q 248 : # Group.where(name: usernames).pluck(:name)
Query(Group)
.where("name = ?")
.select('name')
# Q 249 : # Group.where.not(id: self.id).find_by_email(email)
Query(Group)
.where("id = ?")
.where("email = ?")
# Q 250 : # GroupArchivedMessage.where(group_id: group_id, topic_id: topic_id).destroy_all
Query(GroupArchivedMessage)
.where("group_id = ?")
.where("topic_id = ?")
# Q 251 : # GroupArchivedMessage.where(group_id: group_id, topic_id: topic_id).destroy_all
Query(GroupArchivedMessage)
.where("group_id = ?")
.where("topic_id = ?")
# Q 252 : # GroupArchivedMessage.where(topic_id: @topic.id).pluck(:group_id).each
Query(GroupArchivedMessage)
.where("topic_id = ?")
.select('group_id')
# Q 253 : # GroupRequest.where(group_id: group.id, user_id: user.id).delete_all
Query(GroupRequest)
.where("group_id = ?")
.where("user_id = ?")
# Q 254 : # GroupUser.find_by(group_id: group.id, user_id: user.id)
Query(GroupUser)
.where("group_id = ?")
.where("user_id = ?")
# Q 255 : # GroupUser.find_by(group_id: group.id, user_id: user.id)
Query(GroupUser)
.where("group_id = ?")
.where("user_id = ?")
# Q 256 : # GroupUser.where("group_id IN (SELECT id FROM groups WHERE NOT automatic)").where(user_id: user.id)
Query(GroupUser)
.where("user_id = ?")
# Q 257 : # GroupUser.where(group: groups, user: user)
Query(GroupUser)
.where("group = ?")
.where("user = ?")
# Q 258 : # GroupUser.where(group: groups, user_id: user.id).pluck(:owner)
Query(GroupUser)
.where("group = ?")
.where("user_id = ?")
.select('owner')
# Q 259 : # GroupUser.where(group_id: group.id).where(user_id: user_id).update_all(notification_level: notification_level)
Query(GroupUser)
.where("group_id = ?")
.where("user_id = ?")
# Q 260 : # GroupUser.where(group_id: id, user_id: user.id).exists?
Query(GroupUser)
.where("group_id = ?")
.where("user_id = ?")
.return_limit('1')
# Q 261 : # GroupUser.where(group_id: id, user_id: user_id).exists?
Query(GroupUser)
.where("group_id = ?")
.where("user_id = ?")
.return_limit('1')
# Q 262 : # GroupUser.where(group_id: topic.category.reviewable_by_group_id, user_id: user.id).exists?
Query(GroupUser)
.where("group_id = ?")
.where("user_id = ?")
.return_limit('1')
# Q 263 : # GroupUser.where(group_id: undesired, user_id: user_id).delete_all
Query(GroupUser)
.where("group_id = ?")
.where("user_id = ?")
# Q 264 : # GroupUser.where(user: user).joins(:group).where(groups: { id: post.topic.topic_allowed_groups.map(&:group_id), publish_read_state: true }).exists?
Query(GroupUser)
.where("user = ?")
.joins('group')
.where("id = ?")
.return_limit('1')
# Q 265 : # GroupUser.where(user_id: id).includes(:group).maximum("groups.grant_trust_level")
Query(GroupUser)
.where("user_id = ?")
.includes('group')
# Q 266 : # GroupUser.where(user_id: user.id).where("group_id IN (SELECT id FROM groups WHERE LOWER(name) in (?))", split).destroy_all
Query(GroupUser)
.where("user_id = ?")
# Q 267 : # IgnoredUser.find_by(user: user, ignored_user: user)
Query(IgnoredUser)
.where("user = ?")
.where("ignored_user = ?")
# Q 268 : # IgnoredUser.where(user: user, ignored_user: user).delete_all
Query(IgnoredUser)
.where("user = ?")
.where("ignored_user = ?")
# Q 269 : # IgnoredUser.where(user: user, ignored_user: user).delete_all
Query(IgnoredUser)
.where("user = ?")
.where("ignored_user = ?")
# Q 270 : # IncomingEmail.find(params[:id].to_i)
Query(IncomingEmail)
.where("id = ?")
# Q 271 : # IncomingEmail.find(params[:id].to_i)
Query(IncomingEmail)
.where("id = ?")
# Q 272 : # IncomingEmail.find_by(to_addresses: bounced_to_address)
Query(IncomingEmail)
.where("to_addresses = ?")
# Q 273 : # IncomingEmail.find_by(to_addresses: bounced_to_address)
Query(IncomingEmail)
.where("to_addresses = ?")
# Q 274 : # IncomingEmail.joins(:post).where("posts.topic_id = ?", post_reply_key.post.topic_id).addressed_to(post_reply_key.reply_key).addressed_to_user(user).pluck(:to_addresses, :cc_addresses)
Query(IncomingEmail)
.joins('post')
.select('to_addresses')
.select('cc_addresses')
# Q 275 : # IncomingEmail.where(message_id: message_ids).addressed_to_user(user).pluck(:post_id, :to_addresses, :cc_addresses)
Query(IncomingEmail)
.where("message_id = ?")
.select('post_id')
.select('to_addresses')
.select('cc_addresses')
# Q 276 : # IncomingEmail.where(message_id: message_ids).pluck(:post_id)
Query(IncomingEmail)
.where("message_id = ?")
.select('post_id')
# Q 277 : # IncomingLink.joins(post: :topic).where("topics.archetype = ?", Archetype.default).merge(Topic.in_category_and_subcategories(category_id))
Query(IncomingLink)

# Q 278 : # Invite.find_by(email: Email.downcase(email))
Query(Invite)
.where("email = ?")
# Q 279 : # Invite.find_by(email: Email.downcase(email))
Query(Invite)
.where("email = ?")
# Q 280 : # Invite.find_by(invite_key: params[:id])
Query(Invite)
.where("invite_key = ?")
# Q 281 : # Invite.find_by(invite_key: params[:id])
Query(Invite)
.where("invite_key = ?")
# Q 282 : # Invite.find_by(invited_by_id: user.id, email: params[:email])
Query(Invite)
.where("invited_by_id = ?")
.where("email = ?")
# Q 283 : # Invite.find_by(invited_by_id: user.id, email: params[:email])
Query(Invite)
.where("invited_by_id = ?")
.where("email = ?")
# Q 284 : # Invite.where("email = ?", invite.email).update_all(["user_id = ?", invited_user.id])
Query(Invite)

# Q 285 : # Invite.where("id = ? AND redeemed_at IS NULL AND created_at >= ?", invite.id, SiteSetting.invite_expiry_days.days.ago).update_all("redeemed_at = CURRENT_TIMESTAMP")
Query(Invite)

# Q 286 : # Invite.where("invites.email = ? AND redeemed_at IS NULL AND invites.id != ?", invite.email, invite.id).delete_all
Query(Invite)

# Q 287 : # Invite.where("invites.user_id IS NULL AND invites.email IS NOT NULL AND invited_by_id = ? AND invites.created_at < ?", user.id, SiteSetting.invite_expiry_days.days.ago).find_each
Query(Invite)

# Q 288 : # Invite.where("invites.user_id IS NULL AND invites.email IS NOT NULL AND invited_by_id = ?", user_id).find_each
Query(Invite)

# Q 289 : # Invite.where(email: params[:email], invited_by_id: user.id).first
Query(Invite)
.where("email = ?")
.where("invited_by_id = ?")
.return_limit('1')
# Q 290 : # Invite.where(email: params[:email], invited_by_id: user.id).first
Query(Invite)
.where("email = ?")
.where("invited_by_id = ?")
.return_limit('1')
# Q 291 : # Invite.where(invited_by_id: inviter.id).where("invites.email IS NOT NULL").includes(user: :user_stat).order("CASE WHEN invites.user_id IS NOT NULL THEN 0 ELSE 1 END, user_stats.time_read DESC, invites.redeemed_at DESC").limit(limit).offset(offset).references("user_stats")
Query(Invite)
.where("invited_by_id = ?")
.order('id, time_read, redeemed_at, user_id')
.return_limit('')
# Q 292 : # Invite.with_deleted.where(email: lower_email, invited_by_id: invited_by.id).order("created_at DESC").first
Query(Invite)
.where("email = ?")
.where("invited_by_id = ?")
.order('id, created_at')
.return_limit('1')
# Q 293 : # JavascriptCache.where(digest: params[:digest]).limit(1)
Query(JavascriptCache)
.where("digest = ?")
.return_limit('')
# Q 294 : # JavascriptCache.where(theme_id: theme_ids)
Query(JavascriptCache)
.where("theme_id = ?")
# Q 295 : # MutedUser.where(user: user, muted_user: user).delete_all
Query(MutedUser)
.where("user = ?")
.where("muted_user = ?")
# Q 296 : # MutedUser.where(user: user, muted_user: user).delete_all
Query(MutedUser)
.where("user = ?")
.where("muted_user = ?")
# Q 297 : # MutedUser.where(user_id: target_user.id, muted_user_id: invited_by.id).joins(:muted_user).where("NOT admin AND NOT moderator").exists?
Query(MutedUser)
.where("user_id = ?")
.where("muted_user_id = ?")
.joins('muted_user')
.return_limit('1')
# Q 298 : # Notification.find(params[:id])
Query(Notification)
.where("id = ?")
# Q 299 : # Notification.where(id: notification_ids, user_id: user.id, read: false).update_all(read: true)
Query(Notification)
.where("id = ?")
.where("user_id = ?")
.where("read = ?")
# Q 300 : # Notification.where(topic_id: @post.topic_id, post_number: @post.post_number).delete_all
Query(Notification)
.where("topic_id = ?")
.where("post_number = ?")
# Q 301 : # Notification.where(user_id: user.id).includes(:topic).where(read: false).update_all(read: true)
Query(Notification)
.where("user_id = ?")
.includes('topic')
.where("read = ?")
# Q 302 : # Notification.where(user_id: user.id, topic_id: topic_id).order(created_at: :desc).limit(1).first
Query(Notification)
.where("user_id = ?")
.where("topic_id = ?")
.order('created_at')
.return_limit('')
.return_limit('1')
# Q 303 : # Notification.where(user_id: user.id).visible.includes(:topic).order(created_at: :desc)
Query(Notification)
.where("user_id = ?")
.includes('topic')
.order('created_at')
# Q 304 : # Notification.where(user_id: user.id, topic_id: topic_id, post_number: post_numbers, read: false).update_all(read: true)
Query(Notification)
.where("user_id = ?")
.where("topic_id = ?")
.where("post_number = ?")
.where("read = ?")
# Q 305 : # Notification.where(user_id: user_id, topic_id: topic_id).delete_all
Query(Notification)
.where("user_id = ?")
.where("topic_id = ?")
# Q 306 : # Notification.where(user_id: user_id, topic_id: topic_id, notification_type: Notification.types[:invited_to_private_message]).exists?
Query(Notification)
.where("user_id = ?")
.where("topic_id = ?")
.where("notification_type = ?")
.return_limit('1')
# Q 307 : # Oauth2UserInfo.find_by(uid: oauth2_uid, provider: oauth2_provider)
Query(Oauth2UserInfo)
.where("uid = ?")
.where("provider = ?")
# Q 308 : # Oauth2UserInfo.find_by(user_id: user.id, provider: @name)
Query(Oauth2UserInfo)
.where("user_id = ?")
.where("provider = ?")
# Q 309 : # Permalink.find(params[:id])
Query(Permalink)
.where("id = ?")
# Q 310 : # Permalink.find_by_url("c/#{
# self.parent_category.slug}/#{
# slug}")
Query(Permalink)
.where("url = ?")
# Q 311 : # Permalink.find_by_url("c/#{
# slug}")
Query(Permalink)
.where("url = ?")
# Q 312 : # Permalink.find_by_url(path)
Query(Permalink)
.where("url = ?")
# Q 313 : # Permalink.find_by_url(url)
Query(Permalink)
.where("url = ?")
# Q 314 : # Permalink.find_by_url(url)
Query(Permalink)
.where("url = ?")
# Q 315 : # Permalink.find_by_url(url)
Query(Permalink)
.where("url = ?")
# Q 316 : # Permalink.includes(:topic, :post, :category).order("permalinks.created_at desc")
Query(Permalink)
.includes('topic')
.includes('post')
.includes('category')
.order('id, created_at')
# Q 317 : # Permalink.where(url: Permalink.normalize_url(request.fullpath)).exists?
Query(Permalink)
.where("url = ?")
.return_limit('1')
# Q 318 : # Permalink.where(url: url).exists?
Query(Permalink)
.where("url = ?")
.return_limit('1')
# Q 319 : # Permalink.where(url: url).update_all(category_id: id)
Query(Permalink)
.where("url = ?")
# Q 320 : # PluginStoreRow.find_by(plugin_name: plugin_name, key: key)
Query(PluginStoreRow)
.where("plugin_name = ?")
.where("key = ?")
# Q 321 : # PluginStoreRow.where("plugin_name = ? AND key IN (?)", plugin_name, keys).to_a
Query(PluginStoreRow)

# Q 322 : # PluginStoreRow.where(plugin_name: plugin_name, key: key).destroy_all
Query(PluginStoreRow)
.where("plugin_name = ?")
.where("key = ?")
# Q 323 : # Post.find(id)
Query(Post)
.where("id = ?")
# Q 324 : # Post.find(params[:id].to_i)
Query(Post)
.where("id = ?")
# Q 325 : # Post.find(params[:post_id].to_i)
Query(Post)
.where("id = ?")
# Q 326 : # Post.find_by("topic_id = :topic_id AND post_number = :post_number AND user_id <> :user_id", topic_id: topic_id, post_number: reply_to_post_number, user_id: user_id).try(:user)
Query(Post)
.select('user')
# Q 327 : # Post.find_by(id: @data[:last_post_id])
Query(Post)
.where("id = ?")
# Q 328 : # Post.find_by(id: data["postId"])
Query(Post)
.where("id = ?")
# Q 329 : # Post.find_by(id: post_id)
Query(Post)
.where("id = ?")
# Q 330 : # Post.find_by(id: post_id)
Query(Post)
.where("id = ?")
# Q 331 : # Post.find_by(id: post_id, topic_id: topic_id)
Query(Post)
.where("id = ?")
.where("topic_id = ?")
# Q 332 : # Post.find_by(id: post_ids.first)
Query(Post)
.where("id = ?")
# Q 333 : # Post.find_by(topic_id: SiteSetting.tos_topic_id, post_number: 1)
Query(Post)
.where("topic_id = ?")
.where("post_number = ?")
# Q 334 : # Post.find_by(topic_id: id, post_number: 1)
Query(Post)
.where("topic_id = ?")
.where("post_number = ?")
# Q 335 : # Post.find_by(topic_id: params[:topic_id].to_i, post_number: (
# params[:post_number] || 1).to_i)
Query(Post)
.where("topic_id = ?")
.where("post_number = ?")
# Q 336 : # Post.find_by(topic_id: params[:topic_id].to_i, post_number: (
# params[:post_number] || 1).to_i)
Query(Post)
.where("topic_id = ?")
.where("post_number = ?")
# Q 337 : # Post.find_by(topic_id: post.topic_id, post_number: 2, user_id: Discourse::SYSTEM_USER_ID)
Query(Post)
.where("topic_id = ?")
.where("post_number = ?")
.where("user_id = ?")
# Q 338 : # Post.find_by(topic_id: topic.id, post_number: last_read_post_number)
Query(Post)
.where("topic_id = ?")
.where("post_number = ?")
# Q 339 : # Post.find_by(topic_id: topic_id, post_number: 1)
Query(Post)
.where("topic_id = ?")
.where("post_number = ?")
# Q 340 : # Post.find_by(topic_id: topic_id, post_number: p)
Query(Post)
.where("topic_id = ?")
.where("post_number = ?")
# Q 341 : # Post.find_by(topic_id: topic_id, post_number: post_number)
Query(Post)
.where("topic_id = ?")
.where("post_number = ?")
# Q 342 : # Post.find_by(topic_id: topic_id, post_number: post_number).try(:id)
Query(Post)
.where("topic_id = ?")
.where("post_number = ?")
.select('id')
# Q 343 : # Post.find_by(topic_id: topic_id, post_number: post_number.to_i)
Query(Post)
.where("topic_id = ?")
.where("post_number = ?")
# Q 344 : # Post.from(@posts, :posts).order(post_number: :asc)
Query(Post)
.order('post_number')
# Q 345 : # Post.have_uploads.joins(:topic).joins("LEFT JOIN post_custom_fields ON posts.id = post_custom_fields.post_id AND post_custom_fields.name = '#{
# Post::MISSING_UPLOADS_IGNORED}'").where("post_custom_fields.id IS NULL").select(:id, :cooked)
Query(Post)
.joins('topic')
.select('id')
.select('cooked')
# Q 346 : # Post.includes(:incoming_email).joins("INNER JOIN post_replies ON post_replies.post_id = posts.id ").where("post_replies.reply_id = ?", post_id).order(id: :desc)
Query(Post)
.includes('incoming_email')
.order('id')
# Q 347 : # Post.includes(:user, :topic, :deleted_by, :user_actions).where(user_id: user_id).with_deleted.order(created_at: :desc)
Query(Post)
.includes('user')
.includes('topic')
.includes('user_actions')
.where("user_id = ?")
.order('created_at')
# Q 348 : # Post.includes(:user, :topic, topic: :category).references(:posts, :topics, :category).where("topics.archetype = ?", Archetype.private_message).where(post_type: Post.types[:regular]).where("topics.id IN (SELECT topic_id FROM topic_allowed_groups WHERE group_id = ?)", self.id)
Query(Post)
.includes('user')
.includes('topic')
.where("post_type = ?")
# Q 349 : # Post.includes(topic: %i{topic_allowed_groups topic_allowed_users}).find(params[:id])
Query(Post)
.where("id = ?")
# Q 350 : # Post.joins(:group_mentions).includes(:user, :topic, topic: :category).references(:posts, :topics, :category).where("topics.archetype <> ?", Archetype.private_message).where(post_type: Post.types[:regular]).where("group_mentions.group_id = ?", self.id)
Query(Post)
.joins('group_mentions')
.includes('user')
.includes('topic')
.where("post_type = ?")
# Q 351 : # Post.joins(:topic).includes(:topic).secured(@guardian).merge(Topic.listable_topics.visible.secured(@guardian)).where(user: @user).group("topics.category_id").order("COUNT(*) DESC")
Query(Post)
.joins('topic')
.includes('topic')
.where("user = ?")
.group('category_id, id')
.order('id')
# Q 352 : # Post.joins(:topic).includes(:topic).secured(@guardian).merge(Topic.listable_topics.visible.secured(@guardian)).where(user: @user).where("post_number > 1").order("posts.like_count DESC, posts.created_at DESC").limit(MAX_SUMMARY_RESULTS)
Query(Post)
.joins('topic')
.includes('topic')
.where("user = ?")
.order('id, like_count, created_at')
.return_limit('')
# Q 353 : # Post.joins(:topic).joins("JOIN posts replies ON posts.topic_id = replies.topic_id AND posts.reply_to_post_number = replies.post_number").includes(:topic).secured(@guardian).merge(Topic.listable_topics.visible.secured(@guardian)).where(user: @user).where("replies.user_id <> ?", @user.id).group("replies.user_id").order("COUNT(*) DESC").limit(MAX_SUMMARY_RESULTS).pluck("replies.user_id, COUNT(*)").each
Query(Post)
.joins('topic')
.includes('topic')
.where("user = ?")
.group('user_id, id')
.order('id')
.return_limit('')
.select('replies.user_id, COUNT(*)')
# Q 354 : # Post.joins(:topic).where("topics.id = :id AND topics.archetype = :archetype AND posts.post_number = 1", id: id, archetype: archetype).first
Query(Post)
.joins('topic')
.return_limit('1')
# Q 355 : # Post.joins(:topic).where(["topics.category_id = ?", self.id]).where("topics.visible = true").where("posts.deleted_at IS NULL").where("posts.user_deleted = false")
Query(Post)
.joins('topic')
# Q 356 : # Post.joins(:topic, user: :groups, topic: :category).preload(:topic, user: :groups, topic: :category).references(:posts, :topics, :category).where(groups: { id: id }).where("topics.archetype <> ?", Archetype.private_message).where("topics.visible").where(post_type: Post.types[:regular])
Query(Post)
.joins('topic')
.includes('topic')
.where("id = ?")
.where("post_type = ?")
# Q 357 : # Post.order("posts.created_at desc").where("NOT hidden").joins("join topics on topics.id = topic_id").where("topics.category_id = :id", id: self.id).limit(1).pluck("posts.id").first
Query(Post)
.order('id, created_at')
.return_limit('')
.select('posts.id')
.return_limit('1')
# Q 358 : # Post.private_posts.order(created_at: :desc).where("posts.id <= ?", last_post_id).where("posts.id > ?", last_post_id - 50).includes(topic: :category).includes(user: :primary_group).includes(:reply_to_user).limit(50)
Query(Post)
.order('created_at')
.includes('reply_to_user')
.return_limit('')
# Q 359 : # Post.public_posts.order(created_at: :desc).where("posts.id <= ?", last_post_id).where("posts.id > ?", last_post_id - 50).includes(topic: :category).includes(user: :primary_group).includes(:reply_to_user).limit(50)
Query(Post)
.order('created_at')
.includes('reply_to_user')
.return_limit('')
# Q 360 : # Post.public_posts.where(user_id: user.id).where(post_type: Post.types[:regular]).order(created_at: :desc).includes(:user).includes(topic: :category).limit(50)
Query(Post)
.where("user_id = ?")
.where("post_type = ?")
.order('created_at')
.includes('user')
.return_limit('')
# Q 361 : # Post.secured(guardian).where(id: post_ids).includes(:user, :topic).order(:id).to_a
Query(Post)
.where("id = ?")
.includes('user')
.includes('topic')
.order('id')
# Q 362 : # Post.secured(guardian).where(id: replies.map { |r|
#   
#   r[:id]
# }).pluck(:id).to_set
Query(Post)
.where("id = ?")
.select('id')
# Q 363 : # Post.secured(guardian).where(topic_id: topic_id, user_id: @user.id).count
Query(Post)
.where("topic_id = ?")
.where("user_id = ?")
# Q 364 : # Post.select(:created_at, :user_id, :post_number).where("topic_id = ? and id <> ?", @post.topic_id, @post.id).order("created_at desc").limit(1).first
Query(Post)
.select('created_at')
.select('user_id')
.select('post_number')
.order('id, created_at')
.return_limit('')
.return_limit('1')
# Q 365 : # Post.unscoped.find(params[:id].to_i)
Query(Post)
.where("id = ?")
# Q 366 : # Post.unscoped.order("id desc").offset(SiteSetting.search_recent_posts_size - 1).limit(2).pluck(:id)
Query(Post)
.order('id')
.return_limit('')
.select('id')
# Q 367 : # Post.where("baked_version IS NULL OR baked_version < ?", BAKED_VERSION).order("id desc").limit(limit).pluck(:id).each
Query(Post)
.order('id')
.return_limit('')
.select('id')
# Q 368 : # Post.where("created_at > ?", 30.days.ago).count
Query(Post)

# Q 369 : # Post.where("created_at > ?", 7.days.ago).count
Query(Post)

# Q 370 : # Post.where("post_number < ? AND topic_id = ? AND post_type = ? AND NOT hidden", @post.post_number, @post.topic_id, Post.types[:regular]).order("post_number DESC").limit(1).pluck(:cooked).first
Query(Post)
.order('id, post_number')
.return_limit('')
.select('cooked')
.return_limit('1')
# Q 371 : # Post.where(deleted_at: nil, hidden: true).where("hidden_at < ?", 30.days.ago).find_each
Query(Post)
.where("deleted_at = ?")
.where("hidden = ?")
# Q 372 : # Post.where(deleted_at: nil, user_deleted: true).where("NOT EXISTS (
#             SELECT 1 FROM topics t
#             WHERE t.deleted_at IS NOT NULL AND
#                   t.id = posts.topic_id
#         )").where("updated_at < ?", SiteSetting.delete_removed_posts_after.hours.ago).where("NOT EXISTS (
#                   SELECT 1
#                   FROM post_actions pa
#                   WHERE pa.post_id = posts.id
#                     AND pa.deleted_at IS NULL
#                     AND pa.deferred_at IS NULL
#                     AND pa.post_action_type_id IN (?)
#               )", PostActionType.notify_flag_type_ids).find_each
Query(Post)
.where("deleted_at = ?")
.where("user_deleted = ?")
# Q 373 : # Post.where(hidden: false, deleted_at: nil, topic_id: @topic.id).where("posts.id in (?)", post_ids).joins("LEFT JOIN users u on u.id = posts.user_id").pluck(:id, :cooked, :username).map
Query(Post)
.where("hidden = ?")
.where("deleted_at = ?")
.where("topic_id = ?")
.select('id')
.select('cooked')
.select('username')
# Q 374 : # Post.where(id: @details[:post_id]).pluck(:user_id)
Query(Post)
.where("id = ?")
.select('user_id')
# Q 375 : # Post.where(id: params[:id] || params[:post_id])
Query(Post)
.where("id = ?")
# Q 376 : # Post.where(id: params[:id])
Query(Post)
.where("id = ?")
# Q 377 : # Post.where(id: params[:id].to_i)
Query(Post)
.where("id = ?")
# Q 378 : # Post.where(id: params[:post_id]).first
Query(Post)
.where("id = ?")
.return_limit('1')
# Q 379 : # Post.where(id: params[:post_ids]).order(:id)
Query(Post)
.where("id = ?")
.order('id')
# Q 380 : # Post.where(id: post.id)
Query(Post)
.where("id = ?")
# Q 381 : # Post.where(id: post_id)
Query(Post)
.where("id = ?")
# Q 382 : # Post.where(id: post_id)
Query(Post)
.where("id = ?")
# Q 383 : # Post.where(id: post_id)
Query(Post)
.where("id = ?")
# Q 384 : # Post.where(id: post_ids).each
Query(Post)
.where("id = ?")
# Q 385 : # Post.where(id: post_ids).order(:created_at).last
Query(Post)
.where("id = ?")
.order('created_at')
.return_limit('1')
# Q 386 : # Post.where(id: post_ids).order(:created_at).last
Query(Post)
.where("id = ?")
.order('created_at')
.return_limit('1')
# Q 387 : # Post.where(id: post_ids, topic_id: @topic.id).includes({ user: :primary_group }, :reply_to_user, :deleted_by, :incoming_email, :topic).order("sort_order")
Query(Post)
.where("id = ?")
.where("topic_id = ?")
.includes('reply_to_user')
.includes('incoming_email')
.includes('topic')
.order('id, sort_order')
# Q 388 : # Post.where(id: post_ids_including_replies)
Query(Post)
.where("id = ?")
# Q 389 : # Post.where(id: reply_ids.map { |r|
#   
#   r[:id]
# })
Query(Post)
.where("id = ?")
# Q 390 : # Post.where(post_type: Post.types[:regular], topic_id: @post.topic_id).where("post_number > 1").group(:user_id).count
Query(Post)
.where("post_type = ?")
.where("topic_id = ?")
.group('user_id')
# Q 391 : # Post.where(post_type: Post.types[:regular], topic_id: @post.topic_id).where("post_number > 1").group(:user_id).count
Query(Post)
.where("post_type = ?")
.where("topic_id = ?")
.group('user_id')
# Q 392 : # Post.where(post_type: Topic.visible_post_types(@guardian.user)).joins(:post_search_data, :topic).joins("LEFT JOIN categories ON categories.id = topics.category_id")
Query(Post)
.where("post_type = ?")
.joins('topic')
# Q 393 : # Post.where(topic: @original_topic, id: post_ids).where.not(post_type: Post.types[:small_action]).where.not(raw: "").order(:created_at).tap
Query(Post)
.where("topic = ?")
.where("id = ?")
.where("post_type = ?")
.where("raw = ?")
.order('created_at')
# Q 394 : # Post.where(topic: topic, id: post_ids).order(:post_number).pluck(:post_type).first
Query(Post)
.where("topic = ?")
.where("id = ?")
.order('post_number')
.select('post_type')
.return_limit('1')
# Q 395 : # Post.where(topic: topic_id, post_number: post_number).pluck(:id).first
Query(Post)
.where("topic = ?")
.where("post_number = ?")
.select('id')
.return_limit('1')
# Q 396 : # Post.where(topic_id: @details[:topic_id]).where("created_at > ?", 1.day.ago).where(post_type: Post.types[:regular]).order("created_at desc").limit(SiteSetting.sequential_replies_threshold).pluck(:user_id)
Query(Post)
.where("topic_id = ?")
.where("post_type = ?")
.order('id, created_at')
.return_limit('')
.select('user_id')
# Q 397 : # Post.where(topic_id: @topic.id).where("post_number > ?", @post.post_number).exists?
Query(Post)
.where("topic_id = ?")
.return_limit('1')
# Q 398 : # Post.where(topic_id: @topic.id, post_number: post_number).with_deleted
Query(Post)
.where("topic_id = ?")
.where("post_number = ?")
# Q 399 : # Post.where(topic_id: @topic_id, post_number: @post_number).first
Query(Post)
.where("topic_id = ?")
.where("post_number = ?")
.return_limit('1')
# Q 400 : # Post.where(topic_id: message_ids.map { |message_id|
#   
#   message_id[topic_id_regexp, 1]
# }.compact, post_number: 1).pluck(:id)
Query(Post)
.where("topic_id = ?")
.where("post_number = ?")
.select('id')
# Q 401 : # Post.where(topic_id: opts[:topic_id], post_number: opts[:post_number] || 1).pluck(:id).first
Query(Post)
.where("topic_id = ?")
.where("post_number = ?")
.select('id')
.return_limit('1')
# Q 402 : # Post.where(topic_id: params[:topic_id], post_number: params[:post_number])
Query(Post)
.where("topic_id = ?")
.where("post_number = ?")
# Q 403 : # Post.where(topic_id: post.topic_id, post_number: post.reply_to_post_number).pluck(:id).first
Query(Post)
.where("topic_id = ?")
.where("post_number = ?")
.select('id')
.return_limit('1')
# Q 404 : # Post.where(topic_id: post.topic_id, post_number: post.reply_to_post_number).select(:user_id, :post_type).lock.first
Query(Post)
.where("topic_id = ?")
.where("post_number = ?")
.select('user_id')
.select('post_type')
.return_limit('1')
# Q 405 : # Post.where(topic_id: topic.id, post_number: 1).where.not(locked_by_id: nil).exists?
Query(Post)
.where("topic_id = ?")
.where("post_number = ?")
.where("locked_by_id = ?")
.return_limit('1')
# Q 406 : # Post.where(topic_id: topic_id).sum(column)
Query(Post)
.where("topic_id = ?")
# Q 407 : # Post.where(topic_id: user_export.topic_id).find_each
Query(Post)
.where("topic_id = ?")
# Q 408 : # Post.where(user_id: @user.id).order(created_at: :desc).limit(1).pluck(:created_at).first
Query(Post)
.where("user_id = ?")
.order('created_at')
.return_limit('')
.select('created_at')
.return_limit('1')
# Q 409 : # Post.where(user_id: id, topic_id: topic_id)
Query(Post)
.where("user_id = ?")
.where("topic_id = ?")
# Q 410 : # Post.with_deleted.find(post_reply_key.post_id)
Query(Post)
.where("id = ?")
# Q 411 : # Post.with_deleted.find_by(id: post_action.post_id)
Query(Post)
.where("id = ?")
# Q 412 : # Post.with_deleted.find_by(id: target_id)
Query(Post)
.where("id = ?")
# Q 413 : # Post.with_deleted.where("raw ~ '/uploads/#{
# db}/\\d+/' OR raw ~ '/uploads/#{
# db}/original/(\\d|[a-z])/'").select(:id, :raw, :cooked).all
Query(Post)
.select('id')
.select('raw')
.select('cooked')
# Q 414 : # Post.with_deleted.where(id: @post.id).update_all(Hash[*f.flatten])
Query(Post)
.where("id = ?")
# Q 415 : # Post.with_deleted.where(id: post.id).update_all(raw: post.raw, cooked: post.cooked)
Query(Post)
.where("id = ?")
# Q 416 : # Post.with_deleted.where(id: post_id).pluck(:topic_id).first
Query(Post)
.where("id = ?")
.select('topic_id')
.return_limit('1')
# Q 417 : # Post.with_deleted.where(id: target_id).update_all(cached)
Query(Post)
.where("id = ?")
# Q 418 : # Post.with_deleted.where(user_id: self.id).where(id: PostAction.where(post_action_type_id: PostActionType.notify_flag_type_ids).where(disagreed_at: nil).select(:post_id)).count
Query(Post)
.where("user_id = ?")
.where("id = ?")
# Q 419 : # Post.with_deleted.where(user_id: self.id).where.not(deleted_at: nil).count
Query(Post)
.where("user_id = ?")
.where("deleted_at = ?")
# Q 420 : # PostAction.active.where(post_id: target_id).where(post_action_type_id: PostActionType.notify_flag_type_ids)
Query(PostAction)
.where("post_id = ?")
.where("post_action_type_id = ?")
# Q 421 : # PostAction.active.where(post_id: target_id).where(post_action_type_id: PostActionType.notify_flag_types.values)
Query(PostAction)
.where("post_id = ?")
.where("post_action_type_id = ?")
# Q 422 : # PostAction.active.where(post_id: target_id).where(post_action_type_id: action_type_ids)
Query(PostAction)
.where("post_id = ?")
.where("post_action_type_id = ?")
# Q 423 : # PostAction.find_by(post_action_type_id: PostActionType.types[:like], post_id: @data[:last_post_id], user_id: @user.id)
Query(PostAction)
.where("post_action_type_id = ?")
.where("post_id = ?")
.where("user_id = ?")
# Q 424 : # PostAction.find_by(post_id: params[:post_id], user_id: user.id)
Query(PostAction)
.where("post_id = ?")
.where("user_id = ?")
# Q 425 : # PostAction.find_by(user: @created_by, post: @post, post_action_type_id: @post_action_type_id)
Query(PostAction)
.where("user = ?")
.where("post = ?")
.where("post_action_type_id = ?")
# Q 426 : # PostAction.joins(:post).where(user_id: user.id).where("topic_id = ?", topic.id).each
Query(PostAction)
.joins('post')
.where("user_id = ?")
# Q 427 : # PostAction.joins(:user).where(post_id: post_id).sum("CASE WHEN users.moderator OR users.admin THEN #{
# SiteSetting.staff_like_weight} ELSE 1 END")
Query(PostAction)
.joins('user')
.where("post_id = ?")
# Q 428 : # PostAction.publics.where(post_id: @post.id)
Query(PostAction)
.where("post_id = ?")
# Q 429 : # PostAction.publics.with_deleted.where(post_id: self.id, id: self.custom_fields["deleted_public_actions"]).find_each
Query(PostAction)
.where("post_id = ?")
.where("id = ?")
# Q 430 : # PostAction.unscoped.where(post_action_type_id: post_action_type)
Query(PostAction)
.where("post_action_type_id = ?")
# Q 431 : # PostAction.where(post_action_type_id: PostActionType.types[:spam]).where.not(disagreed_at: nil).pluck(:post_id)
Query(PostAction)
.where("post_action_type_id = ?")
.where("disagreed_at = ?")
.select('post_id')
# Q 432 : # PostAction.where(post_id: @filtered_posts.pluck(:id), post_action_type_id: PostActionType.types[:like])
Query(PostAction)
.where("post_id = ?")
.where("post_action_type_id = ?")
# Q 433 : # PostAction.where(post_id: collection_ids, user_id: user_id)
Query(PostAction)
.where("post_id = ?")
.where("user_id = ?")
# Q 434 : # PostAction.where(post_id: post_id).where(post_action_type_id: post_action_type_id).count
Query(PostAction)
.where("post_id = ?")
.where("post_action_type_id = ?")
# Q 435 : # PostAction.where(user: @destroyed_by, post: @post, post_action_type_id: @post_action_type_id)
Query(PostAction)
.where("user = ?")
.where("post = ?")
.where("post_action_type_id = ?")
# Q 436 : # PostAction.where(user_id: id, post_action_type_id: PostActionType.flag_types_without_custom.values).count
Query(PostAction)
.where("user_id = ?")
.where("post_action_type_id = ?")
# Q 437 : # PostAction.where(user_id: self.id).where(disagreed_at: nil).where(post_action_type_id: PostActionType.notify_flag_type_ids).count
Query(PostAction)
.where("user_id = ?")
.where("disagreed_at = ?")
.where("post_action_type_id = ?")
# Q 438 : # PostAction.where(user_id: user_id).where(post_id: post_id).where(post_action_type_id: post_action_type_ids).where(deleted_at: nil).where(disagreed_at: nil).where(targets_topic: targets_topic).exists?
Query(PostAction)
.where("user_id = ?")
.where("post_id = ?")
.where("post_action_type_id = ?")
.where("deleted_at = ?")
.where("disagreed_at = ?")
.where("targets_topic = ?")
.return_limit('1')
# Q 439 : # PostAction.where(where_attrs).with_deleted.where("deleted_at IS NOT NULL").first
Query(PostAction)
.return_limit('1')
# Q 440 : # PostAction.with_deleted.where(post_id: flagged_post_ids).where.not(user_id: @user.id).where.not(agreed_at: nil).pluck(:post_id).uniq.count
Query(PostAction)
.where("post_id = ?")
.where("user_id = ?")
.where("agreed_at = ?")
.select('post_id')
.distinct('')
# Q 441 : # PostAction.with_deleted.where(post_id: flagged_post_ids).where.not(user_id: @user.id).where.not(agreed_at: nil).pluck(:user_id).uniq.count
Query(PostAction)
.where("post_id = ?")
.where("user_id = ?")
.where("agreed_at = ?")
.select('user_id')
.distinct('')
# Q 442 : # PostActionType.where(id: PostActionType.flag_types.values).order("id")
Query(PostActionType)
.where("id = ?")
.order('id')
# Q 443 : # PostActionType.where(id: id).update_all(reviewable_priority: priority.to_i, score_bonus: priority.to_f)
Query(PostActionType)
.where("id = ?")
# Q 444 : # PostActionType.where(id: reviewable_score_type).pluck(:score_bonus)
Query(PostActionType)
.where("id = ?")
.select('score_bonus')
# Q 445 : # PostCustomField.where(name: "import_id", value: import_id).first.post
Query(Post)
.where("name = ?")
.where("value = ?")
.return_limit('1')
.where("id = ?")
# Q 446 : # PostCustomField.where(name: "import_id", value: post_import_id).first.post
Query(Post)
.where("name = ?")
.where("value = ?")
.return_limit('1')
.where("id = ?")
# Q 447 : # PostCustomField.where(name: Post::MISSING_UPLOADS).delete_all
Query(PostCustomField)
.where("name = ?")
# Q 448 : # PostReply.where(post_id: params[:reply_post_ids]).pluck(:reply_id)
Query(PostReply)
.where("post_id = ?")
.select('reply_id')
# Q 449 : # PostReply.where(reply_id: @post.id).delete_all
Query(PostReply)
.where("reply_id = ?")
# Q 450 : # PostReply.where(reply_id: @post.id).pluck(:post_id)
Query(PostReply)
.where("reply_id = ?")
.select('post_id')
# Q 451 : # PostReplyKey.find_by(reply_key: c)
Query(PostReplyKey)
.where("reply_key = ?")
# Q 452 : # PostReplyKey.where("(post_id,user_id) IN (#{
# (
# ["(?)"] * tuples.size).join(", ")})", *tuples).pluck(:post_id, :user_id, "reply_key::text").each
Query(PostReplyKey)
.select('reply_key::text')
# Q 453 : # PostRevision.find_by(post_id: @post.id, number: @post.version)
Query(PostRevision)
.where("post_id = ?")
.where("number = ?")
# Q 454 : # PostRevision.find_by(post_id: id, number: (
# number + 1))
Query(PostRevision)
.where("post_id = ?")
.where("number = ?")
# Q 455 : # PostRevision.find_by(post_id: post.id, number: revision)
Query(PostRevision)
.where("post_id = ?")
.where("number = ?")
# Q 456 : # PostRevision.find_by(post_id: post_id, number: revision)
Query(PostRevision)
.where("post_id = ?")
.where("number = ?")
# Q 457 : # PostRevision.find_by(post_id: post_id, number: revision)
Query(PostRevision)
.where("post_id = ?")
.where("number = ?")
# Q 458 : # PostRevision.where(post_id: post_id).order(:number)
Query(PostRevision)
.where("post_id = ?")
.order('number')
# Q 459 : # PostTiming.where("topic_id = ? AND user_id = ? AND post_number > ?", topic.id, user.id, last_read).delete_all
Query(PostTiming)

# Q 460 : # PostTiming.where("user_id = ? and topic_id in (?)", user_id, topic_ids).delete_all
Query(PostTiming)

# Q 461 : # PostTiming.where(topic_id: @topic.id, user_id: @user.id).where(post_number: @posts.pluck(:post_number)).pluck(:post_number)
Query(PostTiming)
.where("topic_id = ?")
.where("user_id = ?")
.where("post_number = ?")
.select('post_number')
# Q 462 : # PostTiming.where(topic_id: topic_id, post_number: post_number, user_id: user.id).exists?
Query(PostTiming)
.where("topic_id = ?")
.where("post_number = ?")
.where("user_id = ?")
.return_limit('1')
# Q 463 : # PostUpload.where(post_id: self.id).delete_all
Query(PostUpload)
.where("post_id = ?")
# Q 464 : # QuotedPost.where(post_id: post.id, quoted_post_id: reply_post_id).count
Query(QuotedPost)
.where("post_id = ?")
.where("quoted_post_id = ?")
# Q 465 : # Report.find("web_crawlers", start_date: start_date, end_date: end_date).as_json
Query(Report)
.where("id = ?")
# Q 466 : # Report.find(report_type, args)
Query(Report)
.where("id = ?")
# Q 467 : # Report.find(report_type, args)
Query(Report)
.where("id = ?")
# Q 468 : # Report.find(type).as_json
Query(Report)
.where("id = ?")
# Q 469 : # Reviewable.find_by(id: params[:reviewable_id], created_by: user)
Query(Reviewable)
.where("id = ?")
.where("created_by = ?")
# Q 470 : # Reviewable.viewable_by(user).where(id: params[:reviewable_id]).first
Query(Reviewable)
.where("id = ?")
.return_limit('1')
# Q 471 : # Reviewable.where(category_id: id).update_all(reviewable_by_group_id: reviewable_by_group_id)
Query(Reviewable)
.where("category_id = ?")
# Q 472 : # Reviewable.where(created_by: created_by).pluck(:id)
Query(Reviewable)
.where("created_by = ?")
.select('id')
# Q 473 : # Reviewable.where(created_by_id: id).delete_all
Query(Reviewable)
.where("created_by_id = ?")
# Q 474 : # ReviewableClaimedTopic.where(topic_id: reviewable.topic_id).pluck(:user_id)
Query(ReviewableClaimedTopic)
.where("topic_id = ?")
.select('user_id')
# Q 475 : # ReviewableClaimedTopic.where(topic_id: topic.id).delete_all
Query(ReviewableClaimedTopic)
.where("topic_id = ?")
# Q 476 : # ReviewableClaimedTopic.where(topic_id: topic_ids).includes(:user).each
Query(ReviewableClaimedTopic)
.where("topic_id = ?")
.includes('user')
# Q 477 : # ReviewableFlaggedPost.find_by(target: @post).score
Query(ReviewableFlaggedPost)
.where("target = ?")
# Q 478 : # ReviewableFlaggedPost.pending.default_visible.where(topic_id: id).exists?
Query(ReviewableFlaggedPost)
.where("topic_id = ?")
.return_limit('1')
# Q 479 : # ReviewableFlaggedPost.pending.find_by(target: self)
Query(ReviewableFlaggedPost)
.where("target = ?")
# Q 480 : # ReviewableQueuedPost.pending.where(created_by: @user, topic: @topic).order(:created_at)
Query(ReviewableQueuedPost)
.where("created_by = ?")
.where("topic = ?")
.order('created_at')
# Q 481 : # ReviewableQueuedPost.viewable_by(@user).where(topic_id: @topic.id).pending.count
Query(ReviewableQueuedPost)
.where("topic_id = ?")
# Q 482 : # ReviewableQueuedPost.where(created_by: @user).pending.count
Query(ReviewableQueuedPost)
.where("created_by = ?")
# Q 483 : # ReviewableScore.includes(:reviewable).where("reviewables.type" => "ReviewableFlaggedPost")
Query(ReviewableScore)
.includes('reviewable')
.where("type" = ?")
# Q 484 : # ReviewableScore.joins(reviewable: :topic).where("reviewables.type = ?", name)
Query(ReviewableScore)

# Q 485 : # ReviewableScore.pending.joins(:reviewable).where("reviewables.topic_id = ?", self.id).pluck("COUNT(DISTINCT reviewable_scores.user_id), COALESCE(SUM(reviewable_scores.score), 0.0)").first
Query(ReviewableScore)
.joins('reviewable')
.select('COUNT(DISTINCT reviewable_scores.user_id), COALESCE(SUM(reviewable_scores.score), 0.0)')
.return_limit('1')
# Q 486 : # ReviewableUser.find_by(target: @user)
Query(ReviewableUser)
.where("target = ?")
# Q 487 : # ReviewableUser.find_by(target: invited_user)
Query(ReviewableUser)
.where("target = ?")
# Q 488 : # ReviewableUser.pending.find_by(target: self)
Query(ReviewableUser)
.where("target = ?")
# Q 489 : # ScreenedEmail.find(params[:id].to_i)
Query(ScreenedEmail)
.where("id = ?")
# Q 490 : # ScreenedEmail.limit(200).order("last_match_at desc").to_a
Query(ScreenedEmail)
.return_limit('')
.order('id, last_match_at')
# Q 491 : # ScreenedEmail.order(created_at: :desc).limit(100)
Query(ScreenedEmail)
.order('created_at')
.return_limit('')
# Q 492 : # ScreenedIpAddress.find(params[:id])
Query(ScreenedIpAddress)
.where("id = ?")
# Q 493 : # ScreenedIpAddress.where("? <<= ip_address", subnet).exists?
Query(ScreenedIpAddress)
.return_limit('1')
# Q 494 : # ScreenedIpAddress.where(action_type: ScreenedIpAddress.actions[:block]).where("family(ip_address) = 4").where("ip_address << ?", subnet).delete_all
Query(ScreenedIpAddress)
.where("action_type = ?")
# Q 495 : # ScreenedIpAddress.where(action_type: actions[:allow_admin]).count
Query(ScreenedIpAddress)
.where("action_type = ?")
# Q 496 : # ScreenedUrl.select("domain, sum(match_count) as match_count, max(last_match_at) as last_match_at, min(created_at) as created_at").group(:domain).order("last_match_at DESC").to_a
Query(ScreenedUrl)
.select('domain, sum(match_count) as match_count, max(last_match_at) as last_match_at, min(created_at) as created_at')
.group('domain')
.order('id, last_match_at')
# Q 497 : # SearchLog.order(:id).offset(SiteSetting.search_query_log_max_size).limit(1).pluck(:id)
Query(SearchLog)
.order('id')
.return_limit('')
.select('id')
# Q 498 : # SearchLog.select("COUNT(*) AS count, created_at::date AS date").where("lower(term) = ? AND created_at > ?", term.downcase, start_of(period))
Query(SearchLog)
.select('COUNT(*) AS count, created_at:? AS date')
# Q 499 : # SearchLog.select(select_sql).where("created_at > ?", start_date)
Query(SearchLog)

# Q 500 : # SearchLog.where("created_at < TIMESTAMP ?", SiteSetting.search_query_log_max_retention_days.days.ago).delete_all
Query(SearchLog)

# Q 501 : # SearchLog.where("id < ?", search_id[0]).delete_all
Query(SearchLog)

# Q 502 : # SharedDraft.where(category_id: destination_category_id).pluck(:topic_id)
Query(SharedDraft)
.where("category_id = ?")
.select('topic_id')
# Q 503 : # SharedDraft.where(topic: @topic).delete_all
Query(SharedDraft)
.where("topic = ?")
# Q 504 : # SharedDraft.where(topic_id: topic.id).update_all(category_id: category.id)
Query(SharedDraft)
.where("topic_id = ?")
# Q 505 : # SingleSignOnRecord.find_by(external_id: external_id)
Query(SingleSignOnRecord)
.where("external_id = ?")
# Q 506 : # SingleSignOnRecord.find_by(external_id: external_id).try(:user)
Query(SingleSignOnRecord)
.where("external_id = ?")
.select('user')
# Q 507 : # SingleSignOnRecord.find_by(external_id: external_id.to_s).try(:user)
Query(SingleSignOnRecord)
.where("external_id = ?")
.select('user')
# Q 508 : # SiteSetting.get("rate_limit_#{
# limit_key}")
Query(SiteSetting)

# Q 509 : # SiteSetting.get("rate_limit_new_user_#{
# limit_key}")
Query(SiteSetting)

# Q 510 : # SiteSetting.has_setting?("rate_limit_new_user_#{
# limit_key}")
Query(SiteSetting)

# Q 511 : # StylesheetCache.where(target: qualified_target, digest: digest).exists?
Query(StylesheetCache)
.where("target = ?")
.where("digest = ?")
.return_limit('1')
# Q 512 : # StylesheetCache.where(target: target)
Query(StylesheetCache)
.where("target = ?")
# Q 513 : # StylesheetCache.where(target: target).limit(max_to_keep).order("id desc").pluck(:id).last
Query(StylesheetCache)
.where("target = ?")
.return_limit('')
.order('id')
.select('id')
.return_limit('1')
# Q 514 : # Tag.find_by(name: text)
Query(Tag)
.where("name = ?")
# Q 515 : # Tag.find_by(name: text.gsub!("#{
# tag_postfix}", ""))
Query(Tag)
.where("name = ?")
# Q 516 : # Tag.find_by_name(params[:search_result_id]).id
Query(Tag)
.where("name = ?")
# Q 517 : # Tag.find_by_name(params[:tag_id])
Query(Tag)
.where("name = ?")
# Q 518 : # Tag.find_by_name(params[:tag_id])
Query(Tag)
.where("name = ?")
# Q 519 : # Tag.find_by_name(tag_name)
Query(Tag)
.where("name = ?")
# Q 520 : # Tag.find_by_name(tag_name)
Query(Tag)
.where("name = ?")
# Q 521 : # Tag.find_by_name(tag_name)
Query(Tag)
.where("name = ?")
# Q 522 : # Tag.includes(:tag_search_data).where("tag_search_data.search_data @@ #{
# ts_query}").references(:tag_search_data).order("name asc").limit(limit)
Query(Tag)
.order('id, name')
.return_limit('')
# Q 523 : # Tag.joins(:tag_groups).where("tag_groups.id NOT IN (
#         SELECT tag_group_id
#         FROM tag_group_permissions
#         WHERE group_id = ?)", Group::AUTO_GROUPS[:everyone])
Query(Tag)
.joins('tag_groups')
# Q 524 : # Tag.joins(tag_groups: :tag_group_permissions).where("tag_group_permissions.group_id = ? AND tag_group_permissions.permission_type = ?", Group::AUTO_GROUPS[:everyone], TagGroupPermission.permission_types[:readonly]).pluck(:name)
Query(Tag)
.select('name')
# Q 525 : # Tag.unused.pluck(:name)
Query(Tag)
.select('name')
# Q 526 : # Tag.where("tags.id NOT IN (SELECT tag_id FROM tag_group_memberships)")
Query(Tag)

# Q 527 : # Tag.where("tags.topic_count > 0")
Query(Tag)

# Q 528 : # Tag.where(id: @opts[:tags]).all
Query(Tag)
.where("id = ?")
# Q 529 : # Tag.where_name(clean_name).first
Query(Tag)
.return_limit('1')
# Q 530 : # Tag.where_name(name).exists?
Query(Tag)
.return_limit('1')
# Q 531 : # Tag.where_name(opts[:selected_tags]).pluck(:id)
Query(Tag)
.select('id')
# Q 532 : # Tag.where_name(params[:tag_id]).first
Query(Tag)
.return_limit('1')
# Q 533 : # Tag.where_name(params[:tag_values]).map do |tag|
#   
#   { value: tag.name, url: tag.full_url }
#   
# end.compact
Query(Tag)
.where("id != 0")
# Q 534 : # Tag.where_name(tag_names).all
Query(Tag)

# Q 535 : # Tag.where_name(tags).pluck(:id)
Query(Tag)
.select('id')
# Q 536 : # Tag.where_name(tags).pluck(:id)
Query(Tag)
.select('id')
# Q 537 : # Tag.where_name(tags_arg).pluck(:name)
Query(Tag)
.select('name')
# Q 538 : # TagGroup.find(params[:id])
Query(TagGroup)
.where("id = ?")
# Q 539 : # TagGroup.find_by(name: tag_group_name)
Query(TagGroup)
.where("name = ?")
# Q 540 : # TagGroup.includes(:parent_tag).where("tag_groups.id IN (
#           SELECT tg.id
#             FROM tag_groups tg
#       INNER JOIN tag_group_memberships tgm
#               ON tgm.tag_group_id = tg.id
#            WHERE tg.parent_tag_id IS NOT NULL
#              AND tgm.tag_id IN (?))", tags.map(&:id)).map(&:parent_tag)
Query(TagGroup)
.includes('parent_tag')
# Q 541 : # TagGroup.order("name ASC").includes(:parent_tag).preload(:tags).all
Query(TagGroup)
.order('id, name')
.includes('parent_tag')
.includes('tags')
# Q 542 : # TagGroup.visible(guardian).order("name ASC").includes(:tags).map
Query(TagGroup)
.order('id, name')
.includes('tags')
# Q 543 : # TagGroup.where("lower(name) ILIKE ?", "%#{
# params[:q].strip}%")
Query(TagGroup)

# Q 544 : # TagGroup.where(name: group_names).all.to_a
Query(TagGroup)
.where("name = ?")
# Q 545 : # TagGroup.where(one_per_topic: true).joins(:tag_group_memberships).where("tag_group_memberships.tag_id in (?)", selected_tag_ids).pluck(:id)
Query(TagGroup)
.where("one_per_topic = ?")
.joins('tag_group_memberships')
.select('id')
# Q 546 : # TagGroupMembership.select("distinct on (tag_group_id) tag_id").where(tag_id: selected_tag_ids).where(tag_group_id: exclude_group_ids).map(&:tag_id)
Query(TagGroupMembership)
.select('distinct on (tag_group_id) tag_id')
.where("tag_id = ?")
.where("tag_group_id = ?")
# Q 547 : # TagGroupMembership.select("distinct on (tag_group_id) tag_id").where(tag_id: selected_tag_ids).where(tag_group_id: exclude_group_ids).map(&:tag_id)
Query(TagGroupMembership)
.select('distinct on (tag_group_id) tag_id')
.where("tag_id = ?")
.where("tag_group_id = ?")
# Q 548 : # TagUser.lookup(user, :muted).joins(:tag).pluck("tags.name")
Query(TagUser)
.joins('tag')
.select('tags.name')
# Q 549 : # TagUser.lookup(user, :muted).joins(:tag).where("tags.name = ?", opts[:tags].first).exists?
Query(TagUser)
.joins('tag')
.return_limit('1')
# Q 550 : # TagUser.lookup(user, :muted).pluck(:tag_id)
Query(TagUser)
.select('tag_id')
# Q 551 : # TagUser.lookup(user, :muted).pluck(:tag_id)
Query(TagUser)
.select('tag_id')
# Q 552 : # TagUser.where(user: user, notification_level: notification_levels[level])
Query(TagUser)
.where("user = ?")
.where("notification_level = ?")
# Q 553 : # TagUser.where(user_id: user_id).where("tag_id IN (SELECT tag_id FROM topic_tags WHERE topic_id = :id)", id: topic_id).where("notification_level IN (:levels)", levels: [CategoryUser.notification_levels[:watching], CategoryUser.notification_levels[:tracking]]).order("notification_level DESC").limit(1).pluck(:notification_level).first
Query(TagUser)
.where("user_id = ?")
.order('id, notification_level')
.return_limit('')
.select('notification_level')
.return_limit('1')
# Q 554 : # TagUser.where(user_id: user_id, tag_id: tag_id).first
Query(TagUser)
.where("user_id = ?")
.where("tag_id = ?")
.return_limit('1')
# Q 555 : # Theme.find(@theme_id)
Query(Theme)
.where("id = ?")
# Q 556 : # Theme.find(import_theme_id)
Query(Theme)
.where("id = ?")
# Q 557 : # Theme.find(theme_id).update_javascript_cache!
Query(Theme)
.where("id = ?")
# Q 558 : # Theme.find_by(color_scheme_id: color_scheme_id)
Query(Theme)
.where("color_scheme_id = ?")
# Q 559 : # Theme.find_by(id: @theme_id)
Query(Theme)
.where("id = ?")
# Q 560 : # Theme.find_by(id: SiteSetting.default_theme_id)
Query(Theme)
.where("id = ?")
# Q 561 : # Theme.find_by(id: params[:id])
Query(Theme)
.where("id = ?")
# Q 562 : # Theme.find_by(id: params[:id])
Query(Theme)
.where("id = ?")
# Q 563 : # Theme.find_by(id: params[:id])
Query(Theme)
.where("id = ?")
# Q 564 : # Theme.find_by(id: params[:id])
Query(Theme)
.where("id = ?")
# Q 565 : # Theme.find_by(id: params[:id])
Query(Theme)
.where("id = ?")
# Q 566 : # Theme.find_by(id: params[:id])
Query(Theme)
.where("id = ?")
# Q 567 : # Theme.find_by(id: params[:id])
Query(Theme)
.where("id = ?")
# Q 568 : # Theme.find_by(id: theme_id)
Query(Theme)
.where("id = ?")
# Q 569 : # Theme.find_by(id: theme_id)
Query(Theme)
.where("id = ?")
# Q 570 : # Theme.find_by(name: theme_info["name"])
Query(Theme)
.where("name = ?")
# Q 571 : # Theme.find_by(remote_theme_id: remote_theme_id)
Query(Theme)
.where("remote_theme_id = ?")
# Q 572 : # Theme.order(:name).includes(:child_themes, :parent_themes, :remote_theme, :theme_settings, :settings_field, :locale_fields, :user, :color_scheme, theme_fields: :upload)
Query(Theme)
.order('name')
.includes('child_themes')
.includes('parent_themes')
.includes('remote_theme')
.includes('theme_settings')
.includes('settings_field')
.includes('locale_fields')
.includes('user')
.includes('color_scheme')
# Q 573 : # Theme.pluck(:id)
Query(Theme)
.select('id')
# Q 574 : # Theme.user_selectable.pluck(:id)
Query(Theme)
.select('id')
# Q 575 : # Theme.where("user_selectable OR id = ?", SiteSetting.default_theme_id).pluck(:id, :name)
Query(Theme)
.select('id')
.select('name')
# Q 576 : # Theme.where(color_scheme_id: scheme.id).order(:id).to_a
Query(Theme)
.where("color_scheme_id = ?")
.order('id')
# Q 577 : # Theme.where(color_scheme_id: self.id).pluck(:id)
Query(Theme)
.where("color_scheme_id = ?")
.select('id')
# Q 578 : # Theme.where(id: SiteSetting.default_theme_id).first.color_scheme
Query(ColorScheme)
.where("id = ?")
.return_limit('1')
.where("id = ?")
# Q 579 : # Theme.where(id: Theme.transform_ids([id])).each
Query(Theme)
.where("id = ?")
# Q 580 : # Theme.where(id: Theme.transform_ids(theme_ids)).each
Query(Theme)
.where("id = ?")
# Q 581 : # Theme.where(id: Theme.transform_ids(theme_ids)).find_each
Query(Theme)
.where("id = ?")
# Q 582 : # Theme.where(id: all_ids).includes(:remote_theme).select { |t|
#   
#   !t.supported? || !t.enabled?
# }.pluck(:id)
Query(Theme)
.where("id = ?")
.includes('remote_theme')
.select('id')
# Q 583 : # Theme.where(id: expected).each
Query(Theme)
.where("id = ?")
# Q 584 : # Theme.where(id: theme_ids).joins(:parent_themes).pluck(:parent_theme_id).uniq
Query(Theme)
.where("id = ?")
.joins('parent_themes')
.select('parent_theme_id')
.distinct('')
# Q 585 : # Theme.where(id: theme_ids.first).pluck(:color_scheme_id).first
Query(Theme)
.where("id = ?")
.select('color_scheme_id')
.return_limit('1')
# Q 586 : # ThemeField.find_by_theme_ids(ids).where(type_id: ThemeField.theme_var_type_ids).each
Query(ThemeField)
.where("theme_ids = ?")
.where("type_id = ?")
# Q 587 : # ThemeField.find_by_theme_ids(theme_ids).where(target_id: [Theme.targets[target], Theme.targets[:common]])
Query(ThemeField)
.where("theme_ids = ?")
.where("target_id = ?")
# Q 588 : # ThemeField.find_first_locale_fields(theme_ids, I18n.fallbacks[name])
Query(ThemeField)

# Q 589 : # ThemeField.where(id: field_ids_to_destroy).destroy_all
Query(ThemeField)
.where("id = ?")
# Q 590 : # ThemeField.where(name: "yaml", type_id: ThemeField.types[:yaml], theme_id: theme_ids).pluck(:updated_at)
Query(ThemeField)
.where("name = ?")
.where("type_id = ?")
.where("theme_id = ?")
.select('updated_at')
# Q 591 : # ThemeField.where(theme_id: theme_ids, target_id: Theme.targets[:extra_js]).where("compiler_version <> ?", ThemeField::COMPILER_VERSION)
Query(ThemeField)
.where("theme_id = ?")
.where("target_id = ?")
# Q 592 : # ThemeField.where(type_id: ThemeField.types[:theme_upload_var], name: THEME_SPRITE_VAR_NAME, theme_id: Theme.transform_ids(theme_ids)).pluck(:upload_id).each
Query(ThemeField)
.where("type_id = ?")
.where("name = ?")
.where("theme_id = ?")
.select('upload_id')
# Q 593 : # ThemeSetting.where(theme_id: theme_ids).pluck(:updated_at)
Query(ThemeSetting)
.where("theme_id = ?")
.select('updated_at')
# Q 594 : # Topic.find(params[:id])
Query(Topic)
.where("id = ?")
# Q 595 : # Topic.find(params[:id]).posts.first.id
Query(Topic)
.where("id = ?")
.return_limit('1')
# Q 596 : # Topic.find(params[:id].to_i)
Query(Topic)
.where("id = ?")
# Q 597 : # Topic.find(params[:topic_id].to_i)
Query(Topic)
.where("id = ?")
# Q 598 : # Topic.find(params[:topic_id].to_i)
Query(Topic)
.where("id = ?")
# Q 599 : # Topic.find(params[:topic_id].to_i)
Query(Topic)
.where("id = ?")
# Q 600 : # Topic.find(topic_id)
Query(Topic)
.where("id = ?")
# Q 601 : # Topic.find_by(id: @opts[:topic_id])
Query(Topic)
.where("id = ?")
# Q 602 : # Topic.find_by(id: @topic_id)
Query(Topic)
.where("id = ?")
# Q 603 : # Topic.find_by(id: details[:topic_id])
Query(Topic)
.where("id = ?")
# Q 604 : # Topic.find_by(id: opts[:topic_id])
Query(Topic)
.where("id = ?")
# Q 605 : # Topic.find_by(id: params[:id])
Query(Topic)
.where("id = ?")
# Q 606 : # Topic.find_by(id: params[:id])
Query(Topic)
.where("id = ?")
# Q 607 : # Topic.find_by(id: params[:id])
Query(Topic)
.where("id = ?")
# Q 608 : # Topic.find_by(id: params[:id])
Query(Topic)
.where("id = ?")
# Q 609 : # Topic.find_by(id: params[:id])
Query(Topic)
.where("id = ?")
# Q 610 : # Topic.find_by(id: params[:reviewable_claimed_topic][:topic_id])
Query(Topic)
.where("id = ?")
# Q 611 : # Topic.find_by(id: params[:topic_id])
Query(Topic)
.where("id = ?")
# Q 612 : # Topic.find_by(id: params[:topic_id])
Query(Topic)
.where("id = ?")
# Q 613 : # Topic.find_by(id: params[:topic_id])
Query(Topic)
.where("id = ?")
# Q 614 : # Topic.find_by(id: params[:topic_id])
Query(Topic)
.where("id = ?")
# Q 615 : # Topic.find_by(id: params[:topic_id])
Query(Topic)
.where("id = ?")
# Q 616 : # Topic.find_by(id: params[:topic_id])
Query(Topic)
.where("id = ?")
# Q 617 : # Topic.find_by(id: params[:topic_id])
Query(Topic)
.where("id = ?")
# Q 618 : # Topic.find_by(id: params[:topic_id].to_i)
Query(Topic)
.where("id = ?")
# Q 619 : # Topic.find_by(id: params[:topic_id].to_i)
Query(Topic)
.where("id = ?")
# Q 620 : # Topic.find_by(id: params[:topic_id].to_i)
Query(Topic)
.where("id = ?")
# Q 621 : # Topic.find_by(id: params[:topic_id].to_i)
Query(Topic)
.where("id = ?")
# Q 622 : # Topic.find_by(id: params[:topic_id].to_i)
Query(Topic)
.where("id = ?")
# Q 623 : # Topic.find_by(id: params[:topic_id].to_i)
Query(Topic)
.where("id = ?")
# Q 624 : # Topic.find_by(id: route[:topic_id])
Query(Topic)
.where("id = ?")
# Q 625 : # Topic.find_by(id: search_context[:id].to_i)
Query(Topic)
.where("id = ?")
# Q 626 : # Topic.find_by(id: topic_id)
Query(Topic)
.where("id = ?")
# Q 627 : # Topic.find_by(id: topic_id)
Query(Topic)
.where("id = ?")
# Q 628 : # Topic.find_by(id: topic_id)
Query(Topic)
.where("id = ?")
# Q 629 : # Topic.find_by(id: topic_id)
Query(Topic)
.where("id = ?")
# Q 630 : # Topic.find_by(id: topic_id)
Query(Topic)
.where("id = ?")
# Q 631 : # Topic.find_by(id: topic_id)
Query(Topic)
.where("id = ?")
# Q 632 : # Topic.find_by(id: topic_id)
Query(Topic)
.where("id = ?")
# Q 633 : # Topic.find_by(id: topic_id)
Query(Topic)
.where("id = ?")
# Q 634 : # Topic.find_by(id: topic_id)
Query(Topic)
.where("id = ?")
# Q 635 : # Topic.find_by(slug: "welcome-to-discourse", archetype: Archetype.default)
Query(Topic)
.where("slug = ?")
.where("archetype = ?")
# Q 636 : # Topic.find_by(slug: params[:id].downcase)
Query(Topic)
.where("slug = ?")
# Q 637 : # Topic.find_by(slug: params[:id].downcase)
Query(Topic)
.where("slug = ?")
# Q 638 : # Topic.find_by(slug: params[:slug].downcase)
Query(Topic)
.where("slug = ?")
# Q 639 : # Topic.find_by_id(SiteSetting.get(map[@page][:topic_id]))
Query(Topic)
.where("id = ?")
# Q 640 : # Topic.find_by_id(id)
Query(Topic)
.where("id = ?")
# Q 641 : # Topic.find_by_id(params[:topic_id])
Query(Topic)
.where("id = ?")
# Q 642 : # Topic.includes(:allowed_groups).select(:highest_post_number, :archetype, :id).find_by(id: topic_id)
Query(Topic)
.select('highest_post_number')
.select('archetype')
.select('id')
.where("id = ?")
# Q 643 : # Topic.includes(:allowed_groups).select(:highest_post_number, :archetype, :id).find_by(id: topic_id)
Query(Topic)
.select('highest_post_number')
.select('archetype')
.select('id')
.where("id = ?")
# Q 644 : # Topic.includes(:category).find_by(id: hash[:target_topic_id])
Query(Topic)
.where("id = ?")
# Q 645 : # Topic.joins("INNER JOIN posts ON topics.id = posts.topic_id AND topics.user_id <> posts.user_id").where("posts.deleted_at IS NULL AND posts.user_id = ?", self.user_id).distinct.count
Query(Topic)
.distinct('')
# Q 646 : # Topic.joins("JOIN posts AS p ON p.topic_id = topics.id AND p.post_number = 1").where("topics.id IN (?)", candidate_ids).order("similarity DESC").limit(SiteSetting.max_similar_results)
Query(Topic)
.order('id, similarity')
.return_limit('')
# Q 647 : # Topic.joins(:posts).where("posts.id" => ids).select(["posts.id as post_id", "post_number", "topics.slug", "topics.title", "topics.id"]).each
Query(Topic)
.where("id" = ?")
.select('posts.id as post_id
post_number
topics.slug
topics.title
topics.id
')
# Q 648 : # Topic.listable_topics.visible.secured(@guardian).where("topics.category_id in (?)", top_categories.keys).where(user: @user).group("topics.category_id").order("COUNT(*) DESC").pluck("category_id, COUNT(*)").each
Query(Topic)
.where("user = ?")
.group('category_id, id')
.order('id')
.select('category_id, COUNT(*)')
# Q 649 : # Topic.listable_topics.visible.secured.order("created_at desc").limit(max)
Query(Topic)
.order('id, created_at')
.return_limit('')
# Q 650 : # Topic.listable_topics.visible.secured.order("views desc").limit(max)
Query(Topic)
.order('id, views')
.return_limit('')
# Q 651 : # Topic.listable_topics.where("created_at > ?", 30.days.ago).count
Query(Topic)

# Q 652 : # Topic.listable_topics.where("created_at > ?", 7.days.ago).count
Query(Topic)

# Q 653 : # Topic.listable_topics.where(archetype: Archetype.banner).count
Query(Topic)
.where("archetype = ?")
# Q 654 : # Topic.listable_topics.where(id: topic_ids).minimum(:updated_at)
Query(Topic)
.where("id = ?")
# Q 655 : # Topic.listable_topics.where(pinned_globally: true).order(:created_at).limit(1).pluck(:id)
Query(Topic)
.where("pinned_globally = ?")
.order('created_at')
.return_limit('')
.select('id')
# Q 656 : # Topic.listable_topics.where(slug: slug).pluck(:id)
Query(Topic)
.where("slug = ?")
.select('id')
# Q 657 : # Topic.order("topics.created_at desc").where("visible").where("topics.category_id = :id", id: self.id).limit(1).pluck("topics.id").first
Query(Topic)
.order('id, created_at')
.return_limit('')
.select('topics.id')
.return_limit('1')
# Q 658 : # Topic.private_messages.where("topics.user_id > 0").with_subtype(topic_subtype)
Query(Topic)

# Q 659 : # Topic.private_messages.where(id: topic_id).exists?
Query(Topic)
.where("id = ?")
.return_limit('1')
# Q 660 : # Topic.secured(@guardian).listable_topics.visible.where(user: @user).order("like_count DESC, created_at DESC").limit(MAX_SUMMARY_RESULTS)
Query(Topic)
.where("user = ?")
.order('id, like_count, created_at')
.return_limit('')
# Q 661 : # Topic.select("id, slug, title").where("id in (?)", num_clicks.map { |z|
#   
#   z[0]
# })
Query(Topic)
.select('id, slug, title')
# Q 662 : # Topic.select("topics.category_id, COUNT(*) topic_count, SUM(topics.posts_count) post_count").where("topics.id NOT IN (select cc.topic_id from categories cc WHERE topic_id IS NOT NULL)").group("topics.category_id").visible.to_sql
Query(Topic)
.select('topics.category_id, COUNT(*) topic_count, SUM(topics.posts_count) post_count')
.group('category_id, id')
# Q 663 : # Topic.unscoped.where(id: manager.args[:topic_id]).first
Query(Topic)
.where("id = ?")
.return_limit('1')
# Q 664 : # Topic.visible.listable_topics.secured(Guardian.new(user)).joins("JOIN topic_search_data s ON topics.id = s.topic_id").joins("LEFT JOIN categories c ON topics.id = c.topic_id").where("search_data @@ #{
# ts_query}").where("c.topic_id IS NULL").order("ts_rank(search_data, #{
# ts_query}) DESC").limit(SiteSetting.max_similar_results * 3)
Query(Topic)
.order('id, search_data')
.return_limit('')
# Q 665 : # Topic.visible.secured(Guardian.new(user)).joins("LEFT OUTER JOIN topic_users ON topic_users.topic_id = topics.id AND topic_users.user_id = #{
# user.id.to_i}").joins("LEFT OUTER JOIN category_users ON category_users.category_id = topics.category_id AND category_users.user_id = #{
# user.id.to_i}").joins("LEFT OUTER JOIN users ON users.id = topics.user_id").where(closed: false, archived: false).where("COALESCE(topic_users.notification_level, 1) <> ?", TopicUser.notification_levels[:muted]).created_since(since).where("topics.created_at < ?", (
# SiteSetting.editing_grace_period || 0).seconds.ago).listable_topics.includes(:category)
Query(Topic)
.where("closed = ?")
.where("archived = ?")
# Q 666 : # Topic.where("id = :topic_id AND NOT EXISTS(SELECT 1 FROM POSTS WHERE topic_id = :topic_id AND NOT hidden)", topic_id: topic_id).update_all(visible: false)
Query(Topic)

# Q 667 : # Topic.where("pinned_until < now()").update_all(pinned_at: nil, pinned_globally: false, pinned_until: nil)
Query(Topic)

# Q 668 : # Topic.where("topics.archetype = ?", Archetype.private_message).joins("LEFT JOIN topic_users tu ON topics.id = tu.topic_id AND tu.user_id = #{
# @user.id.to_i}")
Query(Topic)

# Q 669 : # Topic.where("topics.user_id > 0")
Query(Topic)

# Q 670 : # Topic.where(archetype: Archetype.banner).first
Query(Topic)
.where("archetype = ?")
.return_limit('1')
# Q 671 : # Topic.where(archetype: Archetype.banner).first
Query(Topic)
.where("archetype = ?")
.return_limit('1')
# Q 672 : # Topic.where(id: @topic_ids)
Query(Topic)
.where("id = ?")
# Q 673 : # Topic.where(id: args[:topic_id], archetype: Archetype.private_message).exists?
Query(Topic)
.where("id = ?")
.where("archetype = ?")
.return_limit('1')
# Q 674 : # Topic.where(id: category_featured_topics.map(&:topic_id))
Query(Topic)
.where("id = ?")
# Q 675 : # Topic.where(id: ft_by_id.keys).to_a
Query(Topic)
.where("id = ?")
# Q 676 : # Topic.where(id: hash[:target_topic_id], archetype: Archetype.private_message).exists?
Query(Topic)
.where("id = ?")
.where("archetype = ?")
.return_limit('1')
# Q 677 : # Topic.where(id: params[:topic_id]).first
Query(Topic)
.where("id = ?")
.return_limit('1')
# Q 678 : # Topic.where(id: params[:topic_id]).with_deleted.first
Query(Topic)
.where("id = ?")
.return_limit('1')
# Q 679 : # Topic.where(id: route[:topic_id].to_i).first
Query(Topic)
.where("id = ?")
.return_limit('1')
# Q 680 : # Topic.where(id: topic_id)
Query(Topic)
.where("id = ?")
# Q 681 : # Topic.where(id: topic_id)
Query(Topic)
.where("id = ?")
# Q 682 : # Topic.where(id: topic_id).first
Query(Topic)
.where("id = ?")
.return_limit('1')
# Q 683 : # Topic.where(id: topic_id).first
Query(Topic)
.where("id = ?")
.return_limit('1')
# Q 684 : # Topic.where(id: topic_id).pluck(:archetype).first
Query(Topic)
.where("id = ?")
.select('archetype')
.return_limit('1')
# Q 685 : # Topic.where(id: topic_ids).order("reviewable_score DESC")
Query(Topic)
.where("id = ?")
.order('id, reviewable_score')
# Q 686 : # Topic.where(id: topic_ids).to_a
Query(Topic)
.where("id = ?")
# Q 687 : # Topic.where(id: topic_ids).with_deleted.where.not(archetype: "private_message")
Query(Topic)
.where("id = ?")
.where("archetype = ?")
# Q 688 : # Topic.with_deleted.find(post.topic_id)
Query(Topic)
.where("id = ?")
# Q 689 : # Topic.with_deleted.find(post.topic_id)
Query(Topic)
.where("id = ?")
# Q 690 : # Topic.with_deleted.find(post.topic_id)
Query(Topic)
.where("id = ?")
# Q 691 : # Topic.with_deleted.find(topic_id)
Query(Topic)
.where("id = ?")
# Q 692 : # Topic.with_deleted.find_by(id: id).destroy!
Query(Topic)
.where("id = ?")
# Q 693 : # Topic.with_deleted.find_by(id: topic_id)
Query(Topic)
.where("id = ?")
# Q 694 : # Topic.with_deleted.where(id: params[:topic_id]).first
Query(Topic)
.where("id = ?")
.return_limit('1')
# Q 695 : # Topic.with_deleted.where(id: topic_ids.to_a).to_a
Query(Topic)
.where("id = ?")
# Q 696 : # Topic.with_deleted.where(id: topic_or_topic_id).includes(:category)
Query(Topic)
.where("id = ?")
# Q 697 : # TopicEmbed.find_by("lower(embed_url) = ?", url)
Query(TopicEmbed)

# Q 698 : # TopicEmbed.where("embed_url ~* ?", "^https?://#{
# Regexp.escape(embed_url)}$").pluck(:topic_id).first
Query(TopicEmbed)
.select('topic_id')
.return_limit('1')
# Q 699 : # TopicEmbed.where(embed_url: @embed_url).exists?
Query(TopicEmbed)
.where("embed_url = ?")
.return_limit('1')
# Q 700 : # TopicEmbed.where(embed_url: @embed_url).exists?
Query(TopicEmbed)
.where("embed_url = ?")
.return_limit('1')
# Q 701 : # TopicEmbed.where(embed_url: embed_url).first
Query(TopicEmbed)
.where("embed_url = ?")
.return_limit('1')
# Q 702 : # TopicEmbed.where(embed_url: urls).includes(:topic).references(:topic)
Query(TopicEmbed)
.where("embed_url = ?")
.includes('topic')
# Q 703 : # TopicEmbed.where(topic_id: id).exists?
Query(TopicEmbed)
.where("topic_id = ?")
.return_limit('1')
# Q 704 : # TopicEmbed.where(topic_id: post.topic_id).pluck(:embed_url).first
Query(TopicEmbed)
.where("topic_id = ?")
.select('embed_url')
.return_limit('1')
# Q 705 : # TopicLink.find_by(topic_id: topic_id, post_id: reflected_post.try(:id), url: reflected_url)
Query(TopicLink)
.where("topic_id = ?")
.where("post_id = ?")
.where("url = ?")
# Q 706 : # TopicLink.find_by(url: url)
Query(TopicLink)
.where("url = ?")
# Q 707 : # TopicLink.includes(:post, :user).joins(:post, :user).where("posts.id IS NOT NULL AND users.id IS NOT NULL").where(topic_id: topic.id, reflection: false).last(200)
Query(TopicLink)
.includes('post')
.includes('user')
.joins('post')
.joins('user')
.where("topic_id = ?")
.where("reflection = ?")
.return_limit('1')
# Q 708 : # TopicLink.joins(:topic, :post).includes(:topic, :post).where("posts.post_type IN (?)", Topic.visible_post_types(@guardian && @guardian.user)).merge(Topic.listable_topics.visible.secured(@guardian)).where(user: @user).where(internal: false, reflection: false, quote: false).order("clicks DESC, topic_links.created_at DESC").limit(MAX_SUMMARY_RESULTS)
Query(TopicLink)
.joins('topic')
.joins('post')
.includes('topic')
.includes('post')
.where("user = ?")
.where("internal = ?")
.where("reflection = ?")
.where("quote = ?")
.order('id, clicks, created_at')
.return_limit('')
# Q 709 : # TopicLink.where("(post_id = :post_id AND NOT reflection) OR (link_post_id = :post_id AND reflection)", post_id: post.id).delete_all
Query(TopicLink)

# Q 710 : # TopicLink.where("link_post_id = :post_id AND reflection", post_id: post.id).delete_all
Query(TopicLink)

# Q 711 : # TopicLink.where(domain: hosts.keys, user_id: acting_user.id).group(:domain, :post_id).count.each_key
Query(TopicLink)
.where("domain = ?")
.where("user_id = ?")
.group('domain, post_id')
# Q 712 : # TopicTimer.where("topic_timers.execute_at < ?", Time.zone.now).find_each
Query(TopicTimer)

# Q 713 : # TopicTimer.where(topic: @topic).update_all(deleted_at: DateTime.now, deleted_by_id: @published_by.id)
Query(TopicTimer)
.where("topic = ?")
# Q 714 : # TopicUser.find_by(topic_id: topic, user_id: user)
Query(TopicUser)
.where("topic_id = ?")
.where("user_id = ?")
# Q 715 : # TopicUser.joins(:topic).where(:user => user, :notification_level => TopicUser.notification_levels[:watching], "topics.category_id" => topic.category_id).update_all(notification_level: TopicUser.notification_levels[:tracking])
Query(TopicUser)
.joins('topic')
.where("user = ?")
.where("notification_level = ?")
.where("category_id" = ?")
# Q 716 : # TopicUser.joins(:topic).where(user: @user, notification_level: watching, "topics.category_id" => @topic.category_id).count
Query(TopicUser)
.joins('topic')
.where("user = ?")
.where("notification_level = ?")
.where("category_id" = ?")
# Q 717 : # TopicUser.where("user_id = ? and topic_id in (?)", user_id, topic_ids).delete_all
Query(TopicUser)

# Q 718 : # TopicUser.where(topic: self, user: target_user, notification_level: TopicUser.notification_levels[:muted]).exists?
Query(TopicUser)
.where("topic = ?")
.where("user = ?")
.where("notification_level = ?")
.return_limit('1')
# Q 719 : # TopicUser.where(topic_id: @original_topic.id, user_id: posts.pluck(:user_id)).pluck(:user_id, :notification_level).to_h
Query(TopicUser)
.where("topic_id = ?")
.where("user_id = ?")
.select('user_id')
.select('notification_level')
# Q 720 : # TopicUser.where(topic_id: @post.topic_id, user_id: @post.user_id)
Query(TopicUser)
.where("topic_id = ?")
.where("user_id = ?")
# Q 721 : # TopicUser.where(topic_id: topic.id, user_id: user.id).update_all(notification_level: TopicUser.notification_levels[:muted])
Query(TopicUser)
.where("topic_id = ?")
.where("user_id = ?")
# Q 722 : # TopicUser.where(topic_id: topic.id, user_id: user.id).update_all(notification_level: TopicUser.notification_levels[:tracking])
Query(TopicUser)
.where("topic_id = ?")
.where("user_id = ?")
# Q 723 : # TopicUser.where(topic_id: topic_id, user_id: user_id).update_all([attrs_sql, *vals])
Query(TopicUser)
.where("topic_id = ?")
.where("user_id = ?")
# Q 724 : # TopicUser.where(topic_id: topic_id, user_id: user_id).update_all(last_visited_at: now)
Query(TopicUser)
.where("topic_id = ?")
.where("user_id = ?")
# Q 725 : # TopicUser.where(user_id: user.id, topic_id: topic.id).update_all(highest_seen_post_number: last_read, last_read_post_number: last_read)
Query(TopicUser)
.where("user_id = ?")
.where("topic_id = ?")
# Q 726 : # TopicUser.where(user_id: user_id, topic_id: topic.id).exists?
Query(TopicUser)
.where("user_id = ?")
.where("topic_id = ?")
.return_limit('1')
# Q 727 : # TopicUser.where(user_id: user_id, topic_id: topic_id).where("notifications_reason_id IS NULL OR (notification_level < :max AND notification_level > :min)", max: notification_level, min: notification_levels[:regular]).exists?
Query(TopicUser)
.where("user_id = ?")
.where("topic_id = ?")
.return_limit('1')
# Q 728 : # TopicUser.where(user_id: user_id, topic_id: topic_id, notification_level: TopicUser.notification_levels[:muted]).exists?
Query(TopicUser)
.where("user_id = ?")
.where("topic_id = ?")
.where("notification_level = ?")
.return_limit('1')
# Q 729 : # TopicViewItem.where(user_id: @user.id).select("topic_id")
Query(TopicViewItem)
.where("user_id = ?")
.select('topic_id')
# Q 730 : # TranslationOverride.where(locale: I18n.locale).pluck(:translation_key)
Query(TranslationOverride)
.where("locale = ?")
.select('translation_key')
# Q 731 : # TranslationOverride.where(locale: I18n.locale).pluck(:translation_key)
Query(TranslationOverride)
.where("locale = ?")
.select('translation_key')
# Q 732 : # TranslationOverride.where(locale: locale).pluck(:translation_key, :value, :compiled_js)
Query(TranslationOverride)
.where("locale = ?")
.select('translation_key')
.select('value')
.select('compiled_js')
# Q 733 : # TranslationOverride.where(locale: locale, translation_key: keys).delete_all
Query(TranslationOverride)
.where("locale = ?")
.where("translation_key = ?")
# Q 734 : # TranslationOverride.where(translation_key: key, locale: I18n.locale).pluck(:value)
Query(TranslationOverride)
.where("translation_key = ?")
.where("locale = ?")
.select('value')
# Q 735 : # UnsubscribeKey.find_by(key: params[:key])
Query(UnsubscribeKey)
.where("key = ?")
# Q 736 : # UnsubscribeKey.find_by(key: params[:key])
Query(UnsubscribeKey)
.where("key = ?")
# Q 737 : # Upload.by_users.where("url NOT LIKE '%/original/_X/%' AND url LIKE '%/uploads/#{
# db}%'").order(id: :desc)
Query(Upload)
.order('id')
# Q 738 : # Upload.find(SKETCH_LOGO_ID)
Query(Upload)
.where("id = ?")
# Q 739 : # Upload.find(upload_id)
Query(Upload)
.where("id = ?")
# Q 740 : # Upload.find_by(id: params[:id], url: request.env["PATH_INFO"])
Query(Upload)
.where("id = ?")
.where("url = ?")
# Q 741 : # Upload.find_by(id: upload_id)
Query(Upload)
.where("id = ?")
# Q 742 : # Upload.find_by(id: upload_id)
Query(Upload)
.where("id = ?")
# Q 743 : # Upload.find_by(id: value)
Query(Upload)
.where("id = ?")
# Q 744 : # Upload.find_by(sha1: params[:sha])
Query(Upload)
.where("sha1 = ?")
# Q 745 : # Upload.find_by(sha1: sha1)
Query(Upload)
.where("sha1 = ?")
# Q 746 : # Upload.find_by(sha1: sha1)
Query(Upload)
.where("sha1 = ?")
# Q 747 : # Upload.find_by(sha1: sha1)
Query(Upload)
.where("sha1 = ?")
# Q 748 : # Upload.find_by(url: url)
Query(Upload)
.where("url = ?")
# Q 749 : # Upload.find_by(url: urls.sample)
Query(Upload)
.where("url = ?")
# Q 750 : # Upload.find_by(url: value)
Query(Upload)
.where("url = ?")
# Q 751 : # Upload.find_by_id(upload_id)
Query(Upload)
.where("id = ?")
# Q 752 : # Upload.joins(:post_uploads).where("post_uploads.post_id >= ? AND post_uploads.post_id <= ?", ids.min, ids.max).pluck(:sha1)
Query(Upload)
.joins('post_uploads')
.select('sha1')
# Q 753 : # Upload.where("id < ?", Upload::SEEDED_ID_THRESHOLD).pluck(:id, :url).to_h
Query(Upload)
.select('id')
.select('url')
# Q 754 : # Upload.where("uploads.extension IS NOT NULL").find_each
Query(Upload)

# Q 755 : # Upload.where(extension: "unknown").update_all(extension: nil)
Query(Upload)
.where("extension = ?")
# Q 756 : # Upload.where(id: downloaded_images.values).pluck(:id)
Query(Upload)
.where("id = ?")
.select('id')
# Q 757 : # Upload.where(sha1: map.values).pluck(:sha1, :url, :extension).each
Query(Upload)
.where("sha1 = ?")
.select('sha1')
.select('url')
.select('extension')
# Q 758 : # Upload.where(sha1: sha1).pluck(:id).first
Query(Upload)
.where("sha1 = ?")
.select('id')
.return_limit('1')
# Q 759 : # User.admins.real.order(:id).first
Query(User)
.order('id')
.return_limit('1')
# Q 760 : # User.find(-2)
Query(User)
.where("id = ?")
# Q 761 : # User.find(-2)
Query(User)
.where("id = ?")
# Q 762 : # User.find(@fields["user_id"])
Query(User)
.where("id = ?")
# Q 763 : # User.find(@post.user_id)
Query(User)
.where("id = ?")
# Q 764 : # User.find(p.user_id)
Query(User)
.where("id = ?")
# Q 765 : # User.find(p.user_id)
Query(User)
.where("id = ?")
# Q 766 : # User.find(params[:for_user_id])
Query(User)
.where("id = ?")
# Q 767 : # User.find(params[:user_id].to_i)
Query(User)
.where("id = ?")
# Q 768 : # User.find(params[:user_id].to_i)
Query(User)
.where("id = ?")
# Q 769 : # User.find(params[:user_id].to_i)
Query(User)
.where("id = ?")
# Q 770 : # User.find(parsed["performed_by"].to_i)
Query(User)
.where("id = ?")
# Q 771 : # User.find(parsed["target_user_id"].to_i)
Query(User)
.where("id = ?")
# Q 772 : # User.find(user.id)
Query(User)
.where("id = ?")
# Q 773 : # User.find(user_id)
Query(User)
.where("id = ?")
# Q 774 : # User.find_by(id: @user_id)
Query(User)
.where("id = ?")
# Q 775 : # User.find_by(id: @user_id)
Query(User)
.where("id = ?")
# Q 776 : # User.find_by(id: SYSTEM_USER_ID)
Query(User)
.where("id = ?")
# Q 777 : # User.find_by(id: opts[:user_id])
Query(User)
.where("id = ?")
# Q 778 : # User.find_by(id: params[:id])
Query(User)
.where("id = ?")
# Q 779 : # User.find_by(id: params[:id].to_i)
Query(User)
.where("id = ?")
# Q 780 : # User.find_by(id: params[:user_id])
Query(User)
.where("id = ?")
# Q 781 : # User.find_by(id: params[:user_id])
Query(User)
.where("id = ?")
# Q 782 : # User.find_by(id: uid.to_i)
Query(User)
.where("id = ?")
# Q 783 : # User.find_by(id: user_id)
Query(User)
.where("id = ?")
# Q 784 : # User.find_by(id: user_id)
Query(User)
.where("id = ?")
# Q 785 : # User.find_by(id: user_id).staff?
Query(User)
.where("id = ?")
# Q 786 : # User.find_by(id: user_id.to_i)
Query(User)
.where("id = ?")
# Q 787 : # User.find_by(username: params[:username])
Query(User)
.where("username = ?")
# Q 788 : # User.find_by(username: params[:username].to_s)
Query(User)
.where("username = ?")
# Q 789 : # User.find_by(username: username)
Query(User)
.where("username = ?")
# Q 790 : # User.find_by(username_lower: SiteSetting.site_contact_username.downcase)
Query(User)
.where("username_lower = ?")
# Q 791 : # User.find_by(username_lower: api_username.downcase)
Query(User)
.where("username_lower = ?")
# Q 792 : # User.find_by(username_lower: params[:username].downcase)
Query(User)
.where("username_lower = ?")
# Q 793 : # User.find_by(username_lower: search_context[:id].downcase)
Query(User)
.where("username_lower = ?")
# Q 794 : # User.find_by(username_lower: username.downcase)
Query(User)
.where("username_lower = ?")
# Q 795 : # User.find_by(username_lower: username.downcase)
Query(User)
.where("username_lower = ?")
# Q 796 : # User.find_by(username_lower: username.downcase)
Query(User)
.where("username_lower = ?")
# Q 797 : # User.find_by(username_lower: username.downcase)
Query(User)
.where("username_lower = ?")
# Q 798 : # User.find_by_email(@email)
Query(User)
.where("email = ?")
# Q 799 : # User.find_by_email(@email)
Query(User)
.where("email = ?")
# Q 800 : # User.find_by_email(@from_email)
Query(User)
.where("email = ?")
# Q 801 : # User.find_by_email(@user_info[:email])
Query(User)
.where("email = ?")
# Q 802 : # User.find_by_email(@user_info[:email])
Query(User)
.where("email = ?")
# Q 803 : # User.find_by_email(auth_token.dig(:info, :email))
Query(User)
.where("email = ?")
# Q 804 : # User.find_by_email(candidate[:email])
Query(User)
.where("email = ?")
# Q 805 : # User.find_by_email(email)
Query(User)
.where("email = ?")
# Q 806 : # User.find_by_email(email)
Query(User)
.where("email = ?")
# Q 807 : # User.find_by_email(email)
Query(User)
.where("email = ?")
# Q 808 : # User.find_by_email(email)
Query(User)
.where("email = ?")
# Q 809 : # User.find_by_email(email)
Query(User)
.where("email = ?")
# Q 810 : # User.find_by_email(email)
Query(User)
.where("email = ?")
# Q 811 : # User.find_by_email(email)
Query(User)
.where("email = ?")
# Q 812 : # User.find_by_email(email)
Query(User)
.where("email = ?")
# Q 813 : # User.find_by_email(email)
Query(User)
.where("email = ?")
# Q 814 : # User.find_by_email(email)
Query(User)
.where("email = ?")
# Q 815 : # User.find_by_email(lower_email)
Query(User)
.where("email = ?")
# Q 816 : # User.find_by_email(to_address).staff?
Query(User)
.where("email = ?")
# Q 817 : # User.find_by_email(user.primary_email.email)
Query(User)
.where("email = ?")
# Q 818 : # User.find_by_id(self.last_editor_id)
Query(User)
.where("id = ?")
# Q 819 : # User.find_by_username(otp_username)
Query(User)
.where("username = ?")
# Q 820 : # User.find_by_username(params[:session_id])
Query(User)
.where("username = ?")
# Q 821 : # User.find_by_username(params[:username])
Query(User)
.where("username = ?")
# Q 822 : # User.find_by_username(params[:username])
Query(User)
.where("username = ?")
# Q 823 : # User.find_by_username(username).id
Query(User)
.where("username = ?")
# Q 824 : # User.find_by_username_or_email(login)
Query(User)
.where("username_or_email = ?")
# Q 825 : # User.find_by_username_or_email(params[:login])
Query(User)
.where("username_or_email = ?")
# Q 826 : # User.find_by_username_or_email(params[:username])
Query(User)
.where("username_or_email = ?")
# Q 827 : # User.find_by_username_or_email(params[:username].to_s)
Query(User)
.where("username_or_email = ?")
# Q 828 : # User.find_by_username_or_email(params[:username_or_email])
Query(User)
.where("username_or_email = ?")
# Q 829 : # User.find_by_username_or_email(username_or_email)
Query(User)
.where("username_or_email = ?")
# Q 830 : # User.find_by_username_or_email(username_or_email)
Query(User)
.where("username_or_email = ?")
# Q 831 : # User.human_users.find_by_username_or_email(params[:login])
Query(User)
.where("username_or_email = ?")
# Q 832 : # User.human_users.where("admin OR moderator OR id = ?", user_id).pluck(:id)
Query(User)
.select('id')
# Q 833 : # User.human_users.where("admin OR moderator").pluck(:id)
Query(User)
.select('id')
# Q 834 : # User.human_users.where(admin: true).pluck(:username).each
Query(User)
.where("admin = ?")
.select('username')
# Q 835 : # User.includes(:user_option).where(username: names).find_each
Query(User)
.includes('user_option')
.where("username = ?")
# Q 836 : # User.includes(:user_search_data).references(:user_search_data).where(active: true).where(staged: false).where("user_search_data.search_data @@ #{
# ts_query("simple")}").order("CASE WHEN username_lower = '#{
# @original_term.downcase}' THEN 0 ELSE 1 END").order("last_posted_at DESC").limit(limit)
Query(User)
.where("active = ?")
.where("staged = ?")
.order('id, username_lower')
.order('id, last_posted_at')
.return_limit('')
# Q 837 : # User.includes(:user_stat).where(id: user_ids.to_a).to_a
Query(User)
.includes('user_stat')
.where("id = ?")
# Q 838 : # User.joins("JOIN (SELECT unnest uid, row_number() OVER () AS rn
#       FROM unnest('{#{
# ids.join(",")}}'::int[])
#     ) x on uid = users.id").order("rn")
Query(User)
.order('id, rn')
# Q 839 : # User.joins("LEFT JOIN user_options ON user_options.user_id = users.id").joins("LEFT JOIN muted_users ON muted_users.user_id = users.id AND muted_users.muted_user_id = #{
# @user.id.to_i}").joins("LEFT JOIN ignored_users ON ignored_users.user_id = users.id AND ignored_users.ignored_user_id = #{
# @user.id.to_i}").where("user_options.user_id IS NOT NULL").where("
#           (user_options.user_id IN (:user_ids) AND NOT user_options.allow_private_messages) OR
#           muted_users.user_id IN (:user_ids) OR
#           ignored_users.user_id IN (:user_ids)
#         ", user_ids: users.keys).pluck(:id).each
Query(User)
.select('id')
# Q 840 : # User.joins(:posts).where("posts.topic_id IN (?)", topic_ids).distinct
Query(User)
.joins('posts')
.distinct('')
# Q 841 : # User.joins(:user_emails).exists?(staged: true, username_lower: lower, user_emails: { primary: true, email: email })
Query(User)
.joins('user_emails')
.return_limit('1')
# Q 842 : # User.joins(:user_emails).where("username_lower IN (:users) OR lower(user_emails.email) IN (:users)", users: users).pluck(:id, :username_lower, :"user_emails.email")
Query(User)
.joins('user_emails')
.select('id')
.select('username_lower')
# Q 843 : # User.joins(:user_emails).where(id: @user_ids).select(AvatarLookup.lookup_columns).each
Query(User)
.joins('user_emails')
.where("id = ?")
# Q 844 : # User.select(:id).find_by(username_lower: username.downcase)
Query(User)
.select('id')
.where("username_lower = ?")
# Q 845 : # User.select(:username).order("last_posted_at desc").limit(20)
Query(User)
.select('username')
.order('id, last_posted_at')
.return_limit('')
# Q 846 : # User.staff.human_users.where("username_lower not in (?)", reserved_usernames).count
Query(User)

# Q 847 : # User.unscoped.where(trust_level: TrustLevel[0]).where(ip_address: ip_address.to_s).count
Query(User)
.where("trust_level = ?")
.where("ip_address = ?")
# Q 848 : # User.where("0=1")
Query(User)

# Q 849 : # User.where("created_at > ?", 30.days.ago).count
Query(User)

# Q 850 : # User.where("created_at > ?", 7.days.ago).count
Query(User)

# Q 851 : # User.where("id = ? AND primary_group_id = ?", gu.user_id, gu.group_id)
Query(User)

# Q 852 : # User.where("last_seen_at > ?", 30.days.ago).count
Query(User)

# Q 853 : # User.where("last_seen_at > ?", 7.days.ago).count
Query(User)

# Q 854 : # User.where("length(username) < ?", value).pluck(:username).first
Query(User)
.select('username')
.return_limit('1')
# Q 855 : # User.where("length(username) > ?", value).pluck(:username).first
Query(User)
.select('username')
.return_limit('1')
# Q 856 : # User.where("trust_level >= ?", TrustLevel[2]).where(ip_address: ip_address.to_s).count
Query(User)
.where("ip_address = ?")
# Q 857 : # User.where("uploaded_avatar_id IS NOT NULL").find_each
Query(User)

# Q 858 : # User.where(active: false).where("created_at < ?", SiteSetting.purge_unactivated_users_grace_period_days.days.ago).where("NOT admin AND NOT moderator").where("NOT EXISTS
#               (SELECT 1 FROM topic_allowed_users tu JOIN topics t ON t.id = tu.topic_id AND t.user_id > 0 WHERE tu.user_id = users.id LIMIT 1)
#             ").limit(200).find_each
Query(User)
.where("active = ?")
.return_limit('')
# Q 859 : # User.where(active: true)
Query(User)
.where("active = ?")
# Q 860 : # User.where(admin: false, staged: false).find_by_email(invite.email)
Query(User)
.where("admin = ?")
.where("staged = ?")
.where("email = ?")
# Q 861 : # User.where(admin: true).human_users.joins(:user_auth_tokens).order("user_auth_tokens.created_at").pluck(:id).first
Query(User)
.where("admin = ?")
.joins('user_auth_tokens')
.order('id, created_at')
.select('id')
.return_limit('1')
# Q 862 : # User.where(admin: true).human_users.order("last_seen_at DESC")
Query(User)
.where("admin = ?")
.order('id, last_seen_at')
# Q 863 : # User.where(admin: true).where.not(id: id).human_users.blank?
Query(User)
.where("admin = ?")
.where("id = ?")
# Q 864 : # User.where(id: @user_ids).where.not(primary_group_id: nil).select(:id, :primary_group_id)
Query(User)
.where("id = ?")
.where("primary_group_id = ?")
.select('id')
.select('primary_group_id')
# Q 865 : # User.where(id: last_x_replies[0]).pluck(:username).first
Query(User)
.where("id = ?")
.select('username')
.return_limit('1')
# Q 866 : # User.where(id: params[:user_id].to_i)
Query(User)
.where("id = ?")
# Q 867 : # User.where(id: params[:user_ids].to_s.split(","))
Query(User)
.where("id = ?")
# Q 868 : # User.where(id: post_counts_by_user.keys).includes(:primary_group).each
Query(User)
.where("id = ?")
.includes('primary_group')
# Q 869 : # User.where(id: results.map(&:user_ids).flatten.uniq).each
Query(User)
.where("id = ?")
# Q 870 : # User.where(id: session_user_id.to_i).first
Query(User)
.where("id = ?")
.return_limit('1')
# Q 871 : # User.where(id: staff_user_ids).where(ip_address: ip_address.to_s).count
Query(User)
.where("id = ?")
.where("ip_address = ?")
# Q 872 : # User.where(id: user_id).where("moderator OR admin").exists?
Query(User)
.where("id = ?")
.return_limit('1')
# Q 873 : # User.where(id: user_id, admin: false, moderator: false).exists?
Query(User)
.where("id = ?")
.where("admin = ?")
.where("moderator = ?")
.return_limit('1')
# Q 874 : # User.where(id: user_id, admin: false, moderator: false).exists?
Query(User)
.where("id = ?")
.where("admin = ?")
.where("moderator = ?")
.return_limit('1')
# Q 875 : # User.where(id: user_ids)
Query(User)
.where("id = ?")
# Q 876 : # User.where(id: user_ids)
Query(User)
.where("id = ?")
# Q 877 : # User.where(id: user_ids).update_all(user_attributes)
Query(User)
.where("id = ?")
# Q 878 : # User.where(id: user_key.to_i).first
Query(User)
.where("id = ?")
.return_limit('1')
# Q 879 : # User.where(moderator: true, admin: false).human_users.order("last_seen_at DESC")
Query(User)
.where("moderator = ?")
.where("admin = ?")
.order('id, last_seen_at')
# Q 880 : # User.where(staged: false).where(username_lower: usernames).pluck(:username_lower)
Query(User)
.where("staged = ?")
.where("username_lower = ?")
.select('username_lower')
# Q 881 : # User.where(staged: false).where.not(id: post.user_id).joins(:topic_users).where.not(topic_users: { last_read_post_number: nil }).where("topic_users.topic_id = ? AND topic_users.last_read_post_number >= ?", post.topic_id, post.post_number)
Query(User)
.where("staged = ?")
.where("id = ?")
.joins('topic_users')
.where("last_read_post_number = ?")
# Q 882 : # User.where(staged: true).with_email(params[:email].strip.downcase).first
Query(User)
.where("staged = ?")
.return_limit('1')
# Q 883 : # User.where(username: additions + deletions).select("id,username").map { |u|
#   
#   [u.username, u.id]
# }.flatten
Query(User)
.where("username = ?")
.select('id,username')
# Q 884 : # User.where(username: group_params[:owner_usernames].split(",")).pluck(:id)
Query(User)
.where("username = ?")
.select('id')
# Q 885 : # User.where(username: group_params[:usernames].split(","))
Query(User)
.where("username = ?")
# Q 886 : # User.where(username: group_params[:usernames].split(",")).pluck(:id)
Query(User)
.where("username = ?")
.select('id')
# Q 887 : # User.where(username: names).find_each
Query(User)
.where("username = ?")
# Q 888 : # User.where(username: names).pluck(:id, :username).to_h
Query(User)
.where("username = ?")
.select('id')
.select('username')
# Q 889 : # User.where(username: num_clicks.keys).select(:id, :username, :uploaded_avatar_id).inject({ })
Query(User)
.where("username = ?")
.select('id')
.select('username')
.select('uploaded_avatar_id')
# Q 890 : # User.where(username: val).exists?
Query(User)
.where("username = ?")
.return_limit('1')
# Q 891 : # User.where(username_lower: auth_element[:content].strip).first
Query(User)
.where("username_lower = ?")
.return_limit('1')
# Q 892 : # User.where(username_lower: filters[key].downcase).pluck(:id)
Query(User)
.where("username_lower = ?")
.select('id')
# Q 893 : # User.where(username_lower: params[:username].downcase).pluck(:id).first
Query(User)
.where("username_lower = ?")
.select('id')
.return_limit('1')
# Q 894 : # User.where(username_lower: params[:username].to_s.downcase).pluck(:id).first
Query(User)
.where("username_lower = ?")
.select('id')
.return_limit('1')
# Q 895 : # User.where(username_lower: params[:usernames].split(",").map(&:downcase))
Query(User)
.where("username_lower = ?")
# Q 896 : # User.where(username_lower: params[filter]).pluck(:id)
Query(User)
.where("username_lower = ?")
.select('id')
# Q 897 : # User.where(username_lower: username.downcase).first
Query(User)
.where("username_lower = ?")
.return_limit('1')
# Q 898 : # User.with_email(Email.downcase(params[:from])).exists?
Query(User)
.return_limit('1')
# Q 899 : # User.with_email(email).where(staged: false).first
Query(User)
.where("staged = ?")
.return_limit('1')
# Q 900 : # User.with_email(params[:email]).admins.human_users.first
Query(User)
.return_limit('1')
# Q 901 : # User.with_email(u[:email]).first
Query(User)
.return_limit('1')
# Q 902 : # UserAction.joins(:target_topic, :target_post).merge(Topic.listable_topics.visible.secured(@guardian)).where(action_type: UserAction::WAS_LIKED).where(acting_user_id: @user.id).group(:user_id).order("COUNT(*) DESC").limit(MAX_SUMMARY_RESULTS).pluck("user_actions.user_id, COUNT(*)").each
Query(UserAction)
.joins('target_topic')
.joins('target_post')
.where("action_type = ?")
.where("acting_user_id = ?")
.group('user_id')
.order('id')
.return_limit('')
.select('user_actions.user_id, COUNT(*)')
# Q 903 : # UserAction.joins(:target_topic, :target_post).merge(Topic.listable_topics.visible.secured(@guardian)).where(user: @user).where(action_type: UserAction::WAS_LIKED).group(:acting_user_id).order("COUNT(*) DESC").limit(MAX_SUMMARY_RESULTS).pluck("acting_user_id, COUNT(*)").each
Query(UserAction)
.joins('target_topic')
.joins('target_post')
.where("user = ?")
.where("action_type = ?")
.group('acting_user_id')
.order('id')
.return_limit('')
.select('acting_user_id, COUNT(*)')
# Q 904 : # UserAction.where(action_type: UserAction::LIKE).count
Query(UserAction)
.where("action_type = ?")
# Q 905 : # UserAction.where(action_type: UserAction::LIKE).where("created_at > ?", 30.days.ago).count
Query(UserAction)
.where("action_type = ?")
# Q 906 : # UserAction.where(action_type: UserAction::LIKE).where("created_at > ?", 7.days.ago).count
Query(UserAction)
.where("action_type = ?")
# Q 907 : # UserAction.where(target_post_id: @post.id).each
Query(UserAction)
.where("target_post_id = ?")
# Q 908 : # UserAction.where(target_post_id: @post.id).where(user_id: prev_owner.id).where(action_type: USER_ACTIONS_TO_REMOVE).update_all(user_id: new_owner.id)
Query(UserAction)
.where("target_post_id = ?")
.where("user_id = ?")
.where("action_type = ?")
# Q 909 : # UserAction.where(target_post_id: @post.id).where(user_id: prev_owner.id).where(action_type: UserAction::WAS_LIKED).update_all(user_id: new_owner.id)
Query(UserAction)
.where("target_post_id = ?")
.where("user_id = ?")
.where("action_type = ?")
# Q 910 : # UserAction.where(target_topic_id: @post.topic_id).where(user_id: prev_owner.id).where(action_type: UserAction::NEW_TOPIC).update_all(user_id: new_owner.id)
Query(UserAction)
.where("target_topic_id = ?")
.where("user_id = ?")
.where("action_type = ?")
# Q 911 : # UserAction.where(user: @user).where(action_type: UserAction::BOOKMARK).count
Query(UserAction)
.where("user = ?")
.where("action_type = ?")
# Q 912 : # UserAction.where(user_id: @user.id, action_type: UserAction::LIKE).where("created_at > ?", time_period.days.ago).count
Query(UserAction)
.where("user_id = ?")
.where("action_type = ?")
# Q 913 : # UserAction.where(user_id: @user.id, action_type: UserAction::WAS_LIKED).where("created_at > ?", time_period.days.ago)
Query(UserAction)
.where("user_id = ?")
.where("action_type = ?")
# Q 914 : # UserAction.where(user_id: id, action_type: UserAction::LIKE).count
Query(UserAction)
.where("user_id = ?")
.where("action_type = ?")
# Q 915 : # UserAction.where(user_id: id, action_type: UserAction::WAS_LIKED).count
Query(UserAction)
.where("user_id = ?")
.where("action_type = ?")
# Q 916 : # UserAction.where(user_id: user_id, target_topic_id: topic_id, action_type: [RESPONSE, MENTION, QUOTE]).order("created_at DESC").pluck(:target_post_id).first
Query(UserAction)
.where("user_id = ?")
.where("target_topic_id = ?")
.where("action_type = ?")
.order('id, created_at')
.select('target_post_id')
.return_limit('1')
# Q 917 : # UserApiKey.find(params[:id])
Query(UserApiKey)
.where("id = ?")
# Q 918 : # UserApiKey.find_by(key: current_key)
Query(UserApiKey)
.where("key = ?")
# Q 919 : # UserApiKey.where(client_id: client_id, user_id: api_key.user_id).where("id <> ?", api_key.id).destroy_all
Query(UserApiKey)
.where("client_id = ?")
.where("user_id = ?")
# Q 920 : # UserApiKey.where(key: user_api_key, revoked_at: nil).includes(:user).first
Query(UserApiKey)
.where("key = ?")
.where("revoked_at = ?")
.includes('user')
.return_limit('1')
# Q 921 : # UserApiKey.where(user_id: user.id, client_id: params[:client_id]).destroy_all
Query(UserApiKey)
.where("user_id = ?")
.where("client_id = ?")
# Q 922 : # UserArchivedMessage.where(topic_id: @topic.id).pluck(:user_id).each
Query(UserArchivedMessage)
.where("topic_id = ?")
.select('user_id')
# Q 923 : # UserArchivedMessage.where(user_id: user_id, topic_id: topic_id).destroy_all
Query(UserArchivedMessage)
.where("user_id = ?")
.where("topic_id = ?")
# Q 924 : # UserArchivedMessage.where(user_id: user_id, topic_id: topic_id).destroy_all
Query(UserArchivedMessage)
.where("user_id = ?")
.where("topic_id = ?")
# Q 925 : # UserAssociatedAccount.find_by(provider_name: name, user_id: user.id)
Query(UserAssociatedAccount)
.where("provider_name = ?")
.where("user_id = ?")
# Q 926 : # UserAssociatedAccount.find_by(provider_name: name, user_id: user.id)
Query(UserAssociatedAccount)
.where("provider_name = ?")
.where("user_id = ?")
# Q 927 : # UserAssociatedAccount.where(user: user, provider_name: auth_token[:provider]).destroy_all
Query(UserAssociatedAccount)
.where("user = ?")
.where("provider_name = ?")
# Q 928 : # UserAuthToken.find_by(id: params[:token_id], user_id: user.id)
Query(UserAuthToken)
.where("id = ?")
.where("user_id = ?")
# Q 929 : # UserAuthToken.where("rotated_at < ?", 1.minute.ago).where(id: user_token.id, prev_auth_token: token).update_all(auth_token_seen: false)
Query(UserAuthToken)
.where("id = ?")
.where("prev_auth_token = ?")
# Q 930 : # UserAuthToken.where(id: params[:token_id], user_id: user.id).each(&:destroy!)
Query(UserAuthToken)
.where("id = ?")
.where("user_id = ?")
# Q 931 : # UserAuthToken.where(id: user_token.id, auth_token: token).update_all(auth_token_seen: true, seen_at: Time.zone.now)
Query(UserAuthToken)
.where("id = ?")
.where("auth_token = ?")
# Q 932 : # UserAuthToken.where(user_id: id).destroy_all
Query(UserAuthToken)
.where("user_id = ?")
# Q 933 : # UserAuthToken.where(user_id: user.id).each(&:destroy!)
Query(UserAuthToken)
.where("user_id = ?")
# Q 934 : # UserAuthTokenLog.where(user_id: user_id).pluck(:client_ip)
Query(UserAuthTokenLog)
.where("user_id = ?")
.select('client_ip')
# Q 935 : # UserBadge.find(params[:id])
Query(UserBadge)
.where("id = ?")
# Q 936 : # UserBadge.find_by(id: params[:user_badge_id])
Query(UserBadge)
.where("id = ?")
# Q 937 : # UserBadge.find_by(user_id: user.id, badge_id: @badge.id)
Query(UserBadge)
.where("user_id = ?")
.where("badge_id = ?")
# Q 938 : # UserBadge.where(badge_id: id).count
Query(UserBadge)
.where("badge_id = ?")
# Q 939 : # UserCustomField.where(name: "import_id", value: "#{
# external_user_id}#{
# import_source}").first
Query(UserCustomField)
.where("name = ?")
.where("value = ?")
.return_limit('1')
# Q 940 : # UserExport.where("created_at < ?", DESTROY_CREATED_BEFORE).find_each
Query(UserExport)

# Q 941 : # UserExport.where(user_id: @user.id, created_at: (
# Time.zone.now.beginning_of_day..Time.zone.now.end_of_day)).count
Query(UserExport)
.where("user_id = ?")
.where("created_at = ?")
# Q 942 : # UserField.all.includes(:user_field_options).order(:position)
Query(UserField)
.includes('user_field_options')
.order('position')
# Q 943 : # UserField.pluck(:id)
Query(UserField)
.select('id')
# Q 944 : # UserField.pluck(:id)
Query(UserField)
.select('id')
# Q 945 : # UserField.where("show_on_profile OR show_on_user_card").pluck(:id)
Query(UserField)
.select('id')
# Q 946 : # UserField.where(id: params.require(:id)).first
Query(UserField)
.where("id = ?")
.return_limit('1')
# Q 947 : # UserField.where(id: params.require(:id)).first
Query(UserField)
.where("id = ?")
.return_limit('1')
# Q 948 : # UserFieldOption.where(user_field_id: field.id).delete_all
Query(UserFieldOption)
.where("user_field_id = ?")
# Q 949 : # UserHistory.find(params[:id])
Query(UserHistory)
.where("id = ?")
# Q 950 : # UserHistory.for(self, :auto_trust_level_change).where("created_at >= ?", SiteSetting.tl3_promotion_min_duration.to_i.days.ago).where(previous_value: TrustLevel[2].to_s).where(new_value: TrustLevel[3].to_s).exists?
Query(UserHistory)
.where("previous_value = ?")
.where("new_value = ?")
.return_limit('1')
# Q 951 : # UserHistory.for(self, :silence_user).order("id DESC").first
Query(UserHistory)
.order('id')
.return_limit('1')
# Q 952 : # UserHistory.for(self, :suspend_user).order("id DESC").first
Query(UserHistory)
.order('id')
.return_limit('1')
# Q 953 : # UserHistory.where(acting_user_id: @user.id, action: UserHistory.actions[:check_personal_message], topic_id: @topic.id).where("created_at > ?", 1.hour.ago).exists?
Query(UserHistory)
.where("acting_user_id = ?")
.where("action = ?")
.where("topic_id = ?")
.return_limit('1')
# Q 954 : # UserHistory.where(action: UserHistory.actions[:wizard_step], context: steps).distinct.order(:context).pluck(:context)
Query(UserHistory)
.where("action = ?")
.where("context = ?")
.distinct('')
.order('context')
.select('context')
# Q 955 : # UserHistory.where(action: UserHistory.actions[:wizard_step], context: steps_with_fields.map(&:id)).uniq.pluck(:context)
Query(UserHistory)
.where("action = ?")
.where("context = ?")
.distinct('')
.select('context')
# Q 956 : # UserHistory.where(context: id.to_s, action: UserHistory.actions[:disable_theme_component]).order("created_at DESC").first
Query(UserHistory)
.where("context = ?")
.where("action = ?")
.order('id, created_at')
.return_limit('1')
# Q 957 : # UserOpenId.find_by_url(identity_url)
Query(UserOpenId)
.where("url = ?")
# Q 958 : # UserOpenId.where("url LIKE ?", "#{
# @identifier}%").find_by(user_id: user.id)
Query(UserOpenId)
.where("user_id = ?")
# Q 959 : # UserOpenId.where("url LIKE ?", "#{
# @identifier}%").find_by(user_id: user.id)
Query(UserOpenId)
.where("user_id = ?")
# Q 960 : # UserOption.where(user_id: user_id).pluck(:auto_track_topics_after_msecs).first
Query(UserOption)
.where("user_id = ?")
.select('auto_track_topics_after_msecs')
.return_limit('1')
# Q 961 : # UserProfile.find(user_profile_id).increment!(:views)
Query(UserProfile)
.where("id = ?")
# Q 962 : # UserProfile.where("bio_cooked_version IS NULL OR bio_cooked_version < ?", BAKED_VERSION).limit(limit).each
Query(UserProfile)
.return_limit('')
# Q 963 : # UserProfile.where("dismissed_banner_key IS NOT NULL").update_all(dismissed_banner_key: nil)
Query(UserProfile)

# Q 964 : # UserSearch.new(params[:name], include_staged_users: true).search.pluck(:id)
Query(UserSearch)
.select('id')
# Q 965 : # UserSecurityKey.find_by(credential_id: @params[:credentialId])
Query(UserSecurityKey)
.where("credential_id = ?")
# Q 966 : # UserStat.where("reset_bounce_score_after < now()").where("bounce_score > 0").update_all(bounce_score: 0)
Query(UserStat)

# Q 967 : # UserStat.where(user_id: args[:user_id])
Query(UserStat)
.where("user_id = ?")
# Q 968 : # UserStat.where(user_id: id).update_all(update_args)
Query(UserStat)
.where("user_id = ?")
# Q 969 : # UserStat.where(user_id: user_id)
Query(UserStat)
.where("user_id = ?")
# Q 970 : # UserStat.where(user_id: user_id).first
Query(UserStat)
.where("user_id = ?")
.return_limit('1')
# Q 971 : # UserStat.where(user_id: user_id).first
Query(UserStat)
.where("user_id = ?")
.return_limit('1')
# Q 972 : # UserStat.where(user_id: user_id).update_all("likes_given = likes_given + #{
# delta.to_i}")
Query(UserStat)
.where("user_id = ?")
# Q 973 : # UserStat.where(user_id: user_id).update_all("likes_received = likes_received + #{
# delta.to_i}")
Query(UserStat)
.where("user_id = ?")
# Q 974 : # UserVisit.where(user_id: id, visited_at: Time.zone.now.to_date).update_all(update_args)
Query(UserVisit)
.where("user_id = ?")
.where("visited_at = ?")
# Q 975 : # WatchedWord.find_by(id: params[:id])
Query(WatchedWord)
.where("id = ?")
# Q 976 : # WatchedWord.where("word ILIKE ?", new_word).first
Query(WatchedWord)
.return_limit('1')
# Q 977 : # WatchedWord.where(action: action).delete_all
Query(WatchedWord)
.where("action = ?")
# Q 978 : # WatchedWord.where(action: action).pluck(:word).join("\n")
Query(WatchedWord)
.where("action = ?")
.select('word')
# Q 979 : # WebHook.enqueue_hooks(:post, event, id: post.id, category_id: post.topic.category_id, tag_ids: post.topic.tags.pluck(:id), payload: payload)
Query(WebHook)

# Q 980 : # WebHook.enqueue_hooks(:topic, event, id: topic.id, category_id: topic.category_id, tag_ids: topic.tags.pluck(:id), payload: payload)
Query(WebHook)

# Q 981 : # WebHook.find(params[:id])
Query(WebHook)
.where("id = ?")
# Q 982 : # WebHook.limit(limit).offset(offset).includes(:web_hook_event_types).includes(:categories).includes(:groups)
Query(WebHook)
.return_limit('')
.includes('web_hook_event_types')
.includes('categories')
.includes('groups')
# Q 983 : # WebHook.where(active: true).joins(:web_hook_event_types).where("web_hooks.wildcard_web_hook = ? OR web_hook_event_types.name = ?", true, type.to_s).distinct
Query(WebHook)
.where("active = ?")
.joins('web_hook_event_types')
.distinct('')
# Q 984 : # WebHookEvent.find(params[:event_id])
Query(WebHookEvent)
.where("id = ?")
# Q 985 : # WebHookEventType.find(WebHookEventType::POST)
Query(WebHookEventType)
.where("id = ?")
# Q 986 : # badge.user_badges.order("granted_at DESC, id DESC").limit(96)
Query(UserBadge)
.where("badge_id = ?")
.order('id, granted_at')
.return_limit('')
# Q 987 : # badge.user_badges.where(user_id: user_id).count
Query(UserBadge)
.where("badge_id = ?")
.where("user_id = ?")
# Q 988 : # badge_groupings.find
Query(BadgeGrouping)
.where("id = ?")
# Q 989 : # badges.includes(:badge_grouping).includes(:badge_type).where(enabled: true, listable: true)
Query(Badge)
.includes('badge_grouping')
.includes('badge_type')
.where("enabled = ?")
.where("listable = ?")
# Q 990 : # badges.where("name ILIKE ?", "%#{
# search}%")
Query(Badge)

# Q 991 : # badges.where(allow_title: true).limit(1).pluck(:name).first
Query(Badge)
.where("allow_title = ?")
.return_limit('')
.select('name')
.return_limit('1')
# Q 992 : # badges.where(allow_title: true, name: title).exists?
Query(Badge)
.where("allow_title = ?")
.where("name = ?")
.return_limit('1')
# Q 993 : # categories.left_outer_joins(:featured_topics).where(topics: { category_id: allowed_category_ids }).group("categories.id").order("max(topics.bumped_at) DESC NULLS LAST").order("categories.id ASC")
Query(Category)
.left_outer_joins('featured_topics')
.where("category_id = ?")
.group('id')
.order('id, bumped_at')
.order('id')
# Q 994 : # categories.map do |params|
#   
#   category = find_category(params[:site_setting_name])
#   next unless category
#   { id: params[:site_setting_name], name: category.name, selected: unchanged?(category) }
#   
# end.compact
Query(Category)
.where("id != 0")
# Q 995 : # categories.order(:position, :id)
Query(Category)
.order('position, id')
# Q 996 : # categories.pluck(:id)
Query(Category)
.select('id')
# Q 997 : # categories.select { |c|
#   
#   c.suppress_from_latest == true
# }.map(&:id)
Query(Category)

# Q 998 : # category.subcategories.pluck(:id)
Query(Category)
.where("category_id = ?")
.select('id')
# Q 999 : # category.tag_groups.count
Query(TagGroup)
.where("category_id = ?")
# Q 1000 : # category.tag_groups.count
Query(TagGroup)
.where("category_id = ?")
# Q 1001 : # category.topic.first_post
Query(Topic)
.where("id = ?")
# Q 1002 : # category.topic.first_post
Query(Topic)
.where("id = ?")
# Q 1003 : # category.topic.ordered_posts.first
Query(Topic)
.where("id = ?")
.return_limit('1')
# Q 1004 : # category.topics.where.not(id: category.topic_id).order("created_at ASC").limit(1).first
Query(Topic)
.where("category_id = ?")
.where("id = ?")
.order('id, created_at')
.return_limit('')
.return_limit('1')
# Q 1005 : # category_group.group.present?
Query(Group)
.where("id = ?")
# Q 1006 : # category_groups.includes(:group).each
Query(CategoryGroup)
.includes('group')
# Q 1007 : # user.groups.pluck(:id)
Query(Group)
.where("user_id = ?")
.select('id')
# Q 1008 : # user.security_keys.find_by(id: params[:id].to_i)
Query(UserSecurityKey)
.where("user_id = ?")
.where("id = ?")
# Q 1009 : # user.security_keys.where(factor_type: UserSecurityKey.factor_types[:second_factor]).order(:created_at)
Query(UserSecurityKey)
.where("user_id = ?")
.where("factor_type = ?")
.order('created_at')
# Q 1010 : # user.totps.select(:id, :name, :last_used, :created_at, :method).where(enabled: true).order(:created_at)
Query(UserSecondFactor)
.where("user_id = ?")
.select('id')
.select('name')
.select('last_used')
.select('created_at')
.select('method')
.where("enabled = ?")
.order('created_at')
# Q 1011 : # user.user_second_factors.totps.find_by(id: second_factor_id)
Query(UserSecondFactor)
.where("user_id = ?")
.where("id = ?")
# Q 1012 : # email_logs.where("post_reply_keys.reply_key = ?", reply_key)
Query(EmailLog)

# Q 1013 : # email_logs.where("replace(post_reply_keys.reply_key::VARCHAR, '-', '') ILIKE ?", "%#{
# reply_key}%")
Query(EmailLog)

# Q 1014 : # email_tokens.where("not expired").update_all(expired: true)
Query(EmailToken)

# Q 1015 : # email_tokens.where(email: email, confirmed: true).present?
Query(EmailToken)
.where("email = ?")
.where("confirmed = ?")
# Q 1016 : # find_all_invites_from(inviter, offset).where("invites.user_id IS NOT NULL").order("invites.redeemed_at DESC")
Query(UserExists)
.order('id, redeemed_at')
# Q 1017 : # find_all_invites_from(inviter, offset).where("invites.user_id IS NULL").order("invites.created_at DESC")
Query(UserExists)
.order('id, created_at')
# Q 1018 : # find_by("? <<= ip_address", ip_address.to_s)
Query(ScreenedIpAddress)

# Q 1019 : # find_by(id: id)
Query(Group)
.where("id = ?")
# Q 1020 : # find_by(id: scheme_id)
Query(ColorScheme)
.where("id = ?")
# Q 1021 : # find_by(name: name, https: https, port: port)
Query(IncomingDomain)
.where("name = ?")
.where("https = ?")
.where("port = ?")
# Q 1022 : # find_by(name: name, https: https, port: port)
Query(IncomingDomain)
.where("name = ?")
.where("https = ?")
.where("port = ?")
# Q 1023 : # find_by(path: path, incoming_domain_id: domain_id)
Query(IncomingReferer)
.where("path = ?")
.where("incoming_domain_id = ?")
# Q 1024 : # find_by(path: path, incoming_domain_id: domain_id)
Query(IncomingReferer)
.where("path = ?")
.where("incoming_domain_id = ?")
# Q 1025 : # find_by(target: target)
Query(InvalidAction)
.where("target = ?")
# Q 1026 : # find_by(upload_id: upload.id, width: width, height: height)
Query(OptimizedImage)
.where("upload_id = ?")
.where("width = ?")
.where("height = ?")
# Q 1027 : # find_by(upload_id: upload.id, width: width, height: height)
Query(OptimizedImage)
.where("upload_id = ?")
.where("width = ?")
.where("height = ?")
# Q 1028 : # find_by(url: normalize_url(url))
Query(Normalizer)
.where("url = ?")
# Q 1029 : # find_by(user_id: user, draft_key: key)
Query(Draft)
.where("user_id = ?")
.where("draft_key = ?")
# Q 1030 : # find_by(user_id: user.id, draft_key: key)
Query(Draft)
.where("user_id = ?")
.where("draft_key = ?")
# Q 1031 : # find_by_theme_ids(theme_ids).filter_locale_fields(locale_codes).reorder("X.theme_sort_column", "Y.locale_sort_column").select("DISTINCT ON (X.theme_sort_column) *")
Query(ThemeField)
.where("theme_ids = ?")
.order('id, theme_sort_column, locale_sort_column')
.select('DISTINCT ON (X.theme_sort_column) *')
# Q 1032 : # group.find_by("lower(name) = ?", name.downcase)
Query(Group)

# Q 1033 : # group.group_users.build(user_id: user_id)
Query(GroupUser)
.where("group_id = ?")
# Q 1034 : # group.group_users.build(user_id: user_id, owner: true)
Query(GroupUser)
.where("group_id = ?")
# Q 1035 : # group.group_users.create!(user_id: user_id)
Query(GroupUser)
.where("group_id = ?")
# Q 1036 : # group.group_users.where(user_id: @user.id).exists?
Query(GroupUser)
.where("group_id = ?")
.where("user_id = ?")
.return_limit('1')
# Q 1037 : # group.group_users.where(user_id: user.id).update_all(owner: false)
Query(GroupUser)
.where("group_id = ?")
.where("user_id = ?")
# Q 1038 : # group.group_users.where(user_id: user.id).update_all(owner: true)
Query(GroupUser)
.where("group_id = ?")
.where("user_id = ?")
# Q 1039 : # group.requesters
Query(Group)
.where("group_id = ?")
# Q 1040 : # group.users.human_users
Query(User)
.where("group_id = ?")
# Q 1041 : # group.users.include?(@user)
Query(User)
.where("group_id = ?")
# Q 1042 : # group.users.include?(self)
Query(User)
.where("group_id = ?")
# Q 1043 : # group.users.include?(user)
Query(User)
.where("group_id = ?")
# Q 1044 : # group.users.include?(user)
Query(User)
.where("group_id = ?")
# Q 1045 : # group.users.pluck(:id)
Query(User)
.where("group_id = ?")
.select('id')
# Q 1046 : # group.users.pluck(:id)
Query(User)
.where("group_id = ?")
.select('id')
# Q 1047 : # group.users.where("group_users.owner").include?(user)
Query(User)
.where("group_id = ?")
# Q 1048 : # group_users.pluck(:group_id)
Query(GroupUser)
.select('group_id')
# Q 1049 : # group_users.pluck(:user_id, :notification_level).each
Query(GroupUser)
.select('user_id')
.select('notification_level')
# Q 1050 : # group_users.where("group_id IN (?)", ids).pluck(:group_id)
Query(GroupUser)
.select('group_id')
# Q 1051 : # group_users.where("group_id NOT IN (?)", ids)
Query(GroupUser)

# Q 1052 : # group_users.where(owner: true).pluck(:group_id)
Query(GroupUser)
.where("owner = ?")
.select('group_id')
# Q 1053 : # groups.joins("LEFT JOIN group_users gu ON gu.group_id = groups.id
#     ").where("gu.user_id = ?", user.id)
Query(Group)

# Q 1054 : # groups.map(&:id)
Query(Group)

# Q 1055 : # groups.map(&:id)
Query(Group)

# Q 1056 : # groups.map(&:id)
Query(Group)

# Q 1057 : # groups.order("groups.name asc")
Query(Group)
.order('id, name')
# Q 1058 : # groups.pluck("groups.id")
Query(Group)
.select('groups.id')
# Q 1059 : # groups.visible_groups(self)
Query(Group)

# Q 1060 : # groups.where("automatic IS FALSE OR groups.id = #{
# Group::AUTO_GROUPS[:moderators]}")
Query(Group)

# Q 1061 : # groups.where("groups.title <> ''")
Query(Group)

# Q 1062 : # groups.where(automatic: false)
Query(Group)
.where("automatic = ?")
# Q 1063 : # groups.where(automatic: false)
Query(Group)
.where("automatic = ?")
# Q 1064 : # groups.where(visibility_level: Group.visibility_levels[:public])
Query(Group)
.where("visibility_level = ?")
# Q 1065 : # human_users.where("NOT EXISTS(
#                      SELECT 1
#                      FROM anonymous_users a
#                      WHERE a.user_id = users.id
#                   )")
Query(User)

# Q 1066 : # includes(:post_details).find_by(post_details: { key: key, value: value })
Query(Post)
.includes('post_details')
.where("key = ?")
# Q 1067 : # includes(:user)
Query(Post)
.includes('user')
# Q 1068 : # incoming_emails.includes(:user, post: :topic).order(created_at: :desc).offset(params[:offset] || 0).limit(50)
Query(IncomingEmail)
.includes('user')
.includes('topic')
.order('created_at')
.return_limit('')
# Q 1069 : # incoming_emails.where("error ILIKE ?", "%#{
# params[:error]}%")
Query(IncomingEmail)

# Q 1070 : # incoming_emails.where("from_address ILIKE ?", "%#{
# params[:from]}%")
Query(IncomingEmail)

# Q 1071 : # incoming_emails.where("subject ILIKE ?", "%#{
# params[:subject]}%")
Query(IncomingEmail)

# Q 1072 : # invite.groups.pluck(:id)
Query(Invite)
.select('id')
# Q 1073 : # invite.invited_groups.pluck(:group_id)
Query(Invite)
.select('group_id')
# Q 1074 : # invite.topic_invites.pluck(:topic_id).include?(topic.id)
Query(Invite)
.select('topic_id')
# Q 1075 : # invites.where("redeemed_at is not null").includes(:invited_by).first
Query(Invite)
.return_limit('1')
# Q 1076 : # joins("JOIN themes ON themes.remote_theme_id = remote_themes.id").where.not(remote_url: "")
Query(ImportError)
.where("remote_url = ?")
# Q 1077 : # joins("LEFT JOIN topics ON notifications.topic_id = topics.id").where("topics.id IS NULL OR topics.deleted_at IS NULL")
Query(Notification)

# Q 1078 : # joins(:topic).where("topics.archetype <> ?", Archetype.private_message)
Query(Post)
.joins('topic')
# Q 1079 : # joins(:topic).where("topics.archetype = ?", Archetype.private_message)
Query(Post)
.joins('topic')
# Q 1080 : # joins(:topic).where("topics.subtype = ?", subtype)
Query(Post)
.joins('topic')
# Q 1081 : # joins(:topic).where("topics.visible = true").where(hidden: false)
Query(Post)
.joins('topic')
.where("hidden = ?")
# Q 1082 : # joins(:user_emails).where("lower(user_emails.email) IN (?)", email)
Query(User)
.joins('user_emails')
# Q 1083 : # notifications.uniq(&:id).sort do |x, y|
#   
#   if x.unread_pm? && !y.unread_pm?
#     
#     -1
#   elsif y.unread_pm? && !x.unread_pm?
#     
#     1
#   else
#     
#     y.created_at <=> x.created_at
#   end
#   
# end.take(count)
Query(Notification)
.distinct('')
.return_limit('1')
# Q 1084 : # notifications.visible.order("notifications.created_at desc").first
Query(Notification)
.order('id, created_at')
.return_limit('1')
# Q 1085 : # notifications.where("notification_type <> ?", notification_type)
Query(Notification)

# Q 1086 : # notifications.where("read = false").count
Query(Notification)

# Q 1087 : # optimized_images.find_by(width: width, height: height)
Query(OptimizedImage)
.where("width = ?")
.where("height = ?")
# Q 1088 : # order("action ASC, word ASC")
Query(WatchedWord)
.order('id, action, word')
# Q 1089 : # order("created_at DESC, id DESC")
Query(Post)
.order('id, created_at')
# Q 1090 : # order("notifications.created_at desc").limit(n)
Query(Notification)
.order('id, created_at')
.return_limit('')
# Q 1091 : # order("position asc")
Query(PostActionType)
.order('id, position')
# Q 1092 : # order("post_number ASC")
Query(Post)
.order('id, post_number')
# Q 1093 : # order("sort_order desc, post_number desc")
Query(Post)
.order('id, sort_order, post_number')
# Q 1094 : # order("topic_count DESC")
Query(Category)
.order('id, topic_count')
# Q 1095 : # order("topics.bumped_at desc")
Query(UserExists)
.order('id, bumped_at')
# Q 1096 : # order("topics.created_at desc, topics.id desc")
Query(UserExists)
.order('id, created_at')
# Q 1097 : # order(:sort_order, :post_number)
Query(Post)
.order('sort_order, post_number')
# Q 1098 : # post.errors.add(:base, I18n.t(limit_translation_key, count: max_count))
Query(Post)

# Q 1099 : # post.post_actions.where(post_action_type_id: post_action_type_id).includes(:user).offset(page * page_size).order("post_actions.created_at asc").limit(page_size)
Query(PostAction)
.where("post_id = ?")
.where("post_action_type_id = ?")
.includes('user')
.order('id, created_at')
.return_limit('')
# Q 1100 : # post.topic.category.secure_group_ids
Query(Topic)
.where("id = ?")
# Q 1101 : # post.topic.invite_group(@user, Group[:admins])
Query(Topic)
.where("id = ?")
# Q 1102 : # post_actions.where(post_action_type_id: PostActionType.flag_types_without_custom.values, deleted_at: nil).count
Query(PostAction)
.where("post_action_type_id = ?")
.where("deleted_at = ?")
# Q 1103 : # post_actions.where(user_id: user.id)
Query(PostAction)
.where("user_id = ?")
# Q 1104 : # posts.dup.pluck(:topic_id)
Query(Post)
.select('topic_id')
# Q 1105 : # posts.have_uploads.find_each
Query(Post)

# Q 1106 : # posts.includes(:post_actions).where("post_actions.post_action_type_id" => PostActionType.flag_types_without_custom.values).count
Query(Post)
.includes('post_actions')
.where("post_action_type_id" = ?")
# Q 1107 : # posts.joins("JOIN users u ON u.id = posts.user_id")
Query(Post)

# Q 1108 : # posts.order("#{
# data_ranking} DESC")
Query(Post)
.order('id')
# Q 1109 : # posts.order("MAX(#{
# data_ranking}) DESC")
Query(Post)
.order('id')
# Q 1110 : # posts.order("MAX(posts.created_at) DESC")
Query(Post)
.order('id, created_at')
# Q 1111 : # posts.order("MAX(posts.like_count) DESC")
Query(Post)
.order('id, like_count')
# Q 1112 : # posts.order("MAX(topics.created_at) DESC")
Query(Post)
.order('id, created_at')
# Q 1113 : # posts.order("MAX(topics.views) DESC")
Query(Post)
.order('id, views')
# Q 1114 : # posts.order("post_number desc").limit(batch_size).each
Query(Post)
.order('id, post_number')
.return_limit('')
# Q 1115 : # posts.order("post_number desc").where("not hidden AND posts.deleted_at IS NULL").first
Query(Post)
.order('id, post_number')
.return_limit('1')
# Q 1116 : # posts.order("posts.like_count DESC")
Query(Post)
.order('id, like_count')
# Q 1117 : # posts.order("topics.bumped_at DESC")
Query(Post)
.order('id, bumped_at')
# Q 1118 : # posts.order("topics.created_at DESC")
Query(Post)
.order('id, created_at')
# Q 1119 : # posts.order("topics.views DESC")
Query(Post)
.order('id, views')
# Q 1120 : # posts.order(:post_number).pluck(:post_number)
Query(Post)
.order('post_number')
.select('post_number')
# Q 1121 : # posts.pluck(:id)
Query(Post)
.select('id')
# Q 1122 : # posts.pluck(:id)
Query(Post)
.select('id')
# Q 1123 : # posts.pluck(:user_id).uniq.map { |user_id|
#   
#   [user_id, TopicUser.notification_levels[:watching]]
# }.to_h
Query(Post)
.select('user_id')
.distinct('')
# Q 1124 : # posts.reorder("posts.created_at DESC")
Query(Post)
.order('id, created_at')
# Q 1125 : # posts.select(:sort_order).first.try(:sort_order)
Query(Post)
.select('sort_order')
.return_limit('1')
.select('sort_order')
# Q 1126 : # posts.where("(categories.id IS NULL) OR (NOT categories.read_restricted) OR (categories.id IN (?))", secure_category_ids).references(:categories)
Query(Post)

# Q 1127 : # posts.where("(categories.id IS NULL) OR (NOT categories.read_restricted)").references(:categories)
Query(Post)

# Q 1128 : # posts.where("categories.id NOT IN (
#       SELECT categories.id WHERE categories.search_priority = ?
# )
# ", Searchable::PRIORITIES[:ignore])
Query(Post)

# Q 1129 : # posts.where("post_search_data.search_data @@ #{
# ts_query(weight_filter: weights)}")
Query(Post)

# Q 1130 : # posts.where("posts.raw  || ' ' || u.username || ' ' || COALESCE(u.name, '') ilike ?", "%#{
# term_without_quote}%")
Query(Post)

# Q 1131 : # posts.where("posts.user_id = #{
# @search_context.id}")
Query(Post)

# Q 1132 : # posts.where("posts.user_id = ? OR post_type IN (?)", @user.id, visible_types)
Query(Post)

# Q 1133 : # posts.where("topics.archetype <> ?", Archetype.private_message)
Query(Post)

# Q 1134 : # posts.where("topics.archetype =  ?", Archetype.private_message)
Query(Post)

# Q 1135 : # posts.where("topics.category_id in (?)", category_ids)
Query(Post)

# Q 1136 : # posts.where("topics.id #{
# modifier} IN (
#         SELECT DISTINCT(tt.topic_id)
#         FROM topic_tags tt, tags
#         WHERE tt.tag_id = tags.id AND lower(tags.name) IN (?)
#       )", tags)
Query(Post)

# Q 1137 : # posts.where("topics.id #{
# modifier} IN (
#         SELECT tt.topic_id
#         FROM topic_tags tt, tags
#         WHERE tt.tag_id = tags.id
#         GROUP BY tt.topic_id
#         HAVING to_tsvector(#{
# default_ts_config}, array_to_string(array_agg(lower(tags.name)), ' ')) @@ to_tsquery(#{
# default_ts_config}, ?)
#       )", tags.join("&"))
Query(Post)

# Q 1138 : # posts.where("topics.id = #{
# @search_context.id}").order("posts.post_number #{
# @order == :latest ? "DESC" : ""}")
Query(Post)
.order('id, post_number')
# Q 1139 : # posts.where("topics.visible")
Query(Post)

# Q 1140 : # posts.where(post_type: Post.types[:regular], user_deleted: false).order("score desc nulls last").limit(1).first
Query(Post)
.where("post_type = ?")
.where("user_deleted = ?")
.order('id, score')
.return_limit('')
.return_limit('1')
# Q 1141 : # posts.where(post_type: visible_types)
Query(Post)
.where("post_type = ?")
# Q 1142 : # posts.where(topic_id: topics.pluck(:id))
Query(Post)
.where("topic_id = ?")
# Q 1143 : # public_incoming_links(category_id: category_id).where("incoming_links.created_at > ? AND incoming_links.created_at < ? AND incoming_links.user_id IS NOT NULL", start_date, end_date).joins(:user).group("users.username")
Query(IncomingLinksReport)
.group('username, id')
# Q 1144 : # public_incoming_links(category_id: options[:category_id]).joins(incoming_referer: :incoming_domain).where("incoming_links.created_at > ? AND incoming_domains.name IN (?)", 30.days.ago, domains).group("incoming_domains.name")
Query(IncomingLinksReport)
.group('name, id')
# Q 1145 : # reviewable.reviewable_scores.order("created_at DESC").first
Query(Reviewable)
.order('id, created_at')
.return_limit('1')
# Q 1146 : # reviewable_histories.order(:created_at)
Query(ReviewableHistory)
.order('created_at')
# Q 1147 : # reviewables.map(&:target_id).uniq
Query(Reviewable)
.distinct('')
# Q 1148 : # reviewables.where("status <> ?", Reviewable.statuses[:pending])
Query(Reviewable)

# Q 1149 : # reviewables.where(target_created_by_id: opts[:user_id])
Query(Reviewable)
.where("target_created_by_id = ?")
# Q 1150 : # reviewables.where(topic_id: opts[:topic_id])
Query(Reviewable)
.where("topic_id = ?")
# Q 1151 : # screened_ip_addresses.limit(200).order("match_count desc")
Query(ScreenedIpAddress)
.return_limit('')
.order('id, match_count')
# Q 1152 : # screened_ip_addresses.where("cidr :filter >>= ip_address", filter: filter)
Query(ScreenedIpAddress)

# Q 1153 : # select(:user_id).distinct.where(action_type: [LIKE, NEW_TOPIC, REPLY, NEW_PRIVATE_MESSAGE])
Query(UserAction)
.select('user_id')
.distinct('')
.where("action_type = ?")
# Q 1154 : # self.class.where("lower(email) = ?", email).exists?
Query(UserEmail)
.return_limit('1')
# Q 1155 : # self.errors.add(:base, I18n.t("category.errors.email_already_used_in_group", email: escaped, group_name: Rack::Utils.escape_html(group.name)))
Query(Category)

# Q 1156 : # self.errors.add(:base, I18n.t("groups.errors.cant_allow_membership_requests"))
Query(Group)

# Q 1157 : # self.errors.add(:base, I18n.t("groups.errors.email_already_used_in_category", email: escaped, category_name: Rack::Utils.escape_html(category.name)))
Query(Group)

# Q 1158 : # self.errors.add(:base, I18n.t("groups.errors.email_already_used_in_group", email: escaped, group_name: Rack::Utils.escape_html(group.name)))
Query(Group)

# Q 1159 : # self.errors.add(:base, I18n.t("groups.errors.grant_trust_level_not_valid", trust_level: self.grant_trust_level))
Query(Group)

# Q 1160 : # self.errors.add(:base, I18n.t("groups.errors.invalid_incoming_email", email: escaped))
Query(Group)

# Q 1161 : # self.errors.add(:email, :taken)
Query(UserEmail)

# Q 1162 : # self.find_by("lower(name) = ?", name.downcase)
Query(Tag)

# Q 1163 : # self.find_by(reply_key: reply_key)
Query(EmailLog)
.where("reply_key = ?")
# Q 1164 : # self.group_users.any?(&:owner)
Query(GroupUser)
.where("group_id = ?")
# Q 1165 : # self.group_users.create!(user: user, owner: true)
Query(GroupUser)
.where("group_id = ?")
# Q 1166 : # self.group_users.find_by(user: user)
Query(GroupUser)
.where("group_id = ?")
.where("user = ?")
# Q 1167 : # self.group_users.where(owner: true).exists?
Query(GroupUser)
.where("group_id = ?")
.where("owner = ?")
.return_limit('1')
# Q 1168 : # self.group_users.where(user: user).each(&:destroy)
Query(GroupUser)
.where("group_id = ?")
.where("user = ?")
# Q 1169 : # self.groups.pluck(:id)
Query(Group)
.where("category_id = ?")
.select('id')
# Q 1170 : # self.groups.visible_groups(user)
Query(Group)
.where("category_id = ?")
# Q 1171 : # self.includes(:acting_user, :target_user).where(group_id: group.id).order("group_histories.created_at DESC")
Query(GroupHistory)
.includes('acting_user')
.includes('target_user')
.where("group_id = ?")
.order('id, created_at')
# Q 1172 : # self.joined_remotes.where("commits_behind > 0 OR remote_version <> local_version").pluck("themes.name", "themes.id")
Query(ImportError)
.select('themes.namethemes.id')
# Q 1173 : # self.joined_remotes.where("last_error_text IS NOT NULL").pluck("themes.name", "themes.id")
Query(ImportError)
.select('themes.namethemes.id')
# Q 1174 : # self.member_of(groups, user).where("gu.owner")
Query(Group)

# Q 1175 : # self.order(order || "score desc, created_at desc")
Query(InvalidAction)
.order('')
# Q 1176 : # self.pluck(:id, :name).each
Query(TagGroup)
.select('id')
.select('name')
# Q 1177 : # self.posts.where("cooked LIKE '%/_optimized/%'").find_each(&:rebake!)
Query(Post)
.where("upload_id = ?")
# Q 1178 : # self.where("string_to_array(email_in, '|') @> ARRAY[?]", Email.downcase(email)).first
Query(Category)
.return_limit('1')
# Q 1179 : # self.where("string_to_array(incoming_email, '|') @> ARRAY[?]", Email.downcase(email)).first
Query(Group)
.return_limit('1')
# Q 1180 : # self.where("user_id IS NULL AND updated_at < ?", 1.day.ago).delete_all
Query(UserAssociatedAccount)

# Q 1181 : # self.where("viewed_at >= ? AND viewed_at < ?", start_date, end_date + 1.day)
Query(UserProfileView)

# Q 1182 : # self.where(email_type: "signup").order(created_at: :desc).limit(1).pluck(:to_address).first
Query(EmailLog)
.where("email_type = ?")
.order('created_at')
.return_limit('')
.select('to_address')
.return_limit('1')
# Q 1183 : # self.where(id: parent_slug.to_i).pluck(:id).first
Query(Category)
.where("id = ?")
.select('id')
.return_limit('1')
# Q 1184 : # self.where(id: req_id(date, user_agent)).update_all(["count = count + ?", val])
Query(WebCrawlerRequest)
.where("id = ?")
# Q 1185 : # self.where(id: slug_or_id.to_i, parent_category_id: parent_category_id).first
Query(Category)
.where("id = ?")
.where("parent_category_id = ?")
.return_limit('1')
# Q 1186 : # self.where(req_type: i)
Query(ApplicationRequest)
.where("req_type = ?")
# Q 1187 : # self.where(slug: category_slug, parent_category_id: nil).first
Query(Category)
.where("slug = ?")
.where("parent_category_id = ?")
.return_limit('1')
# Q 1188 : # self.where(slug: category_slug, parent_category_id: parent_category_id).first
Query(Category)
.where("slug = ?")
.where("parent_category_id = ?")
.return_limit('1')
# Q 1189 : # self.where(slug: parent_category_slug, parent_category_id: nil).pluck(:id).first
Query(Category)
.where("slug = ?")
.where("parent_category_id = ?")
.select('id')
.return_limit('1')
# Q 1190 : # self.where(slug: parent_slug, parent_category_id: nil).pluck(:id).first
Query(Category)
.where("slug = ?")
.where("parent_category_id = ?")
.select('id')
.return_limit('1')
# Q 1191 : # self.where(slug: slug_or_id, parent_category_id: parent_category_id).first
Query(Category)
.where("slug = ?")
.where("parent_category_id = ?")
.return_limit('1')
# Q 1192 : # self.where(tag_id: topic.tags.map(&:id), category_id: from_category_id).where("topic_count > 0").update_all("topic_count = topic_count - 1")
Query(CategoryTagStat)
.where("tag_id = ?")
.where("category_id = ?")
# Q 1193 : # self.where(target_user_id: user.id, action: UserHistory.actions[action_type])
Query(UserHistory)
.where("target_user_id = ?")
.where("action = ?")
# Q 1194 : # self.where(target_user_id: user.id, action: UserHistory.actions[action_type])
Query(UserHistory)
.where("target_user_id = ?")
.where("action = ?")
# Q 1195 : # self.where(user: user, notification_level: notification_levels[level])
Query(CategoryUser)
.where("user = ?")
.where("notification_level = ?")
# Q 1196 : # self.with_filters(opts.slice(*staff_filters)).only_staff_actions.order("id DESC").includes(:acting_user, :target_user)
Query(UserHistory)
.order('id')
.includes('acting_user')
.includes('target_user')
# Q 1197 : # tag.categories.where(id: guardian.allowed_category_ids).pluck(:name)
Query(Category)
.where("tag_id = ?")
.where("id = ?")
.select('name')
# Q 1198 : # tag.tag_groups
Query(TagGroup)
.where("tag_id = ?")
# Q 1199 : # tag.tag_groups.include?(tag_group)
Query(TagGroup)
.where("tag_id = ?")
# Q 1200 : # tag.tag_users.where(user: user).first.try(:notification_level)
Query(TagUser)
.where("tag_id = ?")
.where("user = ?")
.return_limit('1')
.select('notification_level')
# Q 1201 : # tags.find
Query(Tag)
.where("id = ?")
# Q 1202 : # theme.color_schemes.find_by(name: name)
Query(ColorScheme)
.where("theme_id = ?")
.where("name = ?")
# Q 1203 : # theme.color_schemes.pluck(:name, :id).flatten
Query(ColorScheme)
.where("theme_id = ?")
.select('name')
.select('id')
# Q 1204 : # theme.theme_fields.pluck(:id)
Query(ThemeField)
.where("theme_id = ?")
.select('id')
# Q 1205 : # theme.theme_fields.where(target_id: ThemeField.basic_targets.map { |t|
#   
#   Theme.targets[t.to_sym]
# }, name: ThemeField.scss_fields + ThemeField.html_fields)
Query(ThemeField)
.where("theme_id = ?")
.where("target_id = ?")
.where("name = ?")
# Q 1206 : # theme.theme_fields.where(target_id: ThemeField.basic_targets.map { |t|
#   
#   Theme.targets[t.to_sym]
# }, name: ThemeField.scss_fields)
Query(ThemeField)
.where("theme_id = ?")
.where("target_id = ?")
.where("name = ?")
# Q 1207 : # theme.theme_settings.to_a.find
Query(ThemeSetting)
.where("theme_id = ?")
.where("id = ?")
# Q 1208 : # theme.theme_translation_overrides.to_a.find
Query(ThemeTranslationOverride)
.where("theme_id = ?")
.where("id = ?")
# Q 1209 : # theme_fields.find
Query(ThemeField)
.where("id = ?")
# Q 1210 : # theme_fields.joins(:upload).where(type_id: ThemeField.types[:theme_upload_var]).each
Query(ThemeField)
.joins('upload')
.where("type_id = ?")
# Q 1211 : # theme_fields.where(target_id: Theme.targets[:extra_js]).pluck(:value_baked).join("\n")
Query(ThemeField)
.where("target_id = ?")
.select('value_baked')
# Q 1212 : # theme_fields.where(type_id: ThemeField.types[:theme_upload_var]).each
Query(ThemeField)
.where("type_id = ?")
# Q 1213 : # themes.find(&:default?)
Query(Theme)
.where("id = ?")
# Q 1214 : # top_topics.joins(:topic).where("topics.category_id = ?", category_id)
Query(TopTopic)
.joins('topic')
# Q 1215 : # topic.all_allowed_users.where(id: @user.id).exists?
Query(Topic)
.where("id = ?")
.return_limit('1')
# Q 1216 : # topic.allowed_groups.joins(:group_users).where(publish_read_state: true).select("ARRAY_AGG(group_users.user_id) AS members", :name, :id).group("groups.id")
Query(Topic)
.where("publish_read_state = ?")
.select('ARRAY_AGG(group_users.user_id) AS members')
.group('id')
# Q 1217 : # topic.allowed_groups.where("topic_allowed_groups.group_id IN (?)", group_ids).pluck(:id)
Query(Topic)
.select('id')
# Q 1218 : # topic.allowed_users.pluck(:id)
Query(Topic)
.select('id')
# Q 1219 : # topic.allowed_users.pluck(:id)
Query(Topic)
.select('id')
# Q 1220 : # topic.category.groups.pluck("groups.id")
Query(Topic)
.select('groups.id')
# Q 1221 : # topic.category.visible_group_names(user).where(automatic: false).pluck(:name).join(", ")
Query(Topic)
.where("automatic = ?")
.select('name')
# Q 1222 : # topic.posts.where("NOT hidden AND post_type in (?)", Topic.visible_post_types).count("distinct user_id")
Query(Topic)

# Q 1223 : # topic.posts.where(post_number: post_number).first
Query(Topic)
.where("post_number = ?")
.return_limit('1')
# Q 1224 : # topic.posts.with_deleted.order(:post_number).first
Query(Topic)
.order('post_number')
.return_limit('1')
# Q 1225 : # topic.tags.pluck(:name)
Query(Topic)
.select('name')
# Q 1226 : # topic.topic_allowed_groups.joins("
#           LEFT JOIN group_users gu
#           ON topic_allowed_groups.group_id = gu.group_id
#           AND gu.user_id = #{
# @user.id.to_i}
#         ").where("gu.group_id IS NOT NULL").pluck(:group_id)
Query(Topic)
.select('group_id')
# Q 1227 : # topic.topic_allowed_groups.pluck(:group_id)
Query(Topic)
.select('group_id')
# Q 1228 : # topic.topic_allowed_groups.where("group_id IN (SELECT group_id FROM group_users WHERE user_id = ?)", user.id).exists?
Query(Topic)
.return_limit('1')
# Q 1229 : # topic.topic_allowed_users.where(user_id: -2).exists?
Query(Topic)
.where("user_id = ?")
.return_limit('1')
# Q 1230 : # topic.topic_allowed_users.where(user_id: user.id).exists?
Query(Topic)
.where("user_id = ?")
.return_limit('1')
# Q 1231 : # topic_allowed_group.group.set_message_default_notification_levels!(topic)
Query(Group)
.where("id = ?")
# Q 1232 : # topic_allowed_groups.find_by(group_id: group.id)
Query(TopicAllowedGroup)
.where("group_id = ?")
# Q 1233 : # topic_allowed_users.find_by(user_id: user.id)
Query(TopicAllowedUser)
.where("user_id = ?")
# Q 1234 : # topic_links.includes(:post).where.not(post_id: disagreed_flag_post_ids).each
Query(TopicLink)
.includes('post')
.where("post_id = ?")
# Q 1235 : # topic_timer.errors.add(:execute_at, :invalid)
Query(TopicTimer)

# Q 1236 : # topic_timers.find_by(deleted_at: nil, public_type: false, user_id: user.id)
Query(TopicTimer)
.where("deleted_at = ?")
.where("public_type = ?")
.where("user_id = ?")
# Q 1237 : # topic_timers.find_by(deleted_at: nil, public_type: true)
Query(TopicTimer)
.where("deleted_at = ?")
.where("public_type = ?")
# Q 1238 : # topics.find
Query(Topic)
.where("id = ?")
# Q 1239 : # topics.joins("LEFT OUTER JOIN top_topics ON top_topics.topic_id = topics.id").order(TopicQuerySQL.order_top_with_notification_levels(score))
Query(Topic)
.order('')
# Q 1240 : # topics.map do |params|
#   
#   post = find_post(params[:site_setting_name])
#   next unless post
#   { id: params[:site_setting_name], name: post.topic.title, selected: unchanged?(post) }
#   
# end.compact
Query(Topic)
.where("id != 0")
# Q 1241 : # topics.order(TopicQuerySQL.order_top_for(score))
Query(Topic)
.order('')
# Q 1242 : # topics.order(TopicQuerySQL.order_top_with_pinned_category_for(score))
Query(Topic)
.order('')
# Q 1243 : # topics.pluck(:id)
Query(Topic)
.select('id')
# Q 1244 : # topics.where("COALESCE(users.trust_level, 0) > 0")
Query(Topic)

# Q 1245 : # topics.where("EXISTS ( SELECT 1 FROM topic_tags WHERE topic_tags.topic_id = topics.id AND tag_id NOT IN (?) )
#         OR NOT EXISTS (SELECT 1 FROM topic_tags WHERE topic_tags.topic_id = topics.id)", muted_tag_ids)
Query(Topic)

# Q 1246 : # topics.where("NOT ( #{
# pinned_clause} )")
Query(Topic)

# Q 1247 : # topics.where("category_id = ?", params[:category_id])
Query(Topic)

# Q 1248 : # topics.where("topics.category_id NOT IN (?)", muted_category_ids)
Query(Topic)

# Q 1249 : # topics.where("topics.id NOT IN (?)", category_topic_ids)
Query(Topic)

# Q 1250 : # topics.where("tu.last_visited_at IS NOT NULL").order("tu.last_visited_at DESC")
Query(Topic)
.order('id, last_visited_at')
# Q 1251 : # topics.where(user_id: user.id)
Query(Topic)
.where("user_id = ?")
# Q 1252 : # upload.optimized_images.find_each(&:destroy!)
Query(OptimizedImage)
.where("upload_id = ?")
# Q 1253 : # uploads.joins("LEFT JOIN #{
# table_name} ON #{
# table_name}.etag = model.etag").where("#{
# table_name}.etag IS NULL AND model.etag IS NOT NULL")
Query(Upload)

# Q 1254 : # user.badges.where(name: title, allow_title: true).exists?
Query(Badge)
.where("user_id = ?")
.where("name = ?")
.where("allow_title = ?")
.return_limit('1')
# Q 1255 : # user.email_change_requests.where(new_email_token_id: token.id).first
Query(EmailChangeRequest)
.where("user_id = ?")
.where("new_email_token_id = ?")
.return_limit('1')
# Q 1256 : # user.email_tokens.active.where(email: user.email).exists?
Query(EmailToken)
.where("user_id = ?")
.where("email = ?")
.return_limit('1')
# Q 1257 : # user.group_users.pluck(:group_id)
Query(GroupUser)
.where("user_id = ?")
.select('group_id')
# Q 1258 : # user.groups.where(title: title).exists?
Query(Group)
.where("user_id = ?")
.where("title = ?")
.return_limit('1')
# Q 1259 : # user.notifications.order("notifications.created_at DESC").where(id: ids).joins(:topic).limit(count)
Query(Notification)
.where("user_id = ?")
.order('id, created_at')
.where("id = ?")
.joins('topic')
.return_limit('')
# Q 1260 : # user.notifications.visible.recent(count).includes(:topic)
Query(Notification)
.where("user_id = ?")
.includes('topic')
# Q 1261 : # user.primary_group.try(:name)
Query(Group)
.where("id = ?")
.select('name')
# Q 1262 : # user.security_keys.select(:credential_id).where(factor_type: UserSecurityKey.factor_types[:second_factor]).pluck(:credential_id)
Query(UserSecurityKey)
.where("user_id = ?")
.select('credential_id')
.where("factor_type = ?")
.select('credential_id')
# Q 1263 : # user_badges.includes(:user, :granted_by, badge: :badge_type, post: :topic, user: :primary_group)
Query(UserBadge)
.includes('user')
.includes('granted_by')
# Q 1264 : # user_badges.includes(badge: [:badge_grouping, :badge_type]).includes(post: :topic).includes(:granted_by)
Query(UserBadge)
.includes('granted_by')
# Q 1265 : # user_badges.where(user_id: user_id)
Query(UserBadge)
.where("user_id = ?")
# Q 1266 : # user_stat.try(:first_post_created_at)
Query(UserStat)
.select('first_post_created_at')
# Q 1267 : # user_visits.find_by(visited_at: date)
Query(UserVisit)
.where("visited_at = ?")
# Q 1268 : # users.includes(:secure_categories).where("users.admin = TRUE OR categories.id = ?", topic.category.id).references(:categories)
Query(User)
.includes('secure_categories')
# Q 1269 : # users.includes(:user_search_data).references(:user_search_data).where("user_search_data.search_data @@ #{
# query}").order(DB.sql_fragment("CASE WHEN username_lower LIKE ? THEN 0 ELSE 1 END ASC", @term_like))
Query(User)
.order('')
# Q 1270 : # users.joins("INNER JOIN group_users ON group_users.user_id = users.id").where("group_users.group_id IN (?)", @groups.map(&:id))
Query(User)

# Q 1271 : # users.order("NOT group_users.owner").order(order).order(username_lower: dir).limit(limit).offset(offset).includes(:primary_group)
Query(User)
.order('id, owner')
.order('')
.order('username_lower')
.return_limit('')
.includes('primary_group')
# Q 1272 : # users.order(order).order(username_lower: dir).where("group_users.owner").includes(:primary_group)
Query(User)
.order('')
.order('username_lower')
.includes('primary_group')
# Q 1273 : # users.pluck(:username).join(",")
Query(User)
.select('username')
# Q 1274 : # users.select("users.*, group_requests.reason, group_requests.created_at requested_at").order(params[:order] == "requested_at" ? "group_requests.created_at #{
# dir}" : "").order(username_lower: dir).limit(limit).offset(offset)
Query(User)
.select('users.*, group_requests.reason, group_requests.created_at requested_at')
.order('')
.order('username_lower')
.return_limit('')
# Q 1275 : # users.where(staged: false)
Query(User)
.where("staged = ?")
# Q 1276 : # where("
#           (
#             posts.cooked LIKE '%<a %' OR
#             posts.cooked LIKE '%<img %' OR
#             posts.cooked LIKE '%<video %'
#           ) AND (
#             posts.cooked LIKE ? OR
#             posts.cooked LIKE '%/original/%' OR
#             posts.cooked LIKE '%/optimized/%' OR
#             posts.cooked LIKE '%data-orig-src=%' OR
#             posts.cooked LIKE '%/uploads/short-url/%'
#           )", "%/uploads/#{
# RailsMultisite::ConnectionManagement.current_db}/%")
Query(Post)

# Q 1277 : # where("1 = 0")
Query(Category)

# Q 1278 : # where("EXISTS(
#           SELECT 1
#     FROM user_emails
#           WHERE user_emails.user_id = :user_id AND
#           (incoming_emails.to_addresses ILIKE '%' || user_emails.email || '%' OR
#                  incoming_emails.cc_addresses ILIKE '%' || user_emails.email || '%')
# )
# ", user_id: user.id)
Query(IncomingEmail)

# Q 1279 : # where("NOT categories.read_restricted")
Query(Category)

# Q 1280 : # where("NOT is_bounce AND error IS NOT NULL")
Query(IncomingEmail)

# Q 1281 : # where("action IN (?)", UserHistory.staff_action_ids)
Query(UserHistory)

# Q 1282 : # where("data::json ->> 'display_username' = ?", username).where(notification_type: notification_type).order(created_at: :desc)
Query(Notification)
.where("notification_type = ?")
.order('created_at')
# Q 1283 : # where("lower(name) IN (?)", name)
Query(Tag)

# Q 1284 : # where("posts.created_at > ?", time_ago)
Query(Post)

# Q 1285 : # where("posts.post_type IN (?)", Topic.visible_post_types(guardian.user))
Query(Post)

# Q 1286 : # where("score >= ?", min_score_for_priority)
Query(InvalidAction)

# Q 1287 : # where("silenced_till IS NOT NULL AND silenced_till > ?", Time.zone.now)
Query(User)

# Q 1288 : # where("silenced_till IS NULL OR silenced_till <= ?", Time.zone.now)
Query(User)

# Q 1289 : # where("suspended_till IS NOT NULL AND suspended_till > ?", Time.zone.now)
Query(User)

# Q 1290 : # where("suspended_till IS NULL OR suspended_till <= ?", Time.zone.now)
Query(User)

# Q 1291 : # where("topics.archetype <> ?", Archetype.private_message)
Query(UserExists)

# Q 1292 : # where("topics.category_id IS NULL OR topics.category_id IN (SELECT id FROM categories WHERE #{
# condition[0]})", condition[1])
Query(UserExists)

# Q 1293 : # where("topics.created_at > ?", time_ago)
Query(UserExists)

# Q 1294 : # where("topics.subtype = ?", subtype)
Query(UserExists)

# Q 1295 : # where("uploads.id > ?", SEEDED_ID_THRESHOLD)
Query(Upload)

# Q 1296 : # where("user_selectable OR id = ?", SiteSetting.default_theme_id)
Query(Theme)

# Q 1297 : # where("users.id > 0")
Query(User)

# Q 1298 : # where("visited_at >= ? and visited_at <= ?", start_date.to_date, end_date.to_date)
Query(UserVisit)

# Q 1299 : # where(active: true)
Query(User)
.where("active = ?")
# Q 1300 : # where(archetype: Archetype.private_message)
Query(UserExists)
.where("archetype = ?")
# Q 1301 : # where(bounced: true)
Query(EmailLog)
.where("bounced = ?")
# Q 1302 : # where(confirmed: false)
Query(EmailToken)
.where("confirmed = ?")
# Q 1303 : # where(date: date, req_type: req_type_id).pluck(:id).first
Query(ApplicationRequest)
.where("date = ?")
.where("req_type = ?")
.select('id')
.return_limit('1')
# Q 1304 : # where(disagreed_at: nil, deferred_at: nil, agreed_at: nil, deleted_at: nil)
Query(PostAction)
.where("disagreed_at = ?")
.where("deferred_at = ?")
.where("agreed_at = ?")
.where("deleted_at = ?")
# Q 1305 : # where(expired: false).where("created_at > ?", valid_after)
Query(EmailToken)
.where("expired = ?")
# Q 1306 : # where(factor_type: UserSecurityKey.factor_types[:second_factor], enabled: true)
Query(UserSecurityKey)
.where("factor_type = ?")
.where("enabled = ?")
# Q 1307 : # where(method: UserSecondFactor.methods[:backup_codes], enabled: true)
Query(UserSecondFactor)
.where("method = ?")
.where("enabled = ?")
# Q 1308 : # where(method: UserSecondFactor.methods[:totp])
Query(UserSecondFactor)
.where("method = ?")
# Q 1309 : # where(method: UserSecondFactor.methods[:totp], enabled: true)
Query(UserSecondFactor)
.where("method = ?")
.where("enabled = ?")
# Q 1310 : # where(post_action_type_id: PostActionType.notify_flag_type_ids)
Query(PostAction)
.where("post_action_type_id = ?")
# Q 1311 : # where(post_action_type_id: PostActionType.public_type_ids)
Query(PostAction)
.where("post_action_type_id = ?")
# Q 1312 : # where(post_action_type_id: PostActionType.types[:spam])
Query(PostAction)
.where("post_action_type_id = ?")
# Q 1313 : # where(primary: false)
Query(UserEmail)
.where("primary = ?")
# Q 1314 : # where(read: false)
Query(Notification)
.where("read = ?")
# Q 1315 : # where(target_id: Theme.targets[:translations], name: locale_codes).joins(self.sanitize_sql_array(["JOIN (
#         SELECT * FROM (VALUES #{
# locale_codes.map {
#   
#   "(?)"
# }.join(",")}) as Y (locale_code, locale_sort_column)
#       ) as Y ON Y.locale_code = theme_fields.name", *locale_codes.map.with_index { |code, index|
#   
#   [code, index]
# }])).order("Y.locale_sort_column")
Query(ThemeField)
.where("target_id = ?")
.where("name = ?")
.order('id, locale_sort_column')
# Q 1316 : # where(theme_id: theme_ids).joins("JOIN (
#           SELECT #{
# theme_ids.map.with_index { |id, idx|
#   
#   "#{
#   id.to_i} AS theme_id, #{
#   idx} AS theme_sort_column"
# }.join(" UNION ALL SELECT ")}
#         ) as X ON X.theme_id = theme_fields.theme_id").order("theme_sort_column")
Query(ThemeField)
.where("theme_id = ?")
.order('id, theme_sort_column')
# Q 1317 : # where(topic_count: 0, pm_topic_count: 0)
Query(Tag)
.where("topic_count = ?")
.where("pm_topic_count = ?")
# Q 1318 : # where(user: user, notification_level: notification_levels[level])
Query(TagUser)
.where("user = ?")
.where("notification_level = ?")
# Q 1319 : # where(user_id: user_id, given_date: date)
Query(GivenDailyLike)
.where("user_id = ?")
.where("given_date = ?")
# Q 1320 : # where(visible: true)
Query(UserExists)
.where("visible = ?")
